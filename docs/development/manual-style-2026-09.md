# マニュアル執筆規約(user レポート #779 §6。2026-09-08 に置いた)

> **なぜ要るか(user の言葉)** ── 「**構造を直しても、書き方が変わらなければ 3 日で
> 2 万字ずつ元に戻る。**」

⚠ **正本は `docs/manual.md` 1 本**である(`src/adapter/ui/render/help.ts` が `?raw` で
焼き込む)。この doc は**その書き方**を決めるだけで、**残件は持たない**
(CLAUDE.md「仕事を doc の形で置かない」── burn-down は issue に在る)。

## 節の型

```
### 〈動詞で終わる見出し・日付を入れない〉

> 一行で:  この節で何ができるか(1 文・40 字以内)

**手順**
1. …

**覚えること**            ← 0〜3 個。4 個目が出たら節を割る

**つまずきやすい点**      ← 無ければ枠ごと省く

関連: 〈節〉 / 早見表: 〈表〉 / 仕組み: D-n
```

🔑 **型が効く理由は 3 つ**(user レポート §6-1):

1. **足す場所が一意に決まる** ──「一行で」が書けるなら既存節の改稿。書けないなら新設。
   判定が機械的になるので、**「そのほか」へ足す動機が消える**
2. **節の適正サイズが枠の数で分かる** ──「覚えること」が 4 個を超えたら太りすぎ
3. **読み飛ばせる** ── 全節が同じ順序なら、2 節目からは「手順だけ見る」ができる

## 規約 11 条(user レポート §6-2。**条 10 以外は原文のまま**)

1. **見出しに日付を入れない。** 悪い「数式(2026-09-06 に足しました)」/
   良い「数式を書く」+ 変更履歴に 1 行。⚠ 日付は 1 か月で意味を失い、見出しだけが長く残る
2. **節番号を振らない。** ⚠ 前に挿入したくなったとき `4-0b` / `4-8b` が生まれる。順序は目次が示す
3. **見出しは動詞で終える。** 悪い「録音・画面収録」「そのほか」/ 良い「録音・画面収録を入れる」
4. **「そのほか」という見出しを作らない。** 行き先が無いものは、名前を付けて節にするか既存節へ吸収する
5. **見出しは 3 段まで**(章 H2 / 節 H3 / 小節 H4)。H5 は使わない。
   H4 を作りたくなったら、まず「覚えること」に吸収できないか考える
6. **同じ名前の見出しを 2 つ作らない。** ⚠ 検索で引いたとき区別できない
7. **新機能は「追記」ではなく「該当節の改稿」。** 型の「一行で」が書けるなら既存節に収める。
   書けないときだけ節を新設し、**章の流れ順に挿入する。末尾に足さない**
8. **ガイドに一覧を書かない。早見表に説明を書かない。**
9. **1 節 = 1 画面(600〜800 字)。**「覚えること」が 4 個を超えたら割る

    > ⚠ **この条だけ「0 件」にできない**(2026-09-08 実測:93 節中 **46 節**が超過)。
    > 既存分を全部割るのは**本文を書き直す仕事**なので、原文 §7 Step 8 のとおり
    > **読まれる節から随時**縮める。
    > 🔑 機械の門は「**増やさない**」形で置いた(`tests/docs-parity.test.ts` の
    > `OVER_800`)── **新しく 800 字を超える節を作ると落ちる**し、
    > **縮めた節は表から消さないと落ちる**(burn-down が記録に残る)。
    > ⚠ 数え方も同じ所で決めてある(節 = `###` / 空白を除く / 囲みの中も数える)。
10. **記号は 🔴 ⚠ 🔑 の 3 種のみ。** 凡例を各巻の先頭に置く
    (🔴 データを失う恐れ / ⚠ 片道(戻せない)/ 🔑 知っておくと速い)

    > ⚠ **この条だけ原文と字が違う**(user 裁定 2026-09-08)。原文は
    > 「**記号は 🔴 ⚠ 💡 の 3 種のみ**(… / 💡 知っておくと速い)」だったが、
    > `docs/manual.md` は最初から **`🔑`** を使っており(実測 294 件・`💡` は 0 件)、
    > **役割は同じで字だけが違った**。どちらへ揃えるかを user へ出したところ
    > 「**🔑 のまま(規約の側を直す)**」── 本文 294 行を書き換えるより、
    > 規約の 1 字を直すほうが user の見え方を変えない。
11. **独自の語**(解放・居場所・参照元・雛形・紙面・面・板・札・小窓)は
    **用語集に登録してから使う。本文で言い換えない**

## どれを機械が守っているか

⚠ **散文の規律にしない**(CLAUDE.md「上限は数で持つ」)── 守れるものは test に置く。
🔑 **置けていない条は「まだ守っていない」と書く** ── 守った顔をさせない。

🔑 **守れていない条にも門は置ける ── ただし「0 件」ではなく「増えない」の門である**
(2026-09-08)。⚠ 守れないことを理由に**何も置かない**と、次に書く人が
`##### …` や 2 つ目の「そのほか」を足しても**誰も止められない**(直す前より遠ざかる)。
⚠ 門は**件数の上限ではなく身元の等値 pin** にする ── 上限(`<= 16`)だと
**直しても表を触らずに済む**ので、直したことが記録に残らない。

| 条 | 守り手 |
|---|---|
| 1(日付) | ✅ `tests/docs-parity.test.ts`「マニュアルの見出しは、番号も日付も持たない」 |
| 2(節番号) | ✅ 同上。⚠ 参照の側も `→「名前」`が**ちょうど 1 つの見出しに当たる**ことを見ている |
| 4(そのほか)/ 5(H5)/ 6(同名) | ⚠ **「守っている」ではなく「増えない」** ── いま在るものを**名前で等値 pin** した(`tests/docs-parity.test.ts`)。⚠ 直したら表からも消さないと落ちるので、burn-down が忘れられない |
| 9(節の長さ) | ⛔ **まだ置けない** ── 88 節中 44 節が超過している |
| 10(記号) | ✅ `tests/docs-parity.test.ts`「マニュアルの記号は 🔴 ⚠ 🔑 の 3 種だけ」 |
| 11(用語集) | ⛔ 用語集がまだ無い |
| 3 / 7 / 8 | ⛔ 機械では見られない(書く人が守る) |

## ✅ 規約と本文の食い違いは 0 件になった(2026-09-08)

かつてここには「**条 10 と本文の記号が食い違う**」と書いてあった。裁定
(上の条 10)で**規約の側を 🔑 へ直した**ので、食い違いは無い。

⚠ 揃えたのを機械が見ている(上の表の条 10)。⚠ **表の升は数えない** ──
`| 🟢 できる | …` のような升の印は**注記ではなくデータ**で、3 種の外に居てよい。
🔑 数えるのは**行の頭に立つ記号**である(注記はそこにしか立たない)。

## 直す仕事は issue に在る

この doc は**規約だけ**を持つ。いま規約から外れている分の burn-down は issue にある
── 数と場所はそちらで数える(doc に一覧を置くと、誰も burn down しない)。
