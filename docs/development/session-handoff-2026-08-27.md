# セッション引き継ぎ(2026-08-27)

> ⚠ **この doc は引き継ぎ PR から必ずリンクする**(PKC3 に `INDEX.md` は無い ──
> 孤児 doc を作らないのが目的で、手段は PR 本文のリンクである)。

## 🔴 最初の仕事: **あり**

**#495(Ctrl+クリック = その場から編集 / Alt+クリック = 追記位置)から着手する。**

- **なぜこれか** … user の裁定が**文言まで確定**しており(2026-08-27)、他の issue の
  前提にならない**独立した変更**である。#496 / #497(追記 I/F とリサイズ)は
  同じ面を触るので、先にキーの割当を決めておくと衝突しない
- **どこから** … `src/adapter/ui/render/row-swap.ts:456-467`(いまは**素の左クリックで
  その塊の編集に入る**)。⚠ `target.closest('a,button,input,summary,label')` の
  除外規則と、リンク上でのブラウザ既定の扱いを壊さないこと
- **完了条件** … Ctrl+クリックでその地点から編集に入り、Alt+クリックで追記位置が
  決まり、**両方に smoke がある**こと

### ⚠ ただし着手の前に 1 行だけ確かめる(#495 に書いてある未決)

**素のクリックはどうなるか。** user の字は「Ctrl+クリックを**デフォの**その場からの
編集IN導線に変更してください」なので、🔑 **素のクリックは編集に入らない(読むだけ)**
と読むのが自然。⚠ ただしこれは**既存 user の手が変わる**変更なので、
会話で 1 行確認してから実装する(私は確認済みだが返答は未着 ── 下の「裁定待ち」)。

---

## 1. 現在の状態(実測 2026-08-27)

| | |
|---|---|
| main HEAD | **`7d92353`** 録音が本文に入らないことがあるのを直す(捨てずに預かる)(#490) |
| **open PR** | 🔴 **0 件**(この引き継ぎ PR を除く) |
| 作業 branch | `claude/pkc3-libreoffice-wasm-handoff-60opiq` ── **clean、未 push なし** |
| CI | 直近 6 本すべて緑(#490 は 1 回目で緑) |

---

## 2. 着地したもの(この日、6 本)

| PR | 中身 |
|---|---|
| #484 | 情報ペインに「集まり先」── ノートがどのスマートフォルダに集まっているか |
| #485 | 貼付に「ウェブページの形をそのまま(html の囲み)」を足す |
| **#486** | **見出しを本文と見分けやすくする(帯 = 畳みのボタン、大きさの下限)** |
| #488 | 貼付が「大きすぎて読まなかった」ときの理由を、そう書く |
| #489 | 録音 smoke が落ちた回に「どの段で止まったか」を残す |
| #490 | 録音が本文に入らないことがあるのを直す(捨てずに預かる) |

閉じた issue: **#475**(cowork レポート #16 の 5 項目)/ **#487**(貼付の理由)

---

## 3. 🔴 残件 ── user 報告からの起票 11 件(2026-08-27)

**着手の順に並べる。**なぜその順かも書く。

| 順 | # | 中身 | なぜこの順か |
|---|---|---|---|
| 1 | **#495** | Ctrl / Alt クリックの割当 | 裁定が確定・独立。下 2 つの前提になる |
| 2 | **#496** | 追記 I/F の見出しリストが横幅を変える | 症状が具体的で、直し方も user が指定済み |
| 3 | **#497** | ペインと追記枠のリサイズ(畳めるまで) | 機構を 1 つ作る。#496 と同じ面 |
| 4 | **#491** | 閲覧配布用 HTML が「書き出し」から見つからない | 🔑 **実在するので、動線を足すだけ**(小さい) |
| 5 | **#498 / #499** | 予定表の消せない予定 / 「足す」の日付重複 | 同じ面 ── **まとめて 1 度触る** |
| 6 | **#492** | 埋め込み上限の撤廃 | ⚠ **ワーカーへの移送とセット**でないと画面が固まる |
| 7 | **#494** | タグをその場で打つ | ⚠ frontmatter の修理(#284 系)の上に載る |
| 8 | **#500 / #501** | 右ペインの研究 / ボタンのグルーピング | ⚠ **同時に決める**。片方だけ直すともう片方で崩れる |
| 9 | **#493** | 自動 TOC | 置き場が #500 の結論に依存する |

### そのほかの残件

- **#283 P4 自由配置の板** … ✅ **GO 済み**(user 裁定「板は作って良いです」)。
  段取りは #283 のコメント(a: 位置どおりに描く + b: 掴んで動かすと本文が書き換わる
  を**まとめて 1 本**)。⚠ a だけでは判断材料にならない
- **#426 右クリック 段②** … user が「ここで押しても何も出ない」と言った場所から作る
- 🔴 **#473(録音の間欠)は、診断が答えを出した** ── 下の §6 を読むこと。
  **次に足すのは「刻みは来ていたのか」を割る診断**(#473 の最後のコメントに 3 つ書いた)
- **残りの間欠 4 件**(#387 / #410 / #419 / #382)… 診断は入っている。**次の赤を待つ**段階

---

## 4. 🔴 裁定待ち(2 件)── 催促はしない

1. **#495 の素のクリック** … 「Ctrl+クリックが既定の編集 IN」なら、
   **素のクリックは編集に入らない**でよいか(既存 user の手が変わる)
2. **#493 の自動 TOC の置き場** … 右の情報ペイン(推薦)/ 本文の先頭 / 畳んだ帯

---

## 5. 踏んだ罠と、覆った前提 ── **正本は `.claude` と `CLAUDE.md`**

この日の知見は**資産へ入れた**。ここにはポインタだけ置く。

| 何 | 書いた先 |
|---|---|
| 🔴 **CI は手元より速い**(実測 CI 4 回 / 手元 7 回)── 「遅いから時間切れ」は思い込み | `CLAUDE.md` §5(環境差) |
| 🔴 **挙動を変えたのに test が前も後も通るなら守っていない** | `CLAUDE.md` §1 |
| 🔴 **user が「無い」と言っても実在を確かめる**(この日 2 件とも在った) | `CLAUDE.md`(起票の作法) |
| **`expect.poll` は最初の一読で当たれば通る** ── 対照群で遷移の完了を観測する | `.claude/skills/smoke-testing/SKILL.md` |
| **診断は落として字を読むまで済んでいない** | `.claude/skills/smoke-testing/SKILL.md` |

### ⚠ この日、同じ型をまた踏んだもの

- **壊れたポインタ** … #493 の本文で**まだ存在しない issue 番号**を参照した(3 度目)。
  🔑 **実体を作ってから導線を書く**。今回は実番号へ直したが、
  **番号がずれていれば別の issue を指していた**
- **計器の古さ** … check-run が `in_progress` のまま完了を知らせない件を**さらに 2 回**踏んだ。
  🔑 **`list_workflow_jobs` を run id で引く**(CLAUDE.md §4 に既出)

---

## 6. 測って「問題なし」と分かったこと(再調査させない)

- **#283 P4 の前提は崩れていない** … 「関係マップが自由配置の代わりになっていないか」を
  確かめた ── ❌ あれは `neighbourhood.ts` が**位置を計算**して**右ペイン**に出す
  「ノートの見方」であり、user は置けない。かんばん / カレンダー / ファイラも
  落とし先は列・日付・フォルダ。🔑 **2 次元の自由座標を持つ面は PKC3 に 1 つも無い**
- **記法は現役** … `:::format{x=120}` → `data-pkc-x="120"` は今も出る
  (⚠ 設計 doc の行番号 `2714-2728` は**古い**。現物は `markdown-render.ts:3160-3162`)
- **`export-html` / `export-portable` は実装済み**(#491 は動線だけの問題)

### 🔴 #473 ── 診断が答えを出した(2026-08-27 22:01、この引き継ぎ PR の CI で)

⚠ **私の見立ては外れていた。** 次のセッションは**この訂正から始めること**。

```
待つ前:       添付 0 件 / 参照 0 件 / 状態「⚠ エラー: 録音できませんでした(1 バイトも録れていません)」
待ち切った後: 添付 0 件 / 参照 0 件 / 状態「⚠ エラー: 録音できませんでした(1 バイトも録れていません)」
```

| 観測 | 読めること |
|---|---|
| **添付 0 件** | 🔴 **添付がそもそも作られていない** ── 「bytes は在るのに本文だけ届かない」**ではない** |
| 状態の行 | 🔴 アプリ自身が「**1 バイトも録れていません**」と言っている |

つまり `capture.ts` の `finish()` の**最初の分岐**(`blob.size === 0`)で終わっている。

🔴 **#490(`ingest()` が捨てていた穴)はこの赤の原因ではない。**
私は「症状が一致する」と書いたが**一致していなかった** ── #490 は実在した穴の修理
なので取り下げないが、**間欠の説明にはならない**。

⚠ **まだ言えないこと**:
- 過去 2 回(#472 / #486)が同じ形かは**分からない**(当時は診断が無い)
- **なぜ 0 バイトになったか**は未観測。⚠ 手前の「1 バイトも積んでいない」を 15 秒待つ
  assert は**通っている**ので、「**刻みは来たのに `stop()` の blob が空**」の
  可能性がある ── ⚠ **可能性であって確かめていない**

---

## 7. やらないと決めたこと(理由つき)

- **#426 段② を先回りして作らない** … この issue 自身が「段①を配って、
  **どこで押されるかを見てから**決める」と門を立てている。⚠ **危険だからではなく、
  作る順番の話**である(前のコメントでそう書かず、user に不信感を与えた ──
  #426 に説明を追記済み)
- **間欠 smoke に「待ちを伸ばす / 検査を外す」対処をしない** … #382 で
  「緩めたら同じ所で落ちた」を踏んでいる
- **#473 を閉じない** … #489(診断)と #490(穴の修理)が入ったが、
  **CI の赤が直った証拠は 1 つも無い**。条件は「CI で 10 走続けて緑」または
  「診断つきの赤で原因を潰したとき」
