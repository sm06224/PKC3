# ボタンの拍子(リズム)を作り直す ── 名前で揃え、段で受ける(2026-09-21)

> 出どころ:user 指示 2026-09-21(こちらの解釈)。
> **「ボタンの横のサイズが一定ではない。つまり中身の名前にもリズムがない。視覚的にもでこぼこで統一感がなく、ストレス」**
> そして **「サブメニュー化は結構乱暴な解決策だとも理解している。良い案があるなら提案して欲しい」**。
>
> 裁定 2026-09-21:**推奨でよい。実装を進めてよい。**
> ⚠ ただし見え方が変わるので、**名前の全数表は入れる前にもう一度お見せする**(§4)。
>
> 台帳 #1029 / 続き先 #582(操作体系の再設計)・#1017(UI のトータルデザイン)。

## 1. 何が起きているか(実測 2026-09-21)

### 1.1 でこぼこの数

| 並び | ボタン | 名前の幅(全角換算) | 幅の種類 |
|---|---|---|---|
| 編集の帯(`BAR_FORMAT_OPS`) | 15 | **2 〜 14** | 9 |
| 右の列(ノートの操作) | 17 | **3 〜 28** | **17** |

⚠ 右の列は **17 個のボタンが 17 通りの幅**を持っている ── 同じ幅のものが 1 組も無い。

### 1.2 原因は CSS ではなく名前である

`src/styles/app.css` は **「語より細くしない」**(`min-width: max-content`)と決めている。つまり

> **ボタンの幅 = 名前の幅**

高さは `--row-h`(26px)で全部固定されているのに、**横だけが名前の長さの写し**になる。
だから名前の字数がバラバラだと、そのまま右端がガタつく。

🔑 **この規則は事故の後始末であり、戻してはいけない** ── 2026-08-27 に実機で
「**+ ノート**」が器 43px に対し名前 55px で **「+ ノ」に切れていた**(1024px では 28px)。
`min-width: max-content` を入れて切れは **8/8 → 0/8**、段数はどの幅でも増えなかった。
⚠ **固定幅に戻すと、この事故が戻る。**

### 1.3 名前に拍子が無い(同じ列に 4 つの作り方が混在)

| 作り方 | 実例 |
|---|---|
| 名詞 1 語 | 履歴 / 削除 / 添付 / 録音 |
| 動詞(辞書形) | 移す / 書き戻す / 並べ替える |
| 動詞(〜する) | ここから編集する / 時間を計る |
| 指示語つきの文 | **この**スタックを載せる / **この中に**新しいノートを作る |

表記も混在 ── 英語のまま(Word / PDF / PowerPoint)、括弧つき(バックアップ**(このノート)**)、三点リーダつき(移す**…**)。
⚠ **長さが意味と対応していない**(長い名前 = 重い操作でも、よく使う操作でもない)ので、長さを手がかりにできず毎回読む。

### 1.4 既に揃っている物(壊さない)

| | 状態 |
|---|---|
| 高さ | 🟢 **26px に統一済み**(触る端末は 34px)。例外は下の 2 件だけ |
| 字の大きさ | 🟢 **3 段に統一済み**(2026-09-05 #722。直す前は 5 種が混在していた) |
| 主の操作の目印 | 🟢 **1 つの面に 1 つだけ濃くする**機構が在る(`markPrimary`) |

### 1.5 いま壊れている物(見た目の選択ではなく、付け忘れと基準割れ)

| 何が起きているか | 実測 |
|---|---|
| 🔴 **「今日」に絵が付いていない** | 絵の名前を取り違えて呼んでおり、その名前は一覧に無い → 無地。検査も無い |
| 🔴 **「時間を計る」「構成をコピー」も無地** | 一覧に未登録。同じ並びの他のボタンには絵が在るので**混ざって見える** |
| 🔴 **画面下の「開く」「元に戻す」が 18px** | 他は 26px。指で押せる下限(24px)を**この 2 つだけ**下回る |
| 絵の被覆 | 登録 **46** に対し、押せる操作は約 **150** ── 3 個に 1 個しか絵が無い |

## 2. 直し方 ── 4 つの手を、効く順に

### 2.1 【手 1】見出しに動詞を持たせ、ボタンを名詞にする(いちばん効く)

いまボタンが長いのは、**1 個ずつが自分で全部を説明しようとしている**からである。
**塊の見出しが動詞を引き受ければ、ボタンは名詞 1 語で足りる。**
名詞は短く、字数が揃うので、**幅は放っておいても揃う**(§1.2 のとおり幅は名前の写しだから)。

**名前の規則(案)**

| # | 規則 | 理由 |
|---|---|---|
| 1 | **名前は全角 6 字(12 桁)以内**。超える説明は `title` へ逃がす | 12 桁は「バックアップ」が収まる長さ。これを上限にすると段が 2 つで済む |
| 2 | **1 つの塊の中では品詞を揃える**(名詞なら全部名詞) | 拍子は「長さ」だけでなく「形」でも出る |
| 3 | **指示語を書かない**(「この」「その」)── 対象は面が決めている | 右の列は「選んだノートに効く」ことが確定している |
| 4 | **括弧で対象を補わない**(バックアップ(このノート))── 塊の見出しが言う | 括弧は字数を食い、拍子を崩す |
| 5 | **「…」は「押すと次に選ぶ」の印としてだけ使う**(移す… / 目印を選ぶ…) | 意味のある記号にすれば拍子の一部になる |
| 6 | **製品名は英語のまま**(Word / PowerPoint / PDF / Markdown / HTML) | 既に決まっている(`STANDARD_TERMS`) |

### 2.2 【手 2】幅は「段」で受ける(固定幅にはしない)

名前を揃えても端数は出る。そこで **下限だけ**を 2 段で与える:

| 段 | 下限 | 収まる名前 |
|---|---|---|
| **短** | 全角 3 字ぶん | 履歴 / 削除 / 参照 / 添付 / 今日 |
| **長** | 全角 6 字ぶん | バックアップ / 別ウィンドウ / 名前を変える |

🔑 **下限だけを決め、名前が長ければ伸びる** ── だから §1.2 の事故(字が切れる)は起きない。
⚠ 段を 3 つ以上にしない(3 つ目を作ると、また「どれに入れるか」の判断が毎回生える)。

### 2.3 【手 3】塊は「線」ではなく「間」で切る

区切り線を引くと、**無彩色の地に線が増える**(思想③「地は無彩色、色は情報にだけ」)。
代わりに**間(すき間)の大きさを 2 段**にする ── 塊の中は詰め、塊の間だけ空ける。
部品も色も増やさずに拍子が出る。

⚠ いま左の列の下だけが区切り線を持っている(`collection-bar`)。**そこも間に揃える**か、
**線を正式な作法にする**かは、どちらかに倒す(混在をやめる)。→ 推薦は**間**。

### 2.4 【手 4】絵の混在をやめる

**1 つの並びの中では、全部に絵を付けるか、全部字だけにするか**のどちらかにする。
⚠ 全部に付けると幅が増えるので、**8 幅で測ってから**決める(過去にタブと左の列で
「絵を付けたら入り切らず段が増えた」実測がある ── 544px → 362px で段が 3 → 2 に戻った)。

**いまの混ざり具合(実測 2026-09-21。段 A で 3 件直した後の数字)**

| 並び | 絵あり / 全体 | 絵の無いもの |
|---|---|---|
| **右の列** 写す | **0 / 2** | 参照をコピー / 素の Markdown |
| **右の列** 開く | **0 / 2** | 別のウィンドウで開く / このスタックを載せる |
| **右の列** 書き出す | 5 / 7 | バックアップ(このフォルダ)/ PowerPoint |
| **右の列** このノート | 1 / 4 | 名前を変える / 移す… / この中に新しいノートを作る |
| 左の列 | 13 / 17 | 収録とタイマーの「止める」「捨てる」、アラームの「開く」「閉じる」 |
| 中央の帯 | 7 / 10 | 素の Markdown / 書式つき / 選んだ所 の 3 つのコピー |
| 追記欄 | 2 / 5 | 取り消す / 追記する / 強制解放 |
| フォルダ | 1 / 4 | 条件を足す / まとめてタグを付ける・外す |

**書体に無い絵を数えた(2026-09-21)**:いま絵が無い 16 件のうち、
**既に書体に在る絵で素直に言えるのは 7 件**だけである
(止める → 停止 / 捨てる → ごみ箱 / 開く → 時計 / 閉じる → ×/ スタック / フォルダ / 名前を変える → 鉛筆)。
🔴 残りは **書体に「写す」「移す」「別の窓」「スライド」の絵が 1 つも無い**
(`copy` / `move` / `window` / `slides` は 76 件のどれにも無い)。

**だから塊ごとに向きが違う** ── 推薦は「全部付ける」でも「全部外す」でもなく、**塊ごとに倒す**:

| 塊 | 推薦 | 理由 |
|---|---|---|
| **左の列** | 🟢 **足りない 4 つを足して、全部付ける** | 止める / 捨てる / 開く / 閉じる は**既に書体に在る**。1 文字も焼き足さずに揃う |
| **中央の帯** | 🟡 **「写す」の絵を 1 つ焼き足して、全部付ける** | 足りないのは写す系 3 つだけ。**1 文字足せば**揃う |
| 🔴 **右の列** | 🔴 **絵を外して、全部字だけにする** | 9 個に**それらしい絵を当てる**ことになるが、書き出し系は**同じ絵になりがち**で、当てた絵ほど意味が薄い。**8 個外すほうが読みやすく、幅も縮む**(手 2 と同じ向き) |
| 追記欄 / フォルダ / コレクション命令 | 🟡 保留 | 数が少なく、どちらに倒しても効果が小さい。段 C の後に測って決める |

🔑 **「全部に付ける」を選ばない理由は、絵が足りないからではなく、意味が薄いからである** ──
絵は**区別が付くとき**だけ足す。⚠ 区別の付かない絵を並べると、#722 の実測
(「**50 個のうち 23 個が完全に同じ見た目**」)を**絵でもう一度作る**ことになる。

🔑 **これが分かったら覆る**:右の列を字だけにして、user が
「**どれがどれだか探しにくくなった**」と言うなら、**絵ではなく塊の見出し**(手 1)を強くする。

🔑 **塊ごとに全部付けるか、全部無しかへ倒す** ── いちばん読みにくいのは
「**写す**」と「**開く**」のように**塊の中で 0 / 2 なのに、隣の塊では 5 / 7 在る**形である。
⚠ 「全部に付ける」を選ぶなら、**新しい絵を 16 個足すことになる**(足す手順は §1.5 の
登記と書体の 2 か所)── そこまでするかは 8 幅の実測を見てから決める。

## 3. サブメニューの使い所(user の案への回答)

**乱暴になるかどうかは中身で決まる。**

| ✅ 入れてよい | ❌ 入れてはいけない |
|---|---|
| **押した後にどのみちもう一段選ぶ物**(図の種類 / 日付 / 雛形 ── 既にこの形が在る) | **よく押す物**(バックアップ / + ノート) |
| **同じ動詞の変種の束**(「書き出す」の 5 形式 ── 動詞は 1 つで形式だけ違う) | **緊急のときに押す物**(録音・画面録画を**止める**) |
| | **初めての人が探す物**(システム / ヘルプ) |
| | **戻り道**(‹ › / やめる) |

🔑 推薦は「**書き出す形式だけを束ねる**」。⚠ ただし **バックアップは束から出して外に残す**(いちばん押すため)。
⚠ 制約:PKC3 は**折りたたみ部品を使わない**と決めており検査で固定されているので、
サブメニューは「押すと下に出る」形を自前で作る = **部品が 1 つ増える**。
増やすなら、それに見合う所(= 変種の束)だけに絞る。

## 4. Before / After(名前の全数表。⚠ 入れる前に user に見せる)

### 4.0 塊の見出しを、どこにどう置くか(段 C の前提)

> 🔴 **段 B の実機で分かった、見出しが要るもう 1 つの理由**(2026-09-21)。
> 右の列は狭いので**塊の境目の多くが「折り返し」と重なる**(実測:境目 5 か所のうち **4 か所**)。
> ⚠ そのとき**塊の間の縦の空きは 1px** で、**塊の中の折り返しと見分けが付かない**
> ── 横に並んだときだけ 8px で切れて見える。
> 🔑 縦の空きを広げれば直るが、**狭い画面で帯が 25% 高くなる**(実測 140px → 約 175px の見込み)。
> 🔑 **段 C の見出しがこれを解く** ── 新しい行が「**書き出す**」から始まれば、
> それが塊の頭だと一目で分かるので、**縦の空きを買わずに済む**。
> ⚠ 段 C の後もまだ分かりにくければ、そこで初めて縦の空きを広げる(順番を逆にしない)。


手 1 は「**見出しが動詞を引き受ける**」ので、**見出しが無いと名前を短くできない**。
実際、短くできる 6 件は**全部見出しに寄りかかっている**(「バックアップ」がノートのものか
フォルダのものかは、見出しでしか区別できない)。

| 置き方 | 縦に食う量 | 読みやすさ | 判定 |
|---|---|---|---|
| **塊の先頭に、同じ行で小さく置く**(押せない字) | **ほぼ 0**(行は既に在る) | 塊の先頭が「写す」から始まるので、目が塊の頭を掴める | 🟢 **推薦** |
| 塊の上に 1 行の見出しを置く | 6 塊 × 1 行 ≈ **96px** | いちばん読みやすい | ⚠ 右の列は縦が狭い。本文を押し下げないが、操作の帯が伸びる |
| 見出しを置かない | 0 | 🔴 **名前を短くできない**(上の理由) | ✗ |

🔑 **推薦は「同じ行で、塊の先頭に小さく」** ── 縦を食わずに、名前を短くする条件を満たす。
⚠ 横は食う(6 塊 × 4〜6 桁)ので、**折り返しが 1 段増えるかを 8 幅で実測してから**入れる。
⚠ 見出しは**押せない字**にする(押し所を増やさない ── 押せそうに見えて押せないのが
いちばん悪い)。色は使わず、**薄い字**で置く(地は無彩色、色は情報にだけ)。

🔑 **これが分かったら覆る**:実測で**折り返しが 2 段以上増える**なら、見出しをやめて
**名前を「動詞まで入る長さ」(12 桁以内)で作り直す**(「参照をコピー」「ノートを作る」の形)──
そのときは短くできない 3 件(バックアップ 2 種 / スタック)だけ、別の言い方を探す。

### 4.0-a 🔴 実測した ── 見出しは入れない(2026-09-21。§4.0 の「覆る条件」が発火)

§4.0 は推薦の隣に条件を書いていた:「**実測で折り返しが 2 段以上増えるなら、見出しをやめて
名前を作り直す**」。**実測したら、増えた。**

測り方:`npm run build` した `dist/` を配って実ブラウザで開き、ノート 1 件を選んで
右の列の操作の帯を見る。**同じ頁のまま**見出しに `display:none` を付け外しして
A(無し)/ B(在り)を採る(別の起動で測ると別の条件が混ざる)。段数 =
見えているボタンの `top` を丸めた相異なる値の個数。⚠ `when` で畳んだボタンは
`hidden` のまま残るので、**見えている物だけ**数える(最初の稿はここで空振りした)。

| 幅 | ノート A→B(差) | フォルダ A→B(差) |
|---|---|---|
| 1152 | 7 → 9(**+2**) | 9 → 10(+1) |
| 1280 | 7 → 9(**+2**) | 9 → 10(+1) |
| 1366 | 7 → 9(**+2**) | 9 → 10(+1) |
| 1440 | 7 → 9(**+2**) | 9 → 10(+1) |
| 1600 | 4 → 8(**+4**) | 6 → 10(**+4**) |
| 1728 | 4 → 7(**+3**) | 6 → 9(**+3**) |
| 1920 | 4 → 6(**+2**) | 6 → 8(**+2**) |

⚠ 1024px は**タブレットの版面**(情報は畳まれていて「情報」を押すまで出ない)なので
別の性質である ── 参考値として外した。

🔑 **なぜ広い側のほうが差が大きいのか**(直感に反する):右の列は
`minmax(220px, 15vw)` なので、**画面を広げても 220 → 288px しか広がらない**。
狭いときは塊がどのみち単独で折り返すが、広いと**塊どうしが同じ行に相乗り**する ──
見出しはその相乗りを崩すので、**広いほうが失う段が多い**。

🔴 **だから見出しは入れない。** 名前は**動詞を持ったまま**にする(手 1 は取り下げ)。
残すのは **手 2(幅の下限の段 2 つ)**と **手 3(塊の間。段 B で着地済み)**である。

⚠ **これは「見出しが悪い」ではない** ── 悪いのは**右の列が狭いまま 12〜14 個を抱えて
いること**である(見出し無しでも既に 4〜9 段ある)。🔑 効くのは
**数を減らすほう**(§3 の束ね = 段 E)で、その判断材料はこの表である。

### 4.1 右の列(ノートの操作)── 17 個・幅 17 種類 → 幅 2 種類

| 塊(見出し) | いまの名前 | 案 | 幅 |
|---|---|---|---|
| **写す** | 参照をコピー | **参照** | 短 |
| | 素の Markdown | **Markdown** | 長 |
| **開く** | 別のウィンドウで開く | **別ウィンドウ** | 長 |
| | このスタックを載せる | **スタック** | 長 |
| **書き出す** | バックアップ(このノート) | **バックアップ** | 長 |
| | 閲覧用 HTML | **HTML** | 短 |
| | Word / PowerPoint / PDF | **そのまま** | 短・長・短 |
| **取り込む** | 外部の画像を取り込む | **外部の画像** | 長 |
| | 書き戻す | **書き戻す** | 長 |
| **この 1 件** | 履歴 | **履歴** | 短 |
| | 名前を変える | **名前** | 短 |
| | 移す… | **移す…** | 短 |
| | この中に新しいノートを作る | **中に作る** | 長 |
| | 削除 | **削除** | 短 |
| **このフォルダ** | バックアップ(このフォルダ) | **バックアップ** | 長 |

**実測(2026-09-21、この案をそのまま数えた)**:

| | いま | 案 |
|---|---|---|
| 名前の幅(全角換算) | **3 〜 28 桁** | **3 〜 12 桁** |
| 幅の種類 | **17** | **6** |
| 12 桁を超えるもの | 9 件 | **0 件** |

🔑 **6 種類の幅は、段 2 つ(短 ≤ 8 桁 / 長 ≤ 12 桁)に収まる** ── 短い段に 12 件、長い段に 5 件。
下限を 2 段で与えれば、**画面に出る幅は 2 通り**になり、右端に縦の線が 2 本立つ。

⚠ 「参照」「名前」「構成」のように**短くしすぎると意味が落ちる**ものは、
**見出しと `title` が支えているか**を 1 件ずつ画面で確かめる(⑤-2 と同じ作法)。

### 4.2 編集の帯 ── 15 個・幅 9 種類 → 4 つの塊

| 塊 | 中身 | 直す名前 |
|---|---|---|
| **見出し** | 見出し1 / 見出し2 / 見出し3 | そのまま(既に 3 つとも同じ幅) |
| **飾る** | 太字 / 斜体 / コード | そのまま |
| **行の形** | 箇条書き / 番号 / チェック / 引用 | そのまま |
| **差し込む** | 表 / 図 / コードブロック / 数式 / リンク | **コードブロック → コード枠**(14 → 8 桁) |

🔑 編集の帯は**名前が既にほぼ揃っている** ── 効くのは**間で 4 つに切る**(手 3)ことである。

### 4.3 左の列・状態行

| 面 | いま | 案 |
|---|---|---|
| 作る帯 | + ノート / 今日 / 添付 / 録音 / 画面録画 / **時間を計る** | **時間を計る → 計測**(10 → 4 桁) |
| コレクション帯 | 取り込む / バックアップ / 操作を探す / 集計 / システム / フラグ / ヘルプ | そのまま(既に 4〜12 桁に収まっている) |
| 右の列(何も選んでいないとき) | 閲覧用 HTML / **持ち歩ける HTML 1 枚** / Markdown / 構成をコピー | **持ち歩ける HTML 1 枚 → 1 枚の HTML**(20 → 12 桁)/ **構成をコピー → 構成** |
| 状態行 | 開く / 元に戻す | 名前はそのまま。**高さ 18px → 26px**(§1.5) |

## 5. 壊れうる所と、そこに置く門(検査)

⚠ 直したことを**次の PR が崩さない**ように、手ごとに機械の門を置く。

| 手 | 門 |
|---|---|
| 1 名前 | **名前の幅の上限**(全角 6 字)を全数で pin。超えたら落ちる。⚠ 製品名は除外リストで名指し |
| 2 幅の段 | **CSS に現れるボタンの幅の段が 2 つだけ**であることを走査して pin |
| 3 間 | **1 つの帯に区切り線と間が混在しない**ことを pin |
| 4 絵 | **同じ並びのボタンは、全部に絵が在るか、全部無いか**を pin(いまの「一部だけ」を落とす) |
| ── | **押し所は 24px 以上**を全数で pin(いまの 18px 2 件を落とす) |

⚠ 門は「**代替物で満たせない条件**」で書く(CLAUDE.md §1)── たとえば手 4 を
「絵の登録が N 件以上」で書くと、**並びが混在していても緑**になる。見るのは**並びごとの揃い**である。

## 6. 段取り(各段が単独で着地する)

| 段 | 何をするか | 見え方 |
|---|---|---|
| **A** | §1.5 の壊れを直す(絵の付け忘れ 3 件 / 18px の 2 件)+ 押し所 24px の門 | 🔴 わずかに変わる(絵が 3 つ増える・下の 2 つが 8px 高くなる) |
| **B** | 手 3(間で塊を作る)+ 区切り線を間に揃える | 🔴 変わる |
| **C** | ~~手 1(名前)~~ + 手 2(幅の段)── 🔴 **手 1 は実測で取り下げ**(§4.0-a)。入ったのは**幅の下限の段**と、塊を正本へ寄せる整理だけ | ⚪ **変わらない**(名前も並びもそのまま) |
| **D** | 手 4(絵の混在をやめる)── 8 幅で測ってから | 🔴 変わる |
| **E** | 「書き出す」の束ね(§3)── C と D の後、まだ必要なら | 🔴 変わる |

⚠ 各段でお知らせとマニュアルを同じ PR で更新する。

## 7. これが分かったら覆る

- **「書き出す」系を毎日 2 つ以上押している** → §3 の束ね(段 E)は取り下げる
- 🔴 **右の列が広くなる**(`minmax(220px, 15vw)` を見直す / 2 列に組む)→ §4.0-a の実測は
  やり直しになる ── 見出しの推薦が戻る可能性がある(いま入れない理由は**狭さ**だけである)
- **名前を短くしたら意味が取れなくなった**(user の実機) → 手 1 の字数上限を 8 字へ緩め、段を 3 つにする
- **絵を全部に付けたら 8 幅のどれかで段が増えた** → 手 4 は「全部字だけ」へ倒す
