# 分析用に DuckDB を並べる(#682)── 設計

> user 要望(#682 本文、2026-09-03):「**ステップアップ可能なら検索分析用には
> duckdb-wasm を別途提供したいくらいです(これは私が duckdb が大好きだから、
> 素晴らしさの面を利用者に分かち合いたい欲望の体現です)**」

⚠ user 自身が「**欲望の体現**」と書いています ── つまり「要るから」ではなく
「**良いものを分かち合いたいから**」。🔑 だから判断の軸は「要るか」ではなく
「**入れて損が無いか**」です。

⚠ この doc は**裁定を仰ぐため**のものです(doc-first)。⏸ **残っている問いは 1 つだけ**で、
それは §9 に在ります。

---

## 0. 🔴 先に ── **前提が 2 つ変わりました**

この issue は 2026-09-03 に起票され、2026-09-12 のコメントで
「**#854 を先に → その後 DuckDB**」を推薦して止まっていました。**その両方が動きました。**

| | 何が変わったか |
|---|---|
| ① **#854 が着地した** | 添付の csv も xlsx も**手持ちのファイル**も、**いまの sqlite で読めます**(相手は **5 種類**) |
| ② 🔴 **測ったら 3 つ違った** | 下の §1。**推薦の向きが変わります** |

---

## 1. 測りました(2026-09-14、この箱・実ブラウザ)

⚠ 起票以来「**動くとは言えない**」と書き続けていた所です。**測ったので、もう書けます。**

### 🟢 起きます

| | 結果 |
|---|---|
| この箱で起こせるか | 🟢 **起きた**(`crossOriginIsolated` も `SharedArrayBuffer` も真。PKC3 の配り方と合う) |
| `SELECT 42` | 🟢 通る(33.6ms / 39.2ms) |
| 10 万行の集計(100 束に `group by`) | 🟢 通る(**77.1ms** / 103.0ms) |

### 🔴 ただし、想定と 3 つ違いました

| # | 実測 | なぜ効くか |
|---|---|---|
| ① | 🔴 **parquet は同梱されていない** ── 使う瞬間に **`https://extensions.duckdb.org/…` へ取りに行く**(npm の中に parquet を含む file は **0 件**) | PKC3 の柱(**外へ出ない**)と正面から当たる |
| ② | 🔴 **スレッド版のほうが遅く・重く・壊れやすい** | 下の表 |
| ③ | **csv / xlsx / 手持ちのファイル**は #854 で**もう読める** | DuckDB の「得意技」の大半が**既に在る** |

#### ② の中身 ── スレッド版は 3 つとも負けました

| | 素の版(eh) | 🔴 スレッド版(coi) |
|---|---|---|
| 起こすまで | **1.28 秒** | 2.64 秒 |
| 10 万行の集計 | **77.1ms** | 103.0ms |
| 常駐(問い合わせ後、プロセス木の Pss) | **+182.5MB** | **+501.8MB** |
| 取りに行けなかったとき | JS の例外(**捕まえられる**) | 🔴 **wasm ごと落ちて、ページが死ぬ** |
| 配る量 | 34.99 MiB | 35.48 MiB |

⚠ **この 3 つは「10 万行」での話**です ── もっと大きい所は測っていません(§10)。
🔑 それでも**壊れ方の非対称**(捕まえられる例外 / ページごと死ぬ)は行数に依りません。

---

## 2. 画面で何が起きるか

1. SQL の面に **「どのエンジンで引くか」** が 1 つ増える(既定は **いまの sqlite**)
2. **DuckDB** を選ぶと、初めての 1 回だけ「**取ってきて入れますか**」と出る(約 **35MB**)
3. 入れた後は、同じ欄に SQL を打って **DuckDB の文法**で引ける
4. しばらく使わなければ**そっと畳まれ**、記憶を返す(次に押せばまた起きる)

⚠ **いまの引き方は 1 ドットも変わりません** ── 選ばなければ、これまでどおりです。

---

## 3. 何のために足すのか ── 残った値打ちは 3 つ

⚠ #854 が着地したので、**起票時の「既定の使い道」は半分消えました**。残るのは:

| | 中身 | いま |
|---|---|---|
| ① **DuckDB の文法そのもの** | `QUALIFY` / `PIVOT` / `EXCLUDE` / `LIST` 型 / `SELECT * FROM 'x.csv'` など | 🔑 **user の動機そのもの**(「分かち合いたい」) |
| ② **大きい集計** | 列指向なので、幅の広い表の集計が速い | ⚠ **測っていない**(10 万行では**負けた**) |
| ③ **parquet** | repo 全体で **0 件**。いま読む手段が無い | 🔴 §4 の手当てが要る |

🔑 **①が主で、②③ が従です。** ⚠ ②を主に据えると、測っていない主張の上に設計が乗ります。

---

## 4. 🔴 parquet を「外へ取りに行かせない」── 自前で配る

⚠ duckdb-wasm は parquet を**実行時に外の CDN から**取りに行きます。
🔴 これは **PKC3 の柱と正面から当たる**ので、そのままでは入れられません。

🔑 **手当ては既に在る形で足ります** ── **Office 一式と同じ**(`office-pack-*`):

| | どうする |
|---|---|
| 置き場 | **IDB の Blob**(取ってきたものを端末に置く) |
| 取りに行く先 | 🔑 **同一オリジン**(PKC3 が自分で配る)── 外の CDN を見に行かせない |
| いつ取るか | **user が押したときだけ**(勝手に通信しない。2026-08-10 の裁定) |
| 取れなかったら | **理由を 1 行出す**(黙って失敗しない) |

⚠ **Office の仕組みをそのまま呼べません** ── `pkc3-office-pack` という **DB 名**も、
必須 file の一覧も、文言も **Office 名で固定**されています(5 file / 1118 行)。
🔑 **構造は写せる**(取得 → 設置 → パネルの分離)が、**名前の所だけ**を
引数にする小さな一般化が要ります。

---

## 5. 🔴 スレッド版を選びません(測って決めた)

⚠ 起票時のコメント(2026-09-09)は「**threads 版の前提は既に揃っている**」と書いており、
それ自体は正しい(`crossOriginIsolated` も `SharedArrayBuffer` も真)。
🔴 **ですが「使える」と「使うべき」は別でした** ── §1 の②のとおり、
10 万行では**遅く・重く・壊れやすい**ほうでした。

🔑 **素の版(eh)を既定にします。** これで得るものが 3 つ:

- **常駐が 1/3 以下**(+182.5MB 対 +501.8MB)── 不可侵指示「効くのは定常」に沿う
- **落ちない**(取りに行けなかったときに**ページごと死なない**)
- 🔑 **`crossOriginIsolated` に依存しない** ── 将来この前提を外しても、ここは壊れない

⚠ **スレッド版を永久に捨てるのではありません** ── ②(大きい集計)を**測った上で**
勝つなら、そのとき**選べるようにする**(⚠ ただし落ち方の非対称は残るので、
**既定にはしない**)。

---

## 6. 作る物 ── 🔴 **engine を選べる継ぎ目は 0 件**

実地調査(2026-09-14)で数えた結果:

| | いま |
|---|---|
| SQL の面 | `src/adapter/ui/render/sql.ts`(**408 行**)。押し所は `run-sql` / `sql-to-note` / `set-sql-source` / `set-sql-text` |
| 走らせる鎖 | 押す → `RUN_SQL` → 字の門(`sql-guard.ts`)→ `REQUEST_SQL_RUN` → `runReadOnlySql` → **storage worker** |
| 🔴 engine の抽象 | **在りません** ── 安全装置(`PRAGMA query_only` / 歩数の門 / 字の白名簿)が**全部 sqlite の API に直結** |
| worker | storage worker は **`worker-lease` の対象外**(DB の錠を握るので常駐が要る) |

🔑 **だから DuckDB は別のワーカーにします**(storage worker に相乗りさせない)。
そして**使い捨ての規律**(遅延起動 / 溜め / アイドルで解放)に乗せる ──
⚠ **+182.5MB を常駐させない**ためです(実測で、畳むと **114.1MB 返ってきます**。
⚠ ただし **68.4MB は残りました** ── §10)。

### 🔴 安全装置は「写す」のではなく「作り直す」

⚠ sqlite 側の 3 つ(`PRAGMA query_only` / 歩数の門 / 字の白名簿)は
**sqlite 固有の API**なので、そのままでは効きません。DuckDB 側で同じ 3 つを
**別に作る**必要があります ── ⚠ ここを飛ばすと「**読むだけ**」の約束が破れます。

### ⚠ 組み合わせの矛盾を作らない

「相手 × engine」の 2 軸にすると、**成り立たない組み合わせ**が出ます
(例:添付の `.sqlite` を DuckDB で開く)。
🔑 **選べる engine を、選んだ相手で絞る** ── 矛盾を**画面から消します**
(user に「押せるのに効かない」を見せない)。

---

## 7. 段の切り方

| 段 | 中身 | 量 |
|---|---|---|
| **段①** | **DuckDB を別ワーカーで起こし、`SELECT` が通る**(相手は「この PKC のノート」を写したものだけ)+ 使い捨ての規律 | M |
| **段②** | **engine を選ぶ欄**を SQL の面に足す(⚠ 相手で絞る)+ 安全装置 3 つ | M |
| **段③** | **取ってきて入れる**(同一オリジン / opt-in / IDB Blob)── Office の形を小さく一般化 | M |
| 段④ | **parquet**(③ の仕組みに extension を載せる) | S〜M |
| 段⑤ | 大きい集計を**測る** ── 勝つならスレッド版を「選べる」側に足す | S |

🔑 **段① だけでも user の動機(分かち合いたい)は半分満たされます** ── DuckDB の文法が打てます。

---

## 8. 門(test)

| 何を | どう見るか |
|---|---|
| 🔴 **外へ取りに行かない** | 走らせている間、**同一オリジン以外への要求が 0 件**(実ブラウザで観測)。⚠ これは**この設計の一番の約束**なので、必ず機械で見る |
| 🔴 **書けない** | `CREATE` / `INSERT` / `COPY … TO` が**断られる**(sqlite 側と同じ 3 方向) |
| 🔴 **畳まれる** | しばらく使わないと worker が**消える**(記憶が返る)。⚠ **飛んでいる問い合わせがある間は畳まない** |
| **溜める** | 起きる前に来た問い合わせが**落ちない** |
| 🔴 **矛盾する組み合わせが出ない** | 相手ごとに、選べる engine の集合を**等値で pin** |
| ⚠ 空振り防止 | 「DuckDB で引いた」ことを**数える**(sqlite が答えても同じ結果になる問いで、**engine を取り違えても緑**にならないように) |

---

## 9. 🟢 裁定 2026-09-15 ──「**DuckDB を選ぶ口**」は **A**(SQL の面の中)

user 裁定 2026-09-15:**A**。

⚠ ここは**画面の見え方**なので、こちらでは決めませんでした。以下は裁定前に並べた 3 案。

| | 画面で何が出るか |
|---|---|
| **A**(推薦) | SQL の面の「調べる相手」の**隣に、「どのエンジンで引くか」が 1 つ増える**。既定は いまの sqlite |
| **B** | **DuckDB 専用のタイル**(別のアプリ)にする ── アプリの一覧に 1 枚増える |
| **C** | 選ぶ口を**出さない** ── 大きい集計のときだけ、こちらで勝手に切り替える |

### 🟢 裁定は **A**(推薦どおり)

- **同じ場所に在る**(#300 の「主の作業領域を奪わない」と、業務画面の「同じものが同じ場所に」)
- 🔴 **C は「分かち合いたい」と正面から逆**です ── 隠すと、user は
  **DuckDB を使っていることに気づけません**(動機そのものが消える)
- **B は入れ物が 1 つ増える**(§7「3 系統目を作らない」の向きと合わない)。
  ⚠ ただし **DuckDB を主役として見せたい**なら B が勝ちます ── そこはお好みです

🔑 **これが分かったら覆ります**:実際に使って「**毎回エンジンを選ぶのが煩わしい**」と
なれば、**憶える**(前に選んだものを既定にする)側へ倒します。

---

## 10. ⚠ まだ測っていないこと

- 🔴 **大きい集計で本当に勝つか** ── 10 万行では**負けました**(77.1ms 対 103.0ms、
  相手は sqlite ではなく DuckDB の 2 つの版どうし)。
  ⚠ **sqlite と DuckDB を同じ問いで比べていません**
- **畳んでも 68.4MB 残った** ── 返るのは 114.1MB で、**全部は返りません**。
  ⚠ 何が残っているのか(wasm の頁 / worker の殻)は追っていません
- **parquet を自前で配れるか** ── extension の実バイト数も、同一オリジンから
  読ませる書き方も**確かめていません**
- **段① の「この PKC のノートを写す」** ── 写す量と時間を測っていません

---

## 11. 🔴 裁定 2026-09-15 ── **配り先は「PKC3 自身の Pages」**

⚠ **§9 の「A」とは別の問いです。** §9(エンジンを選ぶ欄を画面のどこに出すか)は
**まだ ⏸ のまま**です。ここで決まったのは **35MB の実体をどこから取ってくるか**だけ。

### ① user が何を求めていたのか

DuckDB も、sqlite や Office と同じように **オフラインで使えること**。
そのうえで PKC3 の柱(**勝手に外へ出ない**)を崩さないこと。

### ② そのとき何が問題だったのか

duckdb-wasm の素の姿は **実行時に外の CDN へ取りに行く**(parquet は
`https://extensions.duckdb.org/…`)── **オフラインで死ぬ**と**外へ出る**の
**両方**に当たる。

そして手本になる形が 2 つあり、**どちらから配るかで user に見える差が出た** ──
Office は**別 repo の Pages**(`sm06224/office-pack`)から配っているので、
そこへ相乗りすると **Office が使えない環境では DuckDB も道連れ**になる。

### ③ だから何を決めたか ── **PKC3 自身が配る**

| | どうする |
|---|---|
| 取りに行く先 | **PKC3 と同じオリジン**(`/dev/` を見ているなら `/dev/` の中) |
| 押す所 | **設定 → Office 表示 の隣に「DuckDB」**(Office と同じ見た目・同じ操作) |
| 置き場 | **IDB の Blob** |
| いつ取るか | **user が押したときだけ**(勝手に通信しない) |
| 取れなかったら | **理由を 1 行出す**(黙って失敗しない) |

🔑 **要点は「Office と配信経路を分けたこと」** ── 片方が落ちても、もう片方は生きる。

### 🔴 この裁定が連れてくる門 ── 先に書いておく

⚠ **35MB は `dist/` に置くが、precache には載せない。**
そのままだと `scripts/dist-inspect.mjs` が
**「precache に載っていない生成物がある」**で落ちる ── 2026-08-29 に
`portable-template.html` で**本番配布を実際に止めた**のと同じ形である。

🔑 **前例どおりにやる**:名指しで除外し、⚠ **外したぶんの門を置き直す**
── 除外した瞬間、その file は **0 バイトでも 100MB でも通る**ようになる
(tripwire を 1 つ撤廃したのと同じ)。別立ての予算を置き、
⚠ **その予算が渡っていなければ鳴らす**(optional にすると、呼び側が
渡し忘れた日に門ごと消える)。

### ⚠ 測って分かったこと(2026-09-15)

| | 値 | 引いた所 |
|---|---|---|
| npm の `@duckdb/duckdb-wasm` を展開した量 | 🔴 **142.5 MiB** | `npm view … dist.unpackedSize` = 149,377,663 |
| そのうち**配る**のは | **約 35 MiB**(素の版 `eh` だけ) | §1 の実測 |
| いま precache に載っている量(対照) | **137 file / 8.2 MiB** | `dist/precache.json` を実測 |

🔑 **だから同梱(precache)は採らない** ── 8.2 MiB が 43 MiB になり、
**DuckDB を選ばない user が 5.2 倍を払う**。
⚠ 「配る量は気にしない」(不可侵指示 2026-08-03)は生きているが、それは
**全 user が使う物**についての話であって、**選んだ人だけが使う物**を
全員へ配ってよい理由にはならない。

### ⚠ 段の切り方への影響(§7)

🔴 **段① から「precache に載せない形」で作る。** 普通に束ねると 35MB が
precache に入り、**後で外すときに上の門を踏む**。
🔑 段③(取ってきて入れる)は、その上に **IDB と opt-in の小窓**を足すだけになる。

---

### 🟢 裁定のあと測った ── **案 A は成り立ちます**(2026-09-15)

⚠ §10 に「**Vite で束ねられるか ── 通る見込みだが通したことはない**」と書いていた分を、
実際に通して潰しました(probe は repo の外に出し、終わったら消してあります)。

| 測ったこと | 結果 |
|---|---|
| **Vite で束ねられるか** | 🟢 **通る** ── JS は **196.8 KiB** の 1 チャンク(`apache-arrow` / `tslib` / `flatbuffers` は解決された) |
| **wasm は JS に混ざるか** | 🟢 **混ざらない** ── 実行時に URL で取りに行く形なので、**35MB は別物として扱える** |
| **自前の URL を渡せるか** | 🟢 **渡せる** ── `instantiate(mainModuleURL, pthreadWorkerURL?, progress?)` |
| 配る実体の内訳 | `duckdb-eh.wasm` **34.3 MiB** + `duckdb-browser-eh.worker.js` **0.7 MiB** ≒ **35 MiB**(§1 の 34.99 MiB と一致) |

🔑 **だから実行時に外へ 1 バイトも出しません** ── IDB の Blob から `URL.createObjectURL`
を作って `instantiate` に渡せば、取りに行く先は**自分の中**になります。
🔑 `progress` の口が在るので、Office と同じく**取得中の進み具合**も出せます。

### 🔴 ただし、焼き込まれた宛先が 1 つ在りました

| 探した語 | bundle 内の件数 |
|---|---|
| `extensions.duckdb.org`(parquet の取得先) | 🟢 **0 件** |
| 🔴 `https://cdn.jsdelivr.net/npm/` | **1 件** |

⚠ これは `getJsDelivrBundles()`(既定の bundle を CDN から引く口)の宛先です。
**呼ばなければ 1 バイトも出ません**が、🔴 **呼べる関数が配る物の中に在る**以上、
「呼ばないと決めた」だけでは守れません ── 次に書く人が素直に使います。

🔑 **門にする**:`getJsDelivrBundles` / `getJsDelivrBundles` 系を**呼ぶ経路が 0 件**であることを
`src` の全数走査で pin する。⚠ 走査は**注釈を落としてから**当てる ── この doc の綴りや
解説コメントに満たされて、**在るのに 0 件と読む / 無いのに落ちる**のどちらにもなる
(CLAUDE.md §1 の 5 度目・10 度目と同型)。

---

## 12. 🔴 裁定 2026-09-15 ── **常駐させない。使うときだけ載せる**

### ① user が何を求めていたのか

DuckDB が**いつも居座る**ことを禁じた。使う瞬間だけ載り、使い終わったら**返す**こと。

### ② そのとき何が問題だったのか

⚠ **こちらの書き方が誤解を招いた** ── 「常駐が実測 +182.5MB なので置きっぱなしにしない」と
書いたので、**いま既に常駐しているように読めた**。
🔑 事実は「**`src/` に DuckDB を起こすコードは 1 行も無い**」(2026-09-15 実測。一致するのは
`DUCKDB_PRECACHE_SKIP` = precache に載せない印だけ)。+182.5MB は**起こしたときの実測値**である。

### ③ だから何をする ── 「使うときだけ」を **2 か所**に効かせる

| | いつ載るか | 使い終わったら |
|---|---|---|
| **35MB の実体** | DuckDB を初めて選んで押したとき | IDB に残す(次は取りに行かない)。⚠ 選ばない user は **0 バイト** |
| **起こしたメモリ** | 問い合わせを打ったとき | 🔴 **アイドルで kill して返す**(`worker-lease.ts`) |

🔑 機構は既に在る ── `WorkerLease` は **遅延起動**(`spawn` は呼ばれるまで走らない)/
**起動待ちのジョブを溜める** / **`idleMs` で kill** を持ち、いま **6 つのワーカー**が乗っている。
⚠ storage worker(sqlite)だけが対象外 ── DB の錠を握るので常駐が要る。
🔴 **DuckDB は storage worker に相乗りさせない**(相乗りさせると、この規律の外に出る)。

### 🟢 測った(2026-09-15)── **積み上がらない。残るのは 1 回目の代金だけ**

⚠ **「68.4MB が返らない」は、1 巡しか測っていない数字だった** ── 2 巡目以降を
測ったら、**載せた分がほぼ全部返って**いた。

計器は `tests/probe/run-duckdb-memory-probe.mjs`(**profile を握るプロセスの Pss**)。
仕事は 10 万行の集計(`range(100000)` を 100 群に `group by`)── 元の +182.5MB と
**同じ仕事**に揃えてある。

| 巡 | 起こした差 | 畳んで返った | 返らなかった |
|---|---|---|---|
| **1** | +172.0MB | 106.4MB | 🔴 **65.6MB** |
| **2** | +109.0MB | 105.7MB | 🟢 **3.3MB** |
| **3** | +106.2MB | 107.8MB | 🟢 **−1.6MB** |
| **4** | +103.9MB | 106.3MB | 🟢 **−2.4MB** |

🔑 **1 回目だけ約 66MB が残り、2 回目以降はほぼゼロ**(−2.4 〜 +3.3MB)。
⚠ 独立した 2 回の 4 巡走行で一致した(前回は 66.5 / 2.1 / 2.8)。
🔑 残る 66MB は**1 タブに 1 回きりの代金**(焼いた wasm とコード資産)であって、
**使うたびに積み上がる物ではない**。

#### 🔑 対照群が、この読み方を支えている

| 対照群(同じ走行の中で) | 値 |
|---|---|
| JS で 200MB を確保して**触る** | **+203.3MB** |
| 捨てて、返り切るまで待つ | **203.2MB 返る** |

⚠ **待たないと 3.9MB しか返らない**(ブラウザは JS の解放をすぐ OS へ返さない)。
🔑 だから「DuckDB が 106MB 返した」は**待ちの効果ではなく、畳み方の効果**である
── ワーカーごと終了させているので、OS へ戻っている。

#### ⚠ 計器の側で 3 回間違えた(全部直した)

| | 何が起きたか |
|---|---|
| ① | **木で辿る計器が、描画プロセスを 1 つも見ていなかった** ── 200MB 確保して **−2.2MB**。Chromium の描画プロセスは **zygote 経由で親が付け替わる**。`--user-data-dir` で選ぶ形へ直した |
| ② | `select 42` は**データを 1 行も読まない** ── +182.5MB(10 万行)と**比べる相手になっていなかった** |
| ③ | 🔴 **対照群の 200MB が、自分の測定へ漏れた** ── 3 巡目に `−202.8MB`。重しが返り切るまで待つ形へ直した |

🔑 **3 つとも「対照群を置いたから」見つかった。** ①だけでも、置いていなければ
**「DuckDB は常駐しない」という嘘を、測った顔で配っていた**。

#### ⚠ まだ測っていないこと

- **10 万行より大きい所**(1 回目の 66MB が増えるかどうか)
- **実機**(この箱の headless Chromium での値である)
- **アプリの UI 経路**(probe 用の束から呼んでいる)

---

## 出所

- user 要望 [#682](https://github.com/sm06224/PKC3/issues/682) 本文(2026-09-03)。台帳は #674
- 実地調査 2026-09-14(read-only)── SQL の面 **408 行** / 相手 **5 種類** /
  `runReadOnlySql` は **4 file** / engine の抽象 **0 件** / `parquet` **0 件**
- 実測 2026-09-14(この箱・実ブラウザ・`/tmp` の中で完結)── §1 の数字はすべてここから。
  対照群(素のタブ)は 268.0〜269.2MB で 3 回とも一致
- [#682 の 2026-09-09 コメント](https://github.com/sm06224/PKC3/issues/682#issuecomment-5594205776)
  ── 「threads の前提は揃っている」は正しい。⚠ ただし §5 のとおり**使うべきとは別**だった
- [#682 の 2026-09-12 コメント](https://github.com/sm06224/PKC3/issues/682#issuecomment-5646739073)
  ── 「#854 を先に」の推薦。🟢 **その #854 は着地した**
- 不可侵指示 2026-08-03(配る量は気にしない / 効くのは定常 / 重い処理はワーカーへ・使い捨てに)
