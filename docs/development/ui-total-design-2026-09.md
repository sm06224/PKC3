# UI のトータルデザイン v2 ── メッセージ・設定の秩序・名前・「操作を探す」(2026-09-20)

> 出どころ:user 指示 2026-09-20(#1017)。同日の 1 稿目は §5 の 4 問が**4 問とも差し戻された**
> (「ログに固執するな」「場当たりの改名ではなく全体に秩序を」「説明が分からない」「それは必要なものなのか」)。
> この稿は user の助言「**PKC2 に固執せず、PKC の思想と哲学に軸足を載せる**」に従い、出発点を置き直した。
> ⚠ **実装は 1 行も入れていない**(doc-first)。裁定が出た所から実装する。

## 0. 出発点 ── PKC の思想(user の言葉が在る所から引く)

PKC の思想は 1 か所に書かれていない。以下は user の言葉として記録が在るものだけを集めた。
**この doc の案は、すべてこの表のどれかから導く**(「PKC2 にそう在った」は根拠にしない)。

| # | 思想 | 出どころ |
|---|---|---|
| ① | **速く、安く、必要十分、利便性最大** | founding doc §0 指示 8(2026-07-30) |
| ② | **単一 HTML で自立する可搬式。container が正本。全 body = PKC-Markdown** | founding doc §0〜§3 |
| ③ | **業務画面** ── 同じ物が常に同じ場所 / 1 画面で完結 / **マウスで完結し、キーボードは近道** / 地は無彩色、色は情報にだけ | user 指示 2026-08-03 |
| ④ | **補助的な物が主の作業領域を奪わない。アプリの基本は別窓、左のタブは残す。main shell に modal を置かない** | #300(2026-08-22)/ 裁定 2026-09-04 / PKC2 2026-05-05 |
| ⑤ | **記法 = user の書き方 = 動線。減らさない。片道の操作を作らない(双方向が既定)** | 裁定 2026-08-07 / 指示 2026-08-23 |
| ⑥ | **PKC は KB + messaging の node。中身を端末の外へ出さない** | PKC2 `docs/vision/pkc-message-externalization.md` / `debug-privacy-philosophy.md` |
| ⑦ | **編集の大半は追記と差し挟み ── 開かずに済ませる道を持つ** | PKC2 実装に user 指示由来で明記(CLAUDE.md「PKC2 の万能の中身」3) |

## 1. 過去の決定の誤り(認める)

| 決定 | いつ | 何を根拠にしたか | 何が誤りだったか | この doc での扱い |
|---|---|---|---|---|
| 「操作を探す」(コマンドパレット)を足した | #425、2026-08-26 | 「PKC2 は 60 個を名前で呼べた」 | **user の要望は 0 件**(git log / docs / 方針正本に無し)。思想③「マウスで完結」の逆向きに置いた。しかも #582 の裁定待ち 2 問は、どちらも**この後始末を user に聞く**形だった | §4:要否を事実で答え、2 問は取り下げる |
| 押す頻度で「左の列」と「設定」へ分けた | #239 | PKC2 の配置 + 頻度 | 思想③「同じ物が同じ場所」とは別の軸。#582 の研究自身が「Office 2000 の適応型メニューと同じ失敗の弱い版」と認めている | §3:軸を「何に効くか」に替える |
| 壊れ対応を、症状 1 件ごとにボタン 1 個で足した | #986 段①②③ / #1004 / #1006 / #1011 / #1015 | その日の症状 | 積み上がった結果:見出し「壊れて直らないときの、最後の手」の下に**不可逆操作が常設** / 動詞句のボタン 3 件 / **同じ `.pkc3.zip` に 4 つの名前 + 名乗らない 1 経路** / 画面の字に「壊れ」が **73 か所** / いちばん重い復旧の文が**消えるステータス行**経由 | §2 §3:置き場と名前を全体で決め直す |
| 「表示」節に、見た目でない設定を平積みした | 逐次 | 置き場が無かった | 17 項目のうち **9 項目**が動作・許可・通知の設定。`settings.ts:187` のコメント「見た目の好みだけ」と実装が食い違う | §3 |
| 用語集を「造語の説明表」として置いた | 2026-09-08 | 規約 11 | 造語(面・札・器・居場所・解放…)を**説明して残す**表になっていた。要るのは**使わない語の表** | §5 |
| 知らせをステータスバーへ出した | #1015 ほか | 既存の 1 行 | 7 系統が 1 行に合流し、読み直せず取り出せない | §2 |

## 2. メッセージ ── 集積地を作る(「ログ」ではない)

### 2.1 いま(実測 2026-09-20)

| 出口 | 何か | 消え方 | 読み直し | 取り出し |
|---|---|---|---|---|
| `showStatus()` | 「コピーしました」等(main.ts 20 + binder.ts 23 か所) | 次の知らせで上書き | ✗ | ✗ |
| `OP_FAILED` → `state.error` | 操作が断られた理由(241 か所) | 無関係な 8 操作で `error: null` | ✗ | ✗ |
| `OP_NOTICE` → `state.notice` | 15 か所 | 同上 | ✗ | ✗ |
| `alertInApp` | 3 か所 | 閉じると消える | ✗ | ✗ |
| お知らせ(`notice-log.ts`) | 開発側からの配信 | 既読は残る | ○(ヘルプ) | ✗ |
| `console.warn` | 29 か所 | DevTools | DevTools のみ | 手で |
| ワーカーのジョブ記録(`JobMonitor`) | 設定 →「処理(ワーカー)」 | メモリ 200 件、リロードで消える | ○ | ✗ |

🔴 `main.ts:1297` が **5 種類(状態 / 同期 / 保存 / 知らせ / エラー)を 1 本の灰色の行に文字列連結**している。
`app-state.ts:2230` のコメントは「OP_FAILED は赤い帯」と書いているが、そのような UI は**実装に無い**。
🔴 中身が混ざる経路:添付ファイル名(`attach.ts:498`)/ 見出し(`binder.ts:2813`)/ 題名(`binder.ts:3994`)/
生の例外文字列(`${e.message}` 30 か所)。

### 2.2 設計 ── **メッセージはノートである**(推薦)

PKC の思想②⑤⑥⑦から導く:**container が正本 / 全 body は PKC-Markdown / PKC は messaging の node /
追記で済ませる。** だからメッセージの集積地は**新しい面ではなく、1 件のノート**にする。

| | 中身 |
|---|---|
| 実体 | **`textlog` 型のノート「メッセージ」1 件**。アプリが最初の起動で作る(消せない・改名できない。中身は user が編集できない) |
| 1 件のメッセージ | textlog の節 = `## 2026-09-20 14:05:03` の見出し + 本文 1〜2 行。本文は**固定の日本語文**で、種類 / 出所 / 次にどこへ行くか(リンク)を持つ |
| 種類 | **結果**(操作が済んだ)/ **注意**(user がすることがある)/ **問題**(操作が通らなかった)/ **配信**(お知らせ)/ **処理**(ワーカー。いまの「処理(ワーカー)」をここへ統合) |
| 上限 | **最大 500 件**(古い節から落とす)。設定「通知」で 100 / 500 / 2,000 から選べる(user が選べる形を先に出す ── 2026-08-28) |
| 読み直し | 普通のノートと同じ ── 左の一覧に在る / 検索できる / 別窓で開ける / 印刷できる。**新しい面も新しい道具も作らない** |
| 取り出し | 普通のノートと同じ ── 情報ペインの「書き出す」/ 本文をコピー。**バグ報告にはこのノートを貼る**。起動のたびに 1 節「版 / 保存先の種類 / フラグ / ブラウザ / 画面の幅」を書くので、報告に要る環境が同じノートに揃う |
| 中身を漏らさない | **書く口を 1 つ**(`postMessage(kind, source, text, detail)`)にし、`text` は固定文の表からしか引けない型にする。`detail` は件数 / 文字数 / lid / 分類名だけ。ノートの本文・題名・添付名・入力欄の字・検索語は**型で入らない**。生の例外文字列は分類名へ落とし、原文は `detail.raw` に **80 字まで**(切り分けに要る) |
| sqlite に書けないとき | 保存が止まっている / 空きが無いときは **IndexedDB の控え(`pkc3-messages-spool`)**へ積み、書けるようになった時点で節として流し込む。起動時に DB が読めなければ、控えをそのまま画面に出す |
| 未読 | ノートを開いたら 0。**`state.error` を 8 か所で消す規則は廃止** ── 「ノートを選ぶと知らせが消える」は構造から消える |
| ステータスバー | **いま起きていること**の 1 行に戻す:保存中 / 本体タブ経由 / **未読 N 件**。要対応の最新 1 件だけ短く出し、**既読になるまで消えない**。押すと「メッセージ」ノートが開く(普通のノートと同じ開き方 ── 別窓で開きたければ Ctrl+クリック) |

⚠ **代案 B(1 稿目の「ログ」)**:別の入れ物 + 専用の一覧の面 +「ログを保存」ボタン。
やらない理由:面と道具(一覧・絞り込み・書き出し・別窓)を**一式作り直す**ことになり、思想①②に反する。
ノートにすれば**既に在る道具が全部そのまま効く**。

⚠ 代案 A の弱点(先に書く):左の一覧に「メッセージ」という 1 件が常に居る(**同じ物が同じ場所** ── 思想③には沿う)。
検索に「メッセージ」ノートの字が混ざる(→ 種類で除外できる。既に種類の絞り込みが在る)。

### 2.3 文言の規則 ── 不安を与えない(user 指示 2026-09-20)

実測:画面に出る字に「壊れ」**73** / 「失敗」45 / 「消え」41 / 「拾え」4 / 「失われ」3 / 「エラー」2 / 「破損」1。

| 規則 | 例 |
|---|---|
| **事実を述べる。評価語を使わない** | 「保存されている中身の一部が壊れています」→「**保存データに問題が見つかりました**」 |
| **不可逆は必ず言う**(これは事実であって脅しではない) | 「元に戻せません」は残す |
| **次にすることを 1 つ、場所はリンクで** | 「設定 → 保存領域 → **作り直す** へ」(押すと飛ぶ) |
| 使わない語 | 壊れ / 破損 / 最後の手 / 拾う / 捨てる / 危険 / 救出 / 復旧 |
| 内部の英語を出さない | `entry row missing` 4 か所 → 「ノートの行が見つかりませんでした」。sqlite の生文言は文ではなく `detail.raw` へ |

### 2.4 段取り

| 段 | 何をするか | 見え方 |
|---|---|---|
| ① | 書く口 1 つ + 7 系統からの接続 + 型と test で中身を止める門 + 控え(spool) | 左の一覧に「メッセージ」ノートが 1 件増える |
| ② | ステータスバーを「状態 + 未読 N」へ。`error: null` の 8 か所を廃止 | 🔴 変わる |
| ③ | 「処理(ワーカー)」の一覧を種類「処理」へ統合(設定の節を 1 つ減らす) | 🔴 変わる |
| ④ | 文言の規則で 73 + 4 + 5 か所を書き換え(§2.3)。門:画面の字に「使わない語」が出たら落ちる(既知は等値 pin で burn-down) | 変わる(壊れたときだけ) |

## 3. 設定画面の秩序 ── 軸を「何に効くか」にする

### 3.1 いま(全数目録 2026-09-20)

見出しは h3 が 10、h4 が 6。「表示」(17 項目)に h4 は 0、「書き出しと片づけ」に h4 が 5。
「壊れて直らないときの、最後の手」の下に**不可逆操作 2 つが常設**。
`.pkc3.zip` を書く関数は `writeArchive` 1 つなのに、押し口は**バックアップ / 書き出す / フォルダを書き出す /
拾って、戻せる形で書き出す** の 4 名 + 名乗らない 1 経路(中身を残して、作り直す)。

### 3.2 案 ── 6 つの節と、いまの項目の行き先

軸は**何に効くか**(思想③「同じ物が同じ場所」。#582 研究の規則 1「置き場は頻度ではなく scope で」)。

| 節 | 中身(← いまの場所) |
|---|---|
| **表示** | 配色 / 文字の大きさ / **ページ設定**(← 紙面)/ 本文の置き場所 / 段組み / 段の境界線 / タグの見せ方 |
| **編集** | 編集の仕方 / 開いたときの状態 / 貼り付け(← 独立節)/ 本文の電話番号 / **ショートカットキー**(← 独立節) |
| **通知** | 予定の知らせ / お知らせ / 狭い画面の断り書き / **メッセージの保管件数**(新、§2) |
| **連携**(外と繋がる所) | 外部の画像(← 独立節)/ zip を開く場所 / アプリの開き方 / ノートを渡して開くことを許したアプリ / 目次を見せているアプリ / Office(← 独立節) |
| **データ** | **使用量**(← 何が容量を使っているか)/ **書き出し**:閲覧用 HTML / 持ち歩ける 1 枚 / Markdown / 構成をコピー / 整理案を適用 / 設定の持ち出し / **片づけ**:使っていない添付を消す / 最近開いた記録を消す(← 独立節) |
| **保存領域** | **点検**(自動の点検の結果を常に 1 行で出す + 「いま点検する」)/ **復元用の控え**(§6。1 行)/ **作り直す** / **初期化する** ── ⚠ 後の 2 つは「詳しい操作」として畳んだ中に置き、**点検で問題が出たときだけ開いた状態で出す** |

**このアプリのデータ**(説明文のみ)は「保存領域」の頭へ。「処理(ワーカー)」は §2 ③で「メッセージ」へ統合し、節を消す。
**左の列のいちばん下の帯**:取り込む / バックアップ / **メッセージ(未読 N)**(新)/ 集計 / 設定 / フラグ / ヘルプ。
「操作を探す」は §4。

### 3.3 修復まわりの名前 ── ボタンを減らして、名前を 1 つにする

| いま | 案 | 根拠 |
|---|---|---|
| バックアップ(左の列) | **バックアップ**(そのまま) | `.pkc3.zip` の名前はこれ 1 つ |
| 書き出す(情報ペイン、1 件) | **バックアップ(このノート)** | 同じ形式に同じ名前。⚠ 説明文だけ「バックアップ形式」と言って label が違う不一致を直す |
| フォルダを書き出す(情報ペイン) | **バックアップ(このフォルダ)** | 同上 |
| 拾って、戻せる形で書き出す | **無くす** ── バックアップが、保存データに問題があるときは**読める分を集める経路を自動で使い**、結果を「メッセージ」に書く(N 件のうち M 件) | 同じ物に 2 つ目の名前を作らない。押し分けを user にさせない |
| 拾って、読める形で書き出す | **無くす** ── 設定の **Markdown** が同じ経路を使う(問題があるときも読める分を出す) | 同上 |
| 壊れていないか調べる | **点検する**(節「保存領域」の下なので対象は見出しが持つ) | 「チェック」は中身が無い(user 指摘)。「保存領域を点検する」と読める |
| 中身を残して、作り直す | **作り直す** | 動詞句をやめる。何が残るかは説明文と確認の窓に書く(既に在る) |
| 中身を捨てる | **初期化する** | 「捨てる」は評価語。何が消えるかは説明文と合言葉の窓に書く(既に在る) |
| 見出し「壊れて直らないときの、最後の手」 | **廃止**(節「保存領域」の畳みの中) | 不安を与える見出しを常設しない |

点検の結果文(§2.3 の規則で):「問題は見つかりませんでした」/「索引に問題が見つかりました(N 件)。中身は無事の見込みです → 作り直すと直ります」/
「本文の保存に問題が見つかりました(N 件)。読める分を戻せます → バックアップを取ってから、作り直す」。
書込を止めるときの文(`CORRUPT_REFUSAL`)は「保存を止めました。読むことはできます。設定 → 保存領域 → 作り直す へ」の 1 行にし、
手順の本文は「メッセージ」の節とマニュアルが持つ。

⚠ **名前の衝突の検算**:「バックアップ」が label として出る所は `commands.ts:60` の 1 か所だけ(実測)。
統合しても**同じ物**を指すので衝突しない。⚠ 1 稿目は「修復の書き出しにもバックアップと名付ける」形で、それは別の物に同じ名前を付ける誤りだった。

## 4. 「操作を探す」── 必要な物か

事実(2026-09-20 実測):

| 問い | 事実 |
|---|---|
| user が求めたか | **求めていない**。#425(2026-08-26)はこちらの起票で、根拠は「PKC2 は 60 個を名前で呼べた」。方針正本 doc に「パレット」は 0 件 |
| 無いと届かない操作 | **0 件**(受け手 267 のうち、ボタンもショートカットも無い操作は無い) |
| ボタンが無くショートカットだけの操作 | **5 件**(ルビ / ハイライト / 圏点 / 打ち消し / コピーした物の履歴)── **Ctrl や Alt の無い端末(スマホ)では、いまはパレットが唯一の入口** |
| 入口 | 左下のボタン 1 + `Ctrl+Shift+P`。左の列を畳むとボタンは消える |

**答え:必要な物ではない。** こちらが足した「保証の経路」であり、思想③「マウスで完結し、キーボードは近道」からは導かれない。

推薦:
1. **左下の「操作を探す」ボタンを外す**(#501「増えすぎたボタン」に沿う。左下の帯が 1 つ軽くなる)
2. `Ctrl+Shift+P` は**残す**(場所を取らない。消すと動線が 1 つ減る ── 思想⑤)
3. ボタンの無い書式 5 件は**書式パネルに「その他」の押し口**を与える(スマホで届く道をパレットに頼らない)
4. **#582 の裁定待ち 2 問(「畳んだとき操作を探すをどこに出すか」「151 件を出すか」)は取り下げる**

⚠ #582 の本題(各ペインのボタンの分類 / 畳んだときの復帰 / 一貫性)は `operation-model-2026-08.md` の研究が持つ。
この doc は設定画面・メッセージ・名前の 3 つに絞り、#582 を閉じない。

## 5. 名前の規則と用語集

### 5.1 規則(6 条)

1. **技術の標準語・形式名は英語かカタカナのまま**:Markdown / HTML / PDF / Word / PowerPoint / zip / CSV / SQL / JSON / URL / バックアップ / メッセージ / ステータスバー / パネル / タブ / メニュー / ボタン / ダイアログ / ショートカット / テンプレート / フォルダ / タグ / リンク
2. **対象は見出しが持ち、ボタンは動作だけ**:節「保存領域」の下なら「点検する / 作り直す / 初期化する」。⚠ 見出しから切り離して読める場所(左の列)では対象を付ける(「バックアップ(このノート)」)
3. **動詞句(「〜して、〜する」)をボタン名にしない**。説明は下の 1 行と確認の窓へ
4. **造語を使わない**:面 → パネル / 画面、口・器・印・札・検め → 使わない、区画 → 読み込めなかった箇所、小窓 → 別ウィンドウ、雛形 → テンプレート、紙面 → ページ設定、居場所 → フォルダ、解放 → 編集を終える
5. **同じ物は 1 つの名前**(`.pkc3.zip` = バックアップ)。**名前を付ける前に、その字が画面の他の場所に在るか grep する**
6. **評価語・脅し語を使わない**(§2.3)。内部の英語文字列を画面に出さない

### 5.2 用語集の持ち方

- 正本は **`src/features/ui-terms.ts`**(標準語 / 使わない語 / 例 の 1 表)。マニュアルの「言葉の意味」はここから生成する ── **説明する表ではなく、使わない語の表**にする
- 門(test):①画面の字(`label` / `textContent` / `OP_FAILED` の文 / メッセージの固定文)に**使わない語**が出たら落ちる(既知の残りは等値 pin で burn-down)②ボタンの `label` に「て、」を含む動詞句を禁止 ③`state.error` / メッセージの文に英語の識別子を禁止
- `manual-style` 規約 11 の「⛔ 用語集がまだ無い」を、この表で解く

## 6. #1007 段② ── 何が起きるか(user の身に起きる場面から)

**ある日**、PKC3 を開くと「保存データに問題が見つかりました。保存を止めました」と出る。
ノートは読める。でも、そこから先は保存できない。

**いま戻せるもの**:①前に取ったバックアップ(取った日まで)②「作り直す」で読めた分。
⚠ **バックアップを取った日から今日までに書いた分は、読めなければ戻らない。**

**段②を入れると**:ノートを保存するたびに、その本文の**写しを別の引き出しにも入れておく**
(引き出しは sqlite とは別の場所なので、sqlite に問題が出ても無事 ── 添付が無事だったのと同じ理由)。
問題が出た日には、**その引き出しから、直近に書いた分がそのまま戻る**。

**user がすること**:何も無い。**画面に増える物**:設定 → 保存領域 に 1 行「**復元用の控え:N 件**」。ボタンは増えない。
「作り直す」を押したとき、引き出しの分も一緒に戻る。

**引き出しの大きさ**(末尾の注記。判断材料ではない):新しい順に 500 件 / 合計 8 MB。1 件が 1 MB を超える本文は入れない。
古い順に落ちる。全ノートを定期的に走査はしない(重くなる)。消したノートを勝手に戻さない。

## 7. 裁定をお願いする点

| # | 何を決めていただくか | 推薦 |
|---|---|---|
| 1 | **メッセージの置き場**:左の一覧に「メッセージ」というノートが 1 件ある形(§2.2 A)/ 専用の一覧の面を作る形(B) | **A** |
| 2 | **設定画面の 6 つの節への並べ直し**(§3.2)と、**修復ボタンの統合と改名**(§3.3) | 案のとおり |
| 3 | **左下の「操作を探す」ボタンを外す**(§4。`Ctrl+Shift+P` と、書式 5 件の押し口は残す) | 外す |
| 4 | **段②(復元用の控え)を入れる**(§6) | 入れる |

## 8. これが分かったら覆る

- **メッセージをノートにする**:sqlite が書けない間に来るメッセージが多く、控えから流し込む順が狂うなら、B(別の入れ物)へ倒す
- **バックアップの統合**:問題があるときのバックアップに「関係・履歴が入らない」ことが user に見えない(メッセージの 1 行で足りない)なら、押し分けを戻す
- **「操作を探す」のボタン**:user が実際に押している記録(メッセージの「結果」に残る)が多ければ、外さない
- **用語**:user が「面」を読めていて変えたくないなら、規則 4 は「新しく書く所だけ」に狭める

## 9. 出典

- 全数調査 3 本(2026-09-20、サブエージェント):設定画面と左の列の目録 / 「操作を探す」の出どころ(`git log -S`)/ 不安語の全数(TypeScript の AST で文字列リテラルだけ数えた)と知らせの出口
- #425 / #582(`operation-model-2026-08.md`)/ #986 / #1004 / #1006 / #1007 / #1011 / #1015 / #501 / `manual-style-2026-09.md` 規約 11
- PKC2(read-only)`docs/vision/pkc-message-externalization.md` / `docs/development/debug-privacy-philosophy.md`
