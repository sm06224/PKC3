# セッション引き継ぎ 2026-09-02

> ⚠ **この doc は「残件の一覧」を持ちません**(user 指示 2026-08-15「ファイルをただ置いて
> おくのはやめろ」)。やることは **issue** に在ります ── ここに書くのは
> **状態・順番・理由**と、**issue に書けない性質のもの**だけです。

## 🔴 最初の仕事: **なし。user の裁定待ちが 6 群(新規は #632 の 6 問と #633 の 4 問) 件**

⚠ 催促はしません。裁定が来るまで着手しないもの(§4)以外に、次のセッションが
**自分の裁定で**進めてよいものが §3 に在ります。

## 1. いまの状態(実測)

| | |
|---|---|
| main HEAD | `04bd062` 右クリックのメニューの下に、指している項目の説明を 2 行で出す(#587 C-3)(#651) |
| **本番(`/`)** | 🟢 **v3.2.0**(2026-08-29 deploy。今日は本番を動かしていない) |
| `/dev/` | main HEAD(今日 4 本の PR が入った ── §2) |
| open PR | **本 PR(引き継ぎ)のみ**(#649 / #650 / #651 は merge 済み) |
| 作業 branch | `claude/pkc3-office-pack-handoff-59opdo`(引き継ぎ PR の commit のみ。未 push なし) |
| local branch | `q5-local` / `q6-local` / `rtf-local` / `handoff-local` ── **全部 main か本 PR に載せ済み**(消してよい) |

## 2. 今日 main に入ったもの(このセッション分)

| PR | |
|---|---|
| #647 | マニュアルを build 時に `manual.html` へ焼き、窓を実 URL にする(#645 段②)── 前セッションから継続 |
| #649 | 🔴 **横に留めた並びが開き直すと外れる穴**(#505 段②「憶える」が一度も成立していなかった)+ マニュアルの窓の当て直し 4 件(#648 A1/A2/A3/A5) |
| #650 | 「ここに追記する」/ Alt+クリックで、畳んだ追記欄を開いて打つ欄にカーソル(#596 A / 設問③ C ── user 裁定 A5) |
| #651 | 右クリックのメニューの下に、指している項目の説明を 2 行で出す(#587 C-3 ── user 裁定 A6「一度推奨で入れて、使用感をテストしたい」) |
| 本 PR | 本 PR ── 設計 doc 2 本(#632 / #633)+ 調査 doc + `.claude` 反映 + 4MB RTF test の時間予算 + 本 doc |

⚠ user 指示 2026-09-02(逐語):「**待たずに継続して推奨で実装を許可します / お知らせに
変更を入れるのを忘れずに！ / 動きがおかしければ指摘します / あなたの力を信じてる**」──
#649 / #650 / #651 はこの指示の下で、CI やレビューの完了を待たずに進めた。
**お知らせは 3 件とも入れてある**(`2026-09-02-manual-page` / `-split-restore` / `-append-opens` /
`-menu-hint-box`)。🔑 **user が実機で見て「動きがおかしい」と言ったら、それが裁定**である
(CLAUDE.md「推薦を出したら、決めるところまでやる」の 2026-09-02 追記)。

## 3. 次セッションが自分の裁定で進めてよいもの(着手の順と理由)

| 順 | 何を | なぜこの順 |
|---|---|---|
| 1 | **#632 / #633 の設計 doc に user の裁定が来たら実装**(doc-first。裁定前は着手しない)。段は各 doc の「段」の表のとおり、**段①だけ**を 1 PR で | 裁定済みの Q1〜Q4 で、いちばん大きい仕事。設計は済んでいるので裁定が来れば即着手できる |
| 2 | **着地した 3 件を user 目線で 1 巡する**(`pkc3-ux-reviewer` を #649 / #650 / #651 の**動線**に当てる:横に留めた並びの復元 / 畳んだ追記欄が開く / メニュー下の説明欄)。「良くできる所」も出させ、issue に**画面の言葉**で起票する | user 指示 2026-08-28「改善はこちらから出す」。3 件とも CI を待たずに入れたので、レビューが 1 巡も回っていない |
| 3 | Q6(#587 C-3)の**使用感の裁定**が来たら、1 組(`withHint` + CSS 2 規則 + `data-pkc-hint`→`title`)で直す / 戻す | 「一度推奨で入れて、使用感をテストしたい」の後半。来るまで触らない |
| 4 | `#604` の索引を **1 件 1 grep で検算**して更新(#596 設問③ / #609 の ⑴ / #587 C は片付いた。本文の表が古い) | 索引は「次に読む人が必ずここから数える」場所(CLAUDE.md)。腐ったまま渡さない |

⚠ **本番リリースは user の示唆待ち**(v3.2.0 のまま。`/dev/` には今日 4 本入っている)──
自分から `v*` tag を打たない / `release.yml` を起動しない。

## 4. 🔴 user の裁定待ち(選択肢まで)

⚠ 裁定待ちの**索引は #604**(2026-08-30 の 6 問の回答と、その後の追加が全部そこに在る)。今日増えたのは **#632 / #633 の設計 doc の設問**。

| 群 | 何を決めてほしいか | どこに書いてあるか |
|---|---|---|
| **A. #632 スマホ用画面(6 問)** | ① 720px 以下は PC で細くしてもスマホ用画面でよいか(推薦 A)② 本文ページの上の帯(← 一覧 / 題名 / 情報 / ⋯)の並び(推薦 A: 情報は全画面)③ お知らせカードをスマホでは画面の 1/4 まで(推薦 A)④ 触る端末では打つ欄の字だけ 16px(推薦 A)⑤ 触る端末では隠れボタン 2 つを常時表示(推薦 A)⑥ 360px 未満は状態の行に 1 度だけ断る(推薦 A) | `docs/development/mobile-screen-design-2026-09.md` §4 |
| **B. #633 スタック(4 問)** | ① 生きている並びはこの端末だけ / 別の端末でも同じ帯(推薦 A: この端末だけ。保存した入れ物が端末をまたぐ)② 帯は本文の上・新しく載せた物が本文のすぐ隣(推薦 A)③ 字を「スタックに載せる」「× 降ろす」へ(推薦 A)④ 入れ物を載せると「上に積む」(推薦 A。入れ替えは片道) | `docs/development/stack-design-2026-09.md` §4 |
| **C. #587 の残り** | C-3(メニュー下の説明欄)の**使用感** / C の続き 2 問(見出し・本文のメニューの揃え方 案 1/2、「書き戻す」に「戻せません」)/ D(「写す」と「コピー」を 1 語に。推薦 D-1) | #587 の 2026-09-02 コメント / #604 |
| **D. #648 マニュアルの窓の残り 6 提案** | I2 / I4 / I6 / D4 / 段③(持ち歩ける 1 枚でも同じ page)/ 💭 product gate ── どれを入れるか | #648 本文 |
| **E. #604 に残る設問** | #596 設問①②(見出しメニューの並び・区切り)/ F-2 Office のマクロを残すか / F-3 Office 一式の入れ替え / #637 の残り(推薦 B)/ #529 / #531 / #528 / #274・#275 の着手 go | #604 |
| **F. #634 A** | リボン UI で落ちる条件の再現には **user の Office 一式の版**が要る(C は着地済み) | #634 |

🔑 **A と B が来たら着手できる**(設計は済んでいる。段①だけを 1 PR で)。

## 5. 踏んだ罠と覆った前提(要約とポインタ ── 正本は `.claude/` と CLAUDE.md)

| 罠 / 覆った前提 | 正本 |
|---|---|
| 🔴 **横に留めた並びの「憶える」は配った初日から一度も成立していなかった**(描画の購読が復元より前に空を書いていた)── お知らせとマニュアルは「次に開いても同じ枠が出ます」と書いていた | #633 コメント(2026-09-02)/ `main.ts` の注記 / `tests/adapter/bootstrap-wiring.test.ts` |
| 🔴 マニュアルの窓で「何も変えずにもう一度押す」と 14px → 13px に縮んでいた ── **効いている値**(既定 13px)を渡していたが、焼いた page は**選んでいなければ触らない**(14px)。読み手が 2 つある値は**同じ門**で読む | `text-scale.ts` の `chosenTextScale` の注記 / `tests/features/text-scale.test.ts` |
| 🔑 **R1**(直す前の dist で新しい test を回して赤を見る)が 3 件とも 1 回で決着した | `.claude/skills/mutation-testing/SKILL.md`「R1」 |
| 🔴 1 本の branch で主題を 2 つ以上並行させるときの作法(local branch に停めて cherry-pick で載せ直す)。⚠ `git cherry-pick -q` は無効 / `git add -A` が untracked の doc を巻き込んだ / この箱は Bash の cwd が `/home/user` へ戻る | `.claude/skills/pr-landing/SKILL.md` §4 |
| ⚠ `KNOWN`(announce.test.ts)に、落とした行の注記だけが **185 行**残っていた | `.claude/skills/notice-writing/SKILL.md` |
| ⚠ この箱の `Workflow` は**同時 2 本**(4 core)── 8 本投げると 1 時間の桁 | `.claude/skills/subagent-scale/SKILL.md` §2 |
| ⚠ 変異試験 H4: 「開いた直後の説明を明示で出す」呼びは **no-op** だった(`focus()` が `focusin` を同期で出す)── 外した | `context-menu.ts` の注記 |

## 6. 測って「問題なし」と分かったこと(再調査させない)

| 何を | 値 | 主張の範囲 |
|---|---|---|
| 配る量の cap 残量 | **591.3 KB(7.9% / cap 7500 KB)** | dev build、#651 の時点。3 PR で動いたのは −1.8 KB |
| フル smoke(headless_shell = CI の binary) | **372 件緑 / 13.6 分**(手元) | #649 の時点。CI 側は smoke 2 分割で各 5〜6 分 |
| `rtf-to-markdown` の 4MB 解析 | 単独 **3.4 秒**(file 全体)/ `npm test` 全量(4 worker)で **5 秒超**が 1 回 | 本 PR で test に 20 秒の予算を明示。製品の速さの話ではない |
| 右クリックの説明 11 件 | 最長 **55 字**(上限 56 = 22rem × 2 行) | `ENTRY_ACTION_HINT_MAX` が全件 pin |
| 説明欄の高さ | 2 行の高さに収まる(実測、chromium / headless_shell) | `context-menu.smoke.spec.ts` |

## 7. やらないと決めたこと(理由つき)

| やらないこと | 理由 |
|---|---|
| 見出し・本文の右クリックのメニューに説明を書く | #587「C の続き」の案 1 / 案 2 が**裁定待ち**(見え方が変わる) |
| Q6 の tooltip(`title`)を残す | C-1 の欠点そのもの(乗せて 1 秒待つ箱が下の項目に重なる / マウスを持たない人に届かない)。戻すなら**裁定で** |
| 焼いたマニュアルの既定 14px をアプリの 13px に揃える | 読み物の大きさは **user の見え方**(2026-08-28「正直変更はユーザーに委ねて欲しい」)。設定で選べば揃う |
| #648 の残り(I2 / I4 / I6 / D4 / 段③ / 💭)を自分の裁定で入れる | 見え方が変わる提案なので user の選択待ち |
| 本番 tag を打つ | user 指示 2026-08-19「本番リリースは私が示唆します」 |
| 4MB RTF の test を「速くする」方向で直す | 主張は「読む」であって「速い」ではない ── 予算を明示するだけ |
