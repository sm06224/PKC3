# セッション引き継ぎ 2026-08-17

⚠ **この doc は「経緯と根拠」だけを持つ。** やること(残件)は **issue に在る**
(user 指示 2026-08-15「ファイルをただ置いておくのはやめろ」)。

## 着地したもの(4 PR / 全部 merge 済み)

| PR | 中身 |
|---|---|
| **#224** | 夜の検査を直す(#221)── 13 晩赤かった原因は probe の観測点。後続 2 本は走ってすらいなかった |
| **#226** | Office 書き戻しの 2 巡目レビュー残件 7 件(#220) |
| **#227** | 計器を動く状態へ戻す(#223)+ **Word 書き出し 段①**(#187) |
| **#228** | 書き出しフィルタの層を割る probe(#225)── 命令行の変換はこの一式では動かなかった |

**検証の総量**: unit 3,277 green / smoke 145 green / **変異試験 37 件すべて KILLED** /
bench 4 本を実走 / branch 上で Nightly を dispatch して全 step 緑で完走。

## 台帳を実測に合わせた(この日の主要な仕事の 1 つ)

- **#217 → close**(実機で保存 3 回・増殖 0 回を裏取り)
- **#218 → close**(根拠が 1 回目の検証結果。切り分けは**反転**した ── #225 へ)
- **#220 / #221 / #223 → close**(上の PR で着地)
- **#180(移植の穴 22 件)** ── 22 件を**実装で当て直し**、**10 件着地 / 3 件は意図的な
  見送り**を確定。残る塊は **A-6 / B-2 / C-1 / C-4 鎖** の 4 つ

## 経緯 ── 何を間違え、どう分かったか

### 1. 引き継ぎの数字は「見た件数」に縛られる

前セッションの引き継ぎは Nightly を「**3 晩**連続赤」と書いていたが、run を全数で
数えると **2026-08-04 から 13 晩**だった(直近 3 件しか見ていなかった)。
🔑 数字を書くときは「**何件見たか**」も書く ── 書かないと全数と読まれる。

### 2. 赤の正体は製品ではなく probe の観測点だった

`run-sidebar-probe.mjs` が **document 全体**で `[data-pkc-entry]` を数えており、
P8(#59)で入った情報ペインのボタン 3 つが同じ属性を持つので、**選択した瞬間に
3 件増える**。一覧の中の 15,000 行は全ノード同一だった ── **主張は今も成り立つ**。

そして step の既定(前が落ちたら skip)のせいで、後続 2 本は **13 晩 1 度も走って
いなかった**。走らせたら**両方壊れていた**(editor は #172 で既定が変わったのに
`editor-body` を掴んだまま / kanban は #59 の封印で切替ボタンが無い)。

### 3. 実機レポートの前提が 1 段違っていた

user から「渡したのは 1 回目の検証結果だった」と訂正が入り、**#218 の切り分けが
反転**した ── **PKC が渡した `.odt` は通り、`.docx` が落ちる**(渡し方ではなく書式)。
🔑 **渡された値にも「どのビルドで採ったか」を問う**(自分の申告にだけ問うのでは足りない)。

### 4. 自分の観測点で 2 度、存在しない不具合を見かけた

- 2 枚目のタブの帯を **root 全体**で探し、**お知らせのカード**に満たされて
  「昇格しても帯が消えない」= 製品の不具合に見えた(実際は正しい)
- `--convert-to` の probe で **対照群(native の odt)も 0 バイト**だった ──
  対照群が無ければ「docx が落ちた」と書いていた

どちらも**報告する前に**気づけた。🔑 面(region)へスコープする / 対照群を先頭に置く。

### 5. CI の guard が私を捕まえた(guard のほうが正しかった)

足した probe が `--language=ja` を渡していないことを `repo-hygiene` が止めた。
⚠ probe が**アプリと違う構成**で LO を起こしていたら、測っているものが別物になる。
⚠ このとき **push 前に unit を回していなかった**(lint だけ見て出した)。

## いま残っていること

**issue を見ること。** 主要なものだけ挙げる:

- **#225** ── `.docx` が保存できない。**層の切り分けは実機でしか採れない**ところまで
  詰めた(指示プロンプトは #225 のコメントに全文)
- **#187** ── Word 書き出し。段① 着地、段②(画像)〜段⑤ が残り
- **#180** ── 移植の穴。実質の塊は A-6(関係グラフ)/ B-2(テンプレート)/
  C-1(#187)/ C-4 鎖(#189 → #194 / #195)
