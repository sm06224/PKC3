# セッション引き継ぎ 2026-08-17

⚠ **この doc は「経緯と根拠」だけを持つ。** やること(残件)は **issue に在る**
(user 指示 2026-08-15「ファイルをただ置いておくのはやめろ」)。

## 着地したもの(4 PR / 全部 merge 済み)

| PR | 中身 |
|---|---|
| **#224** | 夜の検査を直す(#221)── 13 晩赤かった原因は probe の観測点。後続 2 本は走ってすらいなかった |
| **#226** | Office 書き戻しの 2 巡目レビュー残件 7 件(#220) |
| **#227** | 計器を動く状態へ戻す(#223)+ **Word 書き出し 段①**(#187) |
| **#228** | 書き出しフィルタの層を割る probe(#225)── 命令行の変換はこの一式では動かなかった |

**検証の総量**: unit 3,277 green / smoke 145 green / **変異試験 37 件すべて KILLED** /
bench 4 本を実走 / branch 上で Nightly を dispatch して全 step 緑で完走。

## 台帳を実測に合わせた(この日の主要な仕事の 1 つ)

- **#217 → close**(実機で保存 3 回・増殖 0 回を裏取り)
- **#218 → close**(根拠が 1 回目の検証結果。切り分けは**反転**した ── #225 へ)
- **#220 / #221 / #223 → close**(上の PR で着地)
- **#180(移植の穴 22 件)** ── 22 件を**実装で当て直し**、**10 件着地 / 3 件は意図的な
  見送り**を確定。残る塊は **A-6 / B-2 / C-1 / C-4 鎖** の 4 つ

## 経緯 ── 何を間違え、どう分かったか

### 1. 引き継ぎの数字は「見た件数」に縛られる

前セッションの引き継ぎは Nightly を「**3 晩**連続赤」と書いていたが、run を全数で
数えると **2026-08-04 から 13 晩**だった(直近 3 件しか見ていなかった)。
🔑 数字を書くときは「**何件見たか**」も書く ── 書かないと全数と読まれる。

### 2. 赤の正体は製品ではなく probe の観測点だった

`run-sidebar-probe.mjs` が **document 全体**で `[data-pkc-entry]` を数えており、
P8(#59)で入った情報ペインのボタン 3 つが同じ属性を持つので、**選択した瞬間に
3 件増える**。一覧の中の 15,000 行は全ノード同一だった ── **主張は今も成り立つ**。

そして step の既定(前が落ちたら skip)のせいで、後続 2 本は **13 晩 1 度も走って
いなかった**。走らせたら**両方壊れていた**(editor は #172 で既定が変わったのに
`editor-body` を掴んだまま / kanban は #59 の封印で切替ボタンが無い)。

### 3. 実機レポートの前提が 1 段違っていた

user から「渡したのは 1 回目の検証結果だった」と訂正が入り、**#218 の切り分けが
反転**した ── **PKC が渡した `.odt` は通り、`.docx` が落ちる**(渡し方ではなく書式)。
🔑 **渡された値にも「どのビルドで採ったか」を問う**(自分の申告にだけ問うのでは足りない)。

### 4. 自分の観測点で 2 度、存在しない不具合を見かけた

- 2 枚目のタブの帯を **root 全体**で探し、**お知らせのカード**に満たされて
  「昇格しても帯が消えない」= 製品の不具合に見えた(実際は正しい)
- `--convert-to` の probe で **対照群(native の odt)も 0 バイト**だった ──
  対照群が無ければ「docx が落ちた」と書いていた

どちらも**報告する前に**気づけた。🔑 面(region)へスコープする / 対照群を先頭に置く。

### 5. CI の guard が私を捕まえた(guard のほうが正しかった)

足した probe が `--language=ja` を渡していないことを `repo-hygiene` が止めた。
⚠ probe が**アプリと違う構成**で LO を起こしていたら、測っているものが別物になる。
⚠ このとき **push 前に unit を回していなかった**(lint だけ見て出した)。

---

# 続き(同日の後半。#230〜#236)

## 着地したもの(7 PR / 全部 merge 済み)

| PR | 中身 |
|---|---|
| **#230** | Word に**画像**(#187 段②)+ 🔴 **保存直後に書き出すと保存前の本文が出ていた**のを直した |
| **#231** | Word に**図とグラフ** ── **画面に見えている面だけを写す** |
| **#232** | **紙面・改頁・ページ番号・文書の材料**(段③)+ `:::if{format=docx}`(段⑤) |
| **#233** | 組み立てと zip を**ワーカーへ**(段④)── **#187 は段①〜⑤ 完了** |
| **#234** | **関係グラフの設計 doc**(#186)── ⚠ 実装なし・**裁定待ち** |
| **#235** | 閲覧用 HTML の注意も同種 10 件で畳む(#202) |
| **#236** | IDB へ `Blob` を書く罠を注記と型 pin で塞ぐ(#211) |

**検証の総量**: unit 3,332 green / smoke 146 green / **変異試験 37 件すべて KILLED** /
bench を 1 本足した(`tests/bench/run-docx-export.mjs`。対照群つき A/B も回した)。

🟢 **夜の検査が 14 晩ぶりに緑**になった(2026-08-17 18:32 の schedule 実行)──
schedule の run を全数(17 件)数えた結果: `08-03 緑` → **13 晩赤** → `08-17 緑`。
#224 の直しが**定期実行で裏取りできた**。

## この半日で踏んだ型(全部「緑のまま壊れていた」)

1. 🔴 **flake の顔をした製品の穴**(#230)── smoke が 25 回中 7 回赤。
   effect 層は**書込だけ**を直列化しており、**読みはその外**なので
   `getBody` が並んだ書込を追い越す。対照群つきで **11/12 が古い本文**(800ms 待つ群 0/12)。
   🔑 観測点は**配線そのもの**(`Worker.postMessage` を包んで命令の順番を採った)
2. 🔴 **test の入力が「手書きの HTML」だった**(#231)── 本物のレンダラを通したら、
   段① に**実物でしか出ない穴が 3 つ**あった(コピーの ⧉ が段落になる / 切替の ‹/› も出る /
   **描画と原文が二重に出る**)。🔑 **入力は本物の出力にする**
3. 🔴 **「動いているのに 1 度も通っていない」**(#233)── `WorkerLease` の包み
   (`{id, payload}`)を平らな形と読み違え、ワーカーは毎回失敗して落とし所が拾っていた。
   **zip は正しく落ちてくる**ので気づけない。⚠ **最初に採った A/B の数字も嘘だった**
4. 🔴 **主スレッドが worker module を import していた**(同)── `self.onmessage` を差すので
   **window の message を横取り**する。組み立てを別 file へ分けた

## いま残っていること(**ほぼ全部が裁定待ち**)

| # | 件 | 待っているもの |
|---|---|---|
| #186 | 関係グラフ | **設計 doc の A〜E**(`docs/development/graph-view-design-2026-08.md`)。⚠ 調査で分かったが **A-6 は移植の穴ではなく新規設計**(PKC2 の本体にグラフは無い / 撤去済みで履歴も読めない / 実測も無い) |
| #199 | 図入り docx が LO wasm で開かない | 緩和 4 案(⚠ **PKC 自身が書き出す docx が該当する**) |
| #213 | 控えの台帳行 | マニュアルに 1 行足すだけでよいか(**直すと戻せなくなる**) |
| #196 | テンプレート | **text blaze 保留の go**(user 裁定 2026-08-03 のまま) |
| #178 | 多重タブの書込衝突 | 断る / 遅延 / 楽観検査 のどれで守るか |
| #225 / #214 | Office | **実機でしか採れない**(#214 は「同じ回で 1 手増やすだけ」で埋まる) |
| #212 | 差し替えの走査 | issue 自身の合図(**実機で 50ms 超**)待ち |

⚠ **台帳の整理も同時にやった**: #180 を実測に合わせ、#208 を **#215 の重複として close**、
#213 / #214 は「どこまで済んでいるか」を実装で確かめてコメントした。

---

# 続きの続き(同日 21 時台。#242〜#245 と user の新しい指示 4 件)

⚠ **上の「最初の仕事: 無い」は、その時点では誤りだった。** 台帳を実態で見直したら
**#114(測るだけ・2026-08-11 から未着手)が裁定なしで着手できた** ── 「裁定待ち」を
理由に止まる前に、**測る仕事・畳む仕事が残っていないか**を数え直すこと。

## 着地したもの

| PR | 中身 |
|---|---|
| **#242** | 使わない OPFS VFS を建てさせない(#114)── **renderer の常駐 −3.9MB**(6 組とも同じ向き・範囲が重ならない) |
| **#243** | 左下の低頻度ボタンを設定へ逃がす(#239、user 指示)── **畳むのではなく面を移す** |
| **#244** | 整理の面の設計 doc(#240 / #241)── **裁定 6 点**。実装なし |
| **#245** | 今日踏んだ 4 つの型を検証の規律へ反映(`.claude更新`) |

台帳を 3 件 close(**#202** / **#211** / **#114**)。

## user から来た新しい指示(4 件、全部起票済み)

| # | 中身 | 状態 |
|---|---|---|
| **#238** | 🔴 バグレポ「PKC で書き出した docx が LO で開けない(窓が空)」 | **実機の 1 手待ち**(下記) |
| **#239** | 左下の低頻度ボタンを設定へ | ✅ **#243 で着地** |
| **#240** | 左ペインをフォルダ主体へ(複数選択 / 範囲選択 / D&D / まとめて削除) | 設計 doc 済み・**裁定待ち** |
| **#241** | 2 ペインタブファイラ + PKC2 のコマンド機能 | 同上(**同じ doc**) |

## #238 でここまで確定したこと(繰り返さないこと)

🔑 **書き出した `.docx` は壊れていない。** この箱に `libreoffice-writer` を入れて
**native の LibreOffice** に食わせたところ、**図なし・図ありとも PDF になった**
(20,596 B / 22,368 B)。部品も揃っている。

⚠ **最初の試行では 2 本とも落ちたが、対照群(LO 自身の docx / ただの `.txt`)も落ちた** ──
箱に `libreoffice-core` しか無く Writer の filter が無かったのが原因で、そのまま読んでいたら
**存在しない不具合**を書いていた。

🔴 **残る層は PKC 内の Office(LO wasm)側**だが、**この箱では原理的に測れない**:

- Pages から実物の pack を取る → 外向き HTTP が proxy で止まる(`000`)
- git から取る → `sm06224/office-pack` は **`.github` と README しか無い**(pack は CI が焼いて Pages へ出す)

🔑 **分かれ目の 1 手**(実機):**図もグラフも無いノート**を Word 書き出しして PKC の Office で開く。
**開く** → #199 と同じ現象(統合)/ **開かない** → 別の原因(#238 で追う)。
⚠ 図つきで試すと #199 の条件を満たすので**分かれ目にならない**。

## 🔴 最初の仕事(次のセッションへ)

**無い(裁定 / 実機待ち)。** ただし ⚠ **上の警告のとおり、まず台帳を「測る仕事・畳む仕事」で
数え直すこと** ── #114 はそうやって見つかった。

裁定を待っているもの: **#240 / #241**(doc §6 の 6 点)/ **#186**(A〜E)/ **#199**(緩和 4 案)/
**#213** / **#196** / **#178**。実機を待っているもの: **#238**(1 手)/ **#225** / **#214**。
