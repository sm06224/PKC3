# 変更のあゆみ

このファイルは、**アプリの「お知らせ」に出した内容をぜんぶ残す場所**です。

アプリの中のお知らせ(ヘルプ画面の「これまでのお知らせ」)は、配る量を抑えるために
**新しい 20 件だけ**を持ちます。それより古いものはアプリから落ちるので、
**落ちたぶんの原本をここに残します** ── 落ちたら二度と読めない、を作らないためです。

⚠ ここに書いてあるのは **user から見て何が変わったか**だけです。実装の理由や設計は
`docs/development/` 側に在ります。
⚠ 掲示した文面は書き換えません(閉じた user にはもう届かないので、訂正は
**新しい 1 件**として足します)。ここでも書き換えず、誤りが見つかった行の下に
**`⚠ 訂正(日付)`** を足す形にします ── 何を配ったかと、何が正しいかの両方を残すためです。

## 2026-08-29

### 絞り込みで連絡先が 0 件でも、その場で戻れるようになりました

- 一覧で語を打ったまま連絡先のタブへ行くと、当たりが 0 件になり「vCard で書き出す」ごと消えていました。
- 0 件のときは「絞りを外す」が出ます。押すと絞り込みが空になり、連絡先と書き出しの口が戻ります。
- 探す欄の字も、どのタブでも今の絞り込みに合うようになりました。これまでは一覧のタブでしか合っていませんでした。
- タグの札を押して絞ったときも、探す欄にその語が出るようになりました。

### 本文の中に書いたタグが、札で出るようになりました

- 本文の行に「#買い物 #家事」と書くと、その行が札の形で出ます。文章と見分けやすくなります。
- 札を押すと、そのタグが付いたノートで一覧が絞られます。読みながらそのまま辿れます。
- 見せ方は設定の「本文のタグの見せ方」で変えられます。札・枠の札・文字のまま から選べます。
- 本文そのものは変わりません。書いた字はそのままで、見え方だけが変わります。
- 書き出した HTML でも札で出ます(そちらは押せません)。

### 段組みが幅で畳まれたとき、理由が画面に出るようになりました

- 左右のペインや窓を狭めると、幅が足りないぶん段組みは自動でやめて、縦に読む形へ戻ります。
- これまでは何も出ないまま消えていたので、画面下の帯に「幅が足りないので段組みをやめました」と出るようにしました。
- 何 px あれば戻るかも同じ帯に出ます。広げて戻ったときは「段組みに戻しました」と出ます。
- 自分で段数を選んだときは、これまでどおり選んだ結果だけが出ます。

### 本文の中にタグを書けるようになりました(編集中でも打てます)

- 本文の行に「#買い物 #家事」のように書くと、その行がタグの行になります。井桁のすぐ後ろに空白を置かないのが見出しとの違いです。
- これまでタグは本文の先頭の「tags:」の行だけで、編集中は打てませんでした。本文の中なら編集しながら打てます。
- タグは保存のときに重複をまとめて集められます。同じタグを何度書いても 1 つに数えます。
- スマートフォルダは、本文の中に書いたタグでも集めるようになりました。先頭に書いたタグと同じ扱いです。
- 番号だけの行(#117 #121 のような書き方)はタグになりません。これまでの本文がタグの行に変わることはありません。

## 2026-08-28

### ヘルプを閉じてしばらくすると、マニュアルの分のメモリを返します

- ヘルプの面を開くとマニュアル全文が組み立てられます。これまでは閉じたあともずっと抱えたままでした。
- しばらく開かなければ中身を手放し、次に開いたときに組み直します。組み直しは 1 秒かかりません。
- すぐ開き直したときは組み直さないので、待たされることはありません。

### 本文の画像も別の窓で大きく見られ、掴んで送れます

- 本文に貼った画像にマウスを載せると虫めがねのカーソルになります。押すと別の窓が開き、縮める前の大きさで出ます。
- 大きくした絵は、掴んで動かすと見たい所へ寄せられます。端の細い棒を掴まなくても送れます。
- 添付の一覧から開く窓でも、「＋」「−」「等倍」で大きさを変えられるようになりました。開いた直後の見え方は今までどおりです。
- 大きくしすぎたら「収める」で窓に収まる大きさへ戻せます。開き直す必要はありません。
- 編集しているときは、これまでどおり絵を押すとその行の原文が開きます。

### 図を押すと、別の窓で実寸で開けます

- 本文の図にマウスを載せると虫めがねのカーソルになります。押すと別の窓が開き、図が実寸で出ます。
- 窓の中の「＋」「−」「等倍」で大きさを変えられます。Ctrl(mac は Command)を押しながらホイールでも変えられます。
- 段組みで読んでいるときにいちばん効きます。段に収めるために小さくしてあるので、別の窓では小さくなる前の大きさで出ます。
- 押し所は増えていません。図そのものが押し所です。
- 編集しているときは、これまでどおり図を押すとその図の原文が開きます。

### 段組みのまま、その場で書き替えられます

- 本文を段組みで読んでいるとき、これまでは編集に入ると段が丸ごとほどけていました。
- いまは Ctrl(mac は Command)を押しながら行をクリックすると、その行だけが原文になり、段は解けません。
- 打っている箱は段の高さで止まります。超えた分は箱の中で送れるので、打った字が段の外へ消えることはありません。
- 箱が段の下のほうで開いたときだけ、次の段のいちばん上へ 1 度だけ移ります。そのあとは打っても動きません。
- 設定で 2 ペインの編集を選んでいる場合は、これまでどおり段になりません。

### UML の図が選ぶだけで入り、SVG も貼れば絵になります

- 書式の帯の「雛形」を押すと、自分の雛形のあとにクラス図・シーケンス図・状態遷移図・ER 図が並びます。押すとその場に書き出しが入るので、そのまま書き替えるだけです。
- これまでは「図」を押すとフローチャートだけが入り、1 行目を書き替えると別の図になることを知っている人しか使えませんでした。
- 押し所も鍵も増えていません。「図」を押したときに入るものも、これまでと同じです。
- SVG の絵は、svg と書いた囲みに貼るとそのまま絵になります。これまでは html と書いた囲みに入れたときだけ絵になり、それ以外は字のまま出ていました。
- ほかにも 22 種類の図が書けます。1 行目に書く名前の一覧はマニュアルの「どんな図が描けるか」にあります。

### HTML の中で外から取ってこられなかったとき、理由が出ます

- 本文に書いた HTML が、外にあるプログラムや見た目(CSS)を読もうとして止まったとき、これまでは何も出ないまま真っ白になっていました。
- 止めた理由と、何が止まったのかが本文の下に出るようになりました。
- どうしても動かしたいときの道も一緒に出ます。HTML をファイルとして添付し、その画面の「アプリとして登録」を押すと、別の窓で開けます。
- 外の画像だけは、これまでどおりその場で「読み込む」を選べます。

### 段と段のあいだの線を、はっきりさせられます

- 段組みで読んでいるとき、段の境目の線が薄くて見分けにくいというご報告をいただきました。測ったところ、線と地の差が基準を下回っていました。
- 設定の「段の境界線」で「細い(既定・いまと同じ)/ はっきり / 線なし」から選べます。
- 既定は今までと 1 バイトも同じ見え方です。選ばなければ何も変わりません。

### Alt+C で段組みを切り替えられ、いま何段かが出ます

- 読みながら Alt+C を押すと、本文の段組みが 1 段 → 2 段 → 3 段 → 4 段 と回ります。設定画面を開かなくても試せます。
- 押すと画面の下に「本文の段組み: 3 段」と出ます。設定画面にも同じ字が出ます。
- 選んだ数どおりにならないことがあります。1 段の細さには下限があるので、画面が狭いと入る数まで減ります。そのときは「いまの画面では 2 段で出ています(3 段には 1376px 以上の幅が要ります)」と理由まで出ます。
- 選んだ数は消えません。広い画面で開けばそのとおりに出ます。

### 段組みで、縦に長い図や写真が切れなくなりました

- 本文を段組みで読んでいるとき、縦に長い図や写真が段からはみ出して、下のほうが見えなくなっていました。縦のホイールは横送りになるので、見えない分に届く手段がありませんでした。
- いまは段の高さに収まるまで小さくなり、全体が見えます。1 段で読んでいるときの見え方は変わりません。
- 図の右上の「図を保存」も、図と同じ段に出るようになりました。これまでは別の段に離れて出ることがありました。

### 行の編集で、下(上)まで来てもう 1 回押すと隣の行へ移ります

- 行を開いて編集しているとき、いちばん下でもう 1 回 ↓ を押すと、その行を確定して次の行が開きます。上も同じで、いちばん上でもう 1 回 ↑ です。
- これまでの Alt+↓ / Alt+↑ は無くなりました。鍵を覚えなくても、素のカーソルだけで移れます。
- 長い段落は折り返して何行にも見えますが、その途中では移りません。本当にいちばん下まで着いてからです。
- 字を打ったり別のキーを押したりすると数え直しになります。打った直後の 1 回で飛ばないためです。

### クラス図・シーケンス図・状態遷移図・ER 図が描けます

- 図の囲み(mermaid)は、1 行目に書く名前で種類が変わります。クラス図・シーケンス図・状態遷移図・ER 図・要求図・ガント・年表・かんばん・マインドマップなど 22 種類が描けます。
- これまでマニュアルに書いていなかったので、書けることが伝わっていませんでした。マニュアルの「どんな図が描けるか」に一覧を載せました。
- 体験の流れ(journey)だけ、これまで図にならず「Tainted canvases」という英語の理由が出ていました。描けるようになりました。

### 連絡先の取り込みが、失ったものをきちんと言うようになりました

- 取り込む前に編集中だと、これまではファイルを選んだ後で断られていました。いまはボタンを押した時点で「編集を終了してから」と出ます。
- 文字が壊れている .vcf を取り込むと、これまでは名前が黙って化けていました。いまは「文字が壊れている所がありました」と注意が出ます。
- 中身が何も無いカードは、空のノートを作らずに飛ばします(そのときも注意が出ます)。
- 連絡先が多くて途中までしか集められていないときは、「vCard で書き出す」のボタンと、書き出した後の帯でもそう言います。全部だと思って元のファイルを消さないためです。

### 断り方と、目次の飛び方が良くなりました

- 編集中に予定の面で札を掴んで日に落としたり、× で予定を外したりすると、これまでは札が黙って元に戻るだけでした。いまは「編集を終了してから」と画面の下に出ます。
- 断り文が「日付を付ける」「日付を外す」「行の予定を付ける」のように、押した場所に合わせて変わります。
- 保存に失敗しているときは「編集中は使えません」と出なくなりました。編集していないのに編集を探さずに済みます。
- 集計の面でノートを選んでから右の目次を押すと、これまでは 1 回目が空振りして「もう一度押してください」と出ることがありました。いまは 1 回で本文の見出しまで飛びます。

### 連絡先を vCard で出し入れできるようになりました

- スマホや他のアプリから書き出した .vcf ファイルを、左の列の下の「取り込む」から選ぶと、1 枚が 1 ノートになります。電話かメールが書いてあるものが連絡先タブに並びます。
- 連絡先タブの下の「vCard で書き出す」を押すと、いま見えている連絡先が .vcf ファイル 1 つになって落ちてきます。絞り込み中は絞った分だけです。
- 書き出されるのはこのボタンを押したときだけです。バックアップや他の書き出しに、連絡先が自動で混ざることはありません。
- 取り込みで写せない項目(住所など)は、ノートの本文に行として残ります。写真や音など中身が符号化されたものと、電話やメールの種別(携帯・自宅・FAX)は取り込めません(そのときは注意が出ます)。
- 書き出しに入るのは名前・所属・電話・メール・誕生日です。住所やメモなど本文に書いた残りは入らないので、元の .vcf は残しておいてください。

### 付箋を自由に置ける「板」が作れるようになりました

- 本文に :::format{.pkc-place x=40 y=40 w=320 h=200} と書いて ::: で閉じると、その塊が書いた座標に置かれます。
- 塊の右上の持ち手(⠿)を掴んで動かすと、離した位置が本文の x= と y= に書き戻されます。位置の正本は本文です。
- :::format{.pkc-place entry=ノートのID} と書くと、そのノートの題名の札になります。押すと本体が開きます。
- ノートの ID は「参照をコピー」で得られる entry: の後ろの英数字です。閉じ括弧は含めません。コピーした 1 行をそのまま塊の中に貼っても、押せるリンクになります。
- 付箋が座標に置かれるのは読む画面だけです。編集の画面と書き出した HTML では、上から順に並びます。

### 見出しから目次が自動で出るようになりました

- 右の情報ペインに「目次」の行ができました。本文の見出し(#、##、###)から自動で並びます。
- 押すと、本文のその見出しまで移動します。
- 見出しのないノートでは、この行は出ません。
- これまでは本文に :::toc と書いた場所にだけ出る形で、書かないと出ませんでした。
- 書き出した閲覧用 HTML には前から目次が出ていたので、その食い違いも無くなりました。
- :::toc の書き方はこれまでどおり使えます(本文の好きな位置に置きたいとき)。

### タグを、その場で打てるようになりました

- 右の情報ペインの「タグ」の行に、タグを打つ欄ができました。打って Enter で付きます。
- これまでタグを付けるには、本文の先頭に tags: と書くか、フォルダの面で行に印を付ける必要がありました。
- 付いたタグには × が出ます。押すとそのタグだけ外れます(ノートも本文も消えません)。
- 欄に触ると、すでに使っているタグが候補に出ます。同じタグを打ち間違えて増やさずに済みます。
- 候補に無い新しいタグも、これまでどおりそのまま打てます。
- 編集中と、本文の先頭が読めていないときは、この欄は出ません(書ける状態のときだけ出ます)。

### ペインと追記欄の大きさを、自分で決められるようになりました

- 一覧・情報・追記欄の境目にある細い帯を掴んで動かすと、大きさが変わります。
- 帯を押すだけなら、これまでどおり畳む・戻すです。掴んで動かした直後は畳まれません。
- 追記欄の上にも帯が出ました。押すと追記欄が消え、もう一度押すと戻ります。
- 小さくしすぎると畳まれます。帯は残るので、押せば元の大きさで戻ります。
- 帯に焦点を当てて、矢印キーでも動かせます。
- 決めた大きさは覚えます。設定の持ち出しにも入ります。

### 予定の面で、外すのと足すのが分かるようになりました

- 札にマウスを乗せると右端に × が出ます。押すと予定から外れます。ノートも本文も消えません。
- 繰り返しの予定にも × が出ます。こちらは「この繰り返しをやめる」になります(本文の @日付 毎週 が外れます)。
- これまで外すには札を「日付なし」へ掴んで落とすしかなく、繰り返しは掴んでも動かせませんでした。
- 「足す」の日付欄が、最初から今日で埋まるようになりました。そのまま押すと今日のところに出ます。
- 日付なしで足したいときは、欄の日付を消してから押してください。
- 足したものが、その場で面に出るようになりました(これまでは面を開き直すまで出ませんでした)。

### ノート 1 件を、相手が開けるだけの HTML で渡せるようになりました

- 右の情報ペインと、行の右クリックに「閲覧用 HTML」が増えました。
- 押すと、そのノートだけが 1 つの .html になります。相手はブラウザで開くだけで読めます。
- PKC3 を持っていない相手にも渡せます。片道です(取り込み直せません)。
- 設定 → 書き出しと片づけ にある同じ名前のものは、これまでどおり全部をまとめます。

### 段組みが、文字の大きさに合わせて広がるようになりました

- 文字を大きくすると、段の幅も一緒に広がります。1 段に入る字数は変わりません。
- これまでは段の幅が固定だったので、大きくすると 1 段が窮屈になっていました。
- 幅が足りなくなったら、これまでどおり自動でふつうの縦送りに戻ります。
- 「標準」のままなら、見え方はこれまでと同じです。

### 大きなウェブページも、そのまま貼れるようになりました

- ウェブページを貼るときの大きさの上限(1MB)を撤廃しました。「大きすぎて読めませんでした」は出なくなります。
- リッチテキスト(4MB)と、コードの囲みに埋め込む添付(2MB)の上限も一緒に撤廃しました。
- 画像が埋め込まれたページは 1MB を軽く超えるので、これまでは形が失われて平文になっていました。
- 実測では 1MB で約 0.07 秒、4MB で約 0.2 秒です。とても大きいページでは一瞬待つことがあります。

### ウルトラワイドで、本文を段組みにして読めるようになりました

- 設定 → 表示 に「本文の段組み」が増えました。1 段(既定)/ 2 段 / 3 段 / 4 段 から選べます。
- 横に長いノートが新聞のように段へ流れ、右の余白まで使って読めます。
- 送りは横向きになります。マウスホイールはそのまま(縦に回して)横へ送れます。
- 画面の幅が足りないときは、自動でふつうの縦送りに戻ります。
- 表と図は段の幅まで縮みます。広く見たいときは段を減らしてください。
- 編集に入っている間は 1 段に戻ります。

### 文字の大きさを、自分で変えられるようになりました

- **設定 → 表示** に **文字の大きさ**(小 / 標準 / 大 / 特大)が増えました。押すと**その場で効きます**。
- **最初は「標準」で、これまでと同じ大きさ**です ── 選ばなければ見え方は 1 ミリも変わりません。
- **読み幅(1 行の長さ)は動きません**。大きくすると 1 行に入る字が減ります(段が広がるのではなく、字が大きくなります)。
- この端末だけの設定です。ノートの中身には入りませんが、**設定の持ち出し**には入ります。

### 読んでいる本文から編集に入る押し方が変わりました(Ctrl+クリック)

- 読んでいる本文を**その行から編集に入る**のは、**`Ctrl`(mac は `⌘`)を押しながらのクリック**になりました。⚠ リンクの上では奪いません(`Ctrl+クリック` の「新しいタブで開く」はそのままです)。
- ⚠ **これまで同じことをしていた `Alt` を押しながらのクリックは、別の操作になりました** ── **下の追記欄の「入り先」をその節にします**。見出しを一覧から探さずに、足したい場所を直接指せます。
- 押した所より**上に見出しが無い**ときは、入り先を**変えずに**理由を出します(関係のない場所へ足さないためです)。
- 編集中の 1 面では、**字を選んでいる間は押しても行が開かなくなりました** ── それまでは**ドラッグで選び終わった瞬間に選択が消えて編集に入って**いました。⚠ 選んでいても開きたいときは `Ctrl`(`⌘`)+クリックです。
- ⚠ **チェックの印と表の升は、これまでどおり押せばその場で直せます**(押すと本文が書き換わる口は変えていません)。
- **本文の下の余白**を押したときは、これまでどおり**末尾に行が開きます**(空のノートはここが書き始めです)。

## 2026-08-27

### 録音が、本文に入らないことがあったのを直しました

- 録音や画面収録を止めたとき、**まれに添付だけが作られて本文に参照が入らない**ことがありました。
- 止めた瞬間にちょうど本文を書いている最中(直前の保存や追記が飛んでいる最中)だと、そこで諦めていたためです。⚠ その状態は**一瞬しか続かない**ので、少し待てば書けるものを捨てていました。
- これからは書き終わるまで**預かって**、書けるようになったらそのまま入れます(ふつうは一瞬です)。
- 預かっている間は「書けるようになったら入れます」と画面に出ます。

### 見出しが、本文と見分けやすくなりました

- 本文の見出しに、左端の細い色の帯が付きました。段が下がるほど帯が薄くなります。
- 深い見出し(`####` 以降)が本文より小さくならないようにしました。これまでは UA 既定のまま `h5` = 0.83em / `h6` = 0.67em で、**本文より小さく**、地の文に埋もれていました。間隔の規則も `h1`〜`h4` までしか無く、深い見出しは前後の余白も失っていました。
- `#` と `##` には、下に薄い区切り線を引きます(節の切れ目)。
- **その帯が、そのまま畳むボタンです。** 乗せると帯が太くなり、押すとその節を畳めます。
- 折りたたみ記号はふだん出しません。**押し所は帯の幅だけ**なので、見出しの字を押したときは今までどおり編集に入ります(読むつもりで押して中身が消える、が起きません)。
  - ⚠ **訂正(2026-08-28)**: この行の「見出しの字を押したときは今までどおり編集に入ります」は、**読んでいる面では当てはまりません** ── そちらは `Ctrl`(`⌘`)+クリックになりました(上の 2026-08-28 の項)。編集中の 1 面では今までどおり押せば入ります(字を選んでいる間を除く)。帯が畳むボタンであることは変わりません。
- 畳んでいる間だけ `▸` が出たままになります。開き直す口が画面から消えないようにしています。

### 生成 AI の返答を、見た目のまま貼れるようになりました

- 設定 → 貼り付け → 読み取る形 に「ウェブページの形をそのまま(html の囲み)」が増えました。
- ふだんは PKC-Markdown へ戻します(見出し・箇条書き・表・コードの囲み・太字は正しく戻ります)。ですが色や段組など、markdown に無い形は落ちていました。
- これを選ぶと変換せず、html の囲みに入れて見た目のまま残します。囲みの中身は箱の中で描かれます。
- あとで直すときは HTML を触ることになるので、既定は今までどおり自動です。崩れるときだけ切り替えてください。
- 中身に囲みの記号が入っていても、囲みのほうを長くするので途中で切れません。

### ノートが、どのスマートフォルダに集まっているか分かるようになりました

- 右の情報ペインに「集まり先」が増えました。そのノートを集めている入れ物が並び、押すとそこへ移れます。
- タグの札とは別物です。タグの札はその語で探しますが、こちらは実際に並んでいる入れ物へ行きます。
- 入れ物は条件をいくつも持てるので、自分のタグを見てもどこに集まっているかは分かりませんでした。
- 一度も開いていない入れ物は出ません。その入れ物を 1 度開くと、次から出ます。
- 1 つも分かっていないときは行ごと出しません。「無し」とは書かないためです。

### 左の列の下の帯が 3 段から 2 段になり、一覧が広がりました

- 取り込む・バックアップ・操作を探す・集計・設定・フラグ・ヘルプ の帯が、幅の狭い画面で 3 段に折り返して場所を取っていました。
- 図案を出さず、字の左右の余白を詰めることで 2 段に収まるようにしました。一覧に使える高さが 27px 増えます。
- ボタンは 7 つとも残っています。減らしていません。
- 押す的が細くなりすぎないよう、幅の下限を置いています。

### 「操作を探す」の説明が、切れずに読めるようになりました

- 各行の説明が器からはみ出して、横スクロールバーが出ていました。後ろが隠れるので、押せない理由まで読めませんでした。
- 説明は折り返して出るようになりました。割り当たっている鍵は右端に並びます。
- 一覧が長いときは一覧だけが縦に送れます。探す欄と「やめる」は、送っても同じ場所に残ります。
- ノートへのリンクを入れるときの「ノートを選ぶ」も、同じ形にしました。長い題名でも後ろが隠れません。

### お知らせが 1 件ずつ出るようになりました

- これまでは未読が全部 1 つの箱に積まれ、箱の中を送って読む形でした。初めて開いた方には 10 件が積まれるので、箱が大きいのに一度に 1 件しか読めませんでした。
- これからは新しいものから 1 件ずつ出ます。見出しの「次へ」を押すと、その 1 件を読んだことにして次が出ます。
- 「閉じる」は今までどおりです。残っている分もまとめて読んだことにして、帯が消えます。
- 過去のお知らせは、これまでどおりヘルプからいつでも読めます。
- 帯の高さは 270px から 199px になりました(手元の実測、画面の高さ 900px のとき)。

### 種類の札が、押した面ですぐ応えるようになりました

- 種類の札(ノート・ログなど)を一覧以外のタブで押すと、押された印も「解除」も出ないのに、絞りだけ入っていました。一覧へ戻るとノートが減っていて、そのタブからは戻せませんでした。
- 押したその場で印が付き、「解除」も出るようになりました。
- 種類の絞りが効かないタブ(連絡先・アプリ)では、札そのものを出さなくなりました。押しても何も起きない札を置かないためです。
- 予定タブでは今までどおり出ます。そこでは実際に絞れます。

### ノートの行を右クリックすると、できることが出るようになりました

- 一覧やフォルダの行を右クリックすると、そのノートにできること(参照をコピー・素の Markdown・書き出す・履歴・削除)が、その場に出ます。
- これまでは情報ペインを開いて探す必要がありました。右クリックは近道で、これまでのボタンや帯はそのまま残っています。
- 本文のリンクや図の上では、これまでどおりブラウザのメニューが出ます(「リンクをコピー」「画像を保存」が消えないようにするためです)。
- Escape か、外を押すか、画面を送ると閉じます。閉じると、開く前に居た場所へ戻ります。
- 編集中は出ません。編集を終えてからにしてください、と画面の下に出ます。

### 左の列のタブの字が、隣に重ならなくなりました

- タブが 5 枚になってから、「フォルダ」などの字がボタンからはみ出して、隣のタブに重なっていました。字が箱に収まるようにしました。
- タブの図案は出さなくなりました。5 枚を 1 行に並べると、図案の分だけ字が入らなくなるためです。
- 狭い画面では、入りきらなくなったところで折り返します。これまでは 3 段になっていましたが、2 段までになりました。

### 連絡先のタブができました

- 左の列に「連絡先」のタブが増えました。電話やメールを書いたノートが、ここに並びます。
- ノートの先頭の囲みに tel: か email: を書くと、そのノートが連絡先になります(org: で所属も書けます)。
- 名前を押すとそのノートが開き、電話やメールを押すと端末の電話やメールに渡ります。
- 名前は題名です。name: は要りません。
- org: だけを書いたノートは並びません。連絡できるものだけが並びます。
- タブを開いたときに集めます。開かない人には走りません。

### 予定の時刻に、音で知らせられるようになりました

- 設定の「予定の知らせ」を入れると、本文の行に時刻まで書いた予定の時間になったとき、短い音が鳴って画面の下に帯が出ます。
- 対象は行に書いた予定です(- [ ] 打ち合わせ @2026-08-27 14:00 のように、日付のうしろに時刻を足します)。
- 帯の「開く」でそのノートへ移り、「閉じる」で片付きます。済んだ印を付けてある行では鳴りません。
- PKC を開いている間だけ鳴ります。閉じている間は鳴りません(ブラウザでは、閉じたページを時刻で起こすことができないためです)。
- 入れたときに 1 度だけ音が鳴ります。どんな音か分かるのと、その端末で音を出せるかがその場で分かるためです。
- 最初は切です。入れると、起動したときに予定を数えます。

### 作業時間を計って、ノートに残せるようになりました

- 左の列の「画面」の隣に「計る」が増えました。押すと、いま開いているノートの作業時間を計り始めます。
- 計っている間は画面の下に帯が出て、何を計っているかと経った時間が出ます。別のノートを開いて押せば、何本でも同時に計れます。
- 「止める」を押すと、そのノートの本文の末尾に「作業 2026-08-27 06:40–07:03(23:11)」の 1 行が入ります。「捨てる」なら何も残しません。
- 別のタブを見ている間も時間は正しく進みます(刻みを数えず、始めた時刻との差で出しています)。
- 編集している最中に止めたときは、その瞬間の時間で預かって、編集を終えると本文に入れます。
- ページを読み直すと計測は消えます。開いている間だけ動く道具です。

### スマートフォルダに期日と状態が書けるようになりました

- スマートフォルダの本文の先頭に date: と status: を書くと、予定の面に出るようになりました。
- 「請求」のような集まりそのものに、締め切りや進み具合を持たせられます。
- 雛形(スニペット)は、これまでどおり予定の面には出ません。
- これまでは書いても列に入らず、予定の面に出ませんでした。出ない理由もどこにも出ませんでした。
- archived: はこれまでどおりです ── 書いてもノートは一覧から消えません(片付けるのは todo だけです)。

### 本文の音・動画が、その場で聞けるようになりました

- 本文に貼った添付への参照が、音や動画なら、その場に再生の器が出るようになりました。押すとそのまま聞けます。
- 録音や画面収録で入った参照にも、手で添付した音や動画にも効きます。
- 名前のリンクはそのまま残ります。押せば、これまでどおり保存できます。
- 書き出した閲覧用の HTML でも同じように聞けます。渡した相手が添付を持っていなくてかまいません。
- 中身は開くまで読みません。長い収録があるノートを開いても、そのぶん待たされることはありません。
- 印刷には出ません。名前のリンクは紙にも残ります。

### 録音と画面収録ができるようになりました

- 左の列の「添付」の隣に「録音」と「画面」が増えました。押すと、マイクや画面の共有をブラウザが確認します。
- 収録している間は画面の下に帯が出て、経った時間とだいたいの大きさが出ます。「止める」で終わり、「捨てる」なら何も残しません。
- 止めると添付になって、そのとき開いていたノートの本文にその添付への参照が入ります。開いていたノートはそのまま開いたままです。
- 本文に入るのは参照です。押すと保存できます。その場で聞く・見るときは、その添付のノートを開いてください。
- ブラウザの側で「共有を停止」したときと、250MB まで録って自動で止まったときも、そこまでの分は残ります。編集している最中に止まったときは、編集を終えるまで預かって、終わったら入れます。
- ノートを開いていないときや、追記できない種類を開いているときは、添付にするだけで本文には入れません。編集している最中は「止める」を押しても断ります(収録は続きます)。

### 表の升に式が書けるようになりました

- 升に = で始めて打つと計算します。=A1*B1 や =SUM(D2:D3) のように書けます。升には結果が出て、押すと式が出ます。
- 使える関数は SUM / AVERAGE / MIN / MAX / COUNT / IF / ABS / ROUND / CONCAT / LEN の 10 個です。
- 升の名前は A1 が左上です。見出しの行も 1 行目に数えます。
- 書いたものは本文に残ります。計算するのは表示するときだけなので、式が結果に置き換わって消えることはありません。
- = を字として書きたいときは、先頭にアポストロフィを付けてください。
- 読めなかったときは、その升に理由が出ます。0 で割った / 使えない関数名 / ぐるぐる参照している、などです。

### 表の升を押して、そのまま打てるようになりました

- 描かれた表の升を押すと、その升が入力欄になります。周りは表のままです。Enter で確定、Escape で取り消しです。
- CSV の原文を開いて、どのカンマが 1 列目かを数える必要はなくなりました。
- 行の左端の升にマウスを乗せると ＋ × が出ます。行を足したり消したりできます。
- 列は見出しの升に ＋ × が出ます。見出しの無い表では 1 行目に出ます。
- 最後の 1 行と 1 列は消せません。消すと表そのものが消えて、原文に戻ることになるためです。
- 押せるのは読む面だけです。書き出した HTML や印刷では、これまでどおり読むだけです。

### 書き出したファイルにも、囲みの中身が入るようになりました

- 閲覧用 HTML・Word・PowerPoint に書き出すとき、囲みが指している添付をその場で読んで焼き込みます。渡した相手が添付を持っていなくても、本文に書いたのと同じに見えます。
- 前のお知らせで「書き出しにはまだ中身が入りません」とお伝えしましたが、入るようになりました。
- 書き出すときに読めなかった添付だけは、その場に理由が残ります。黙って空にはしません。
- Markdown の ZIP はこれまでどおり原文のままです。asset: の行と添付のファイルが別々に入ります。
- 囲みの見出しに言語を書かず asset:添付の鍵 だけを書いても効くようになりました。

### 囲みの中身を、添付から取れるようになりました

- 囲みの見出しに asset:添付の鍵 と書くと、中身をその添付から読みます。5,000 行の CSV を表で見たり、大きい図の原文を本文から出したりできます。
- 言語は選びません。表も、図も、グラフも、HTML も、ふつうのコードの囲みも同じに効きます。
- 鍵は、その添付を開いて「参照をコピー」で手に入ります。asset: から後ろを使ってください。
- 読めなかったときは、その場に理由が出ます。見つからない / 大きすぎる(上限 2.0MB)/ 書き方が使えない、のどれかです。
- 囲みの中に字も書いておくと、読めなかったときにその字が出ます。
- 書き出した HTML や持ち歩ける 1 枚には、まだ中身が入りません。代わりにどの添付を指しているかが出ます。

## 2026-08-26

### 本文の外部の画像を、手元に取り込めるようになりました

- ウェブページから貼った画像は相手のサイトを指したままなので、そこから消えると見えなくなります。右の列の「外部の画像を取り込む」を押すと、このノートの添付になります。
- ボタンには枚数が出ます。押すとその枚数ぶん、画像の置き場所へ通信します。だから自動ではやりません。
- 本文に外部の画像が無いノートでは、ボタンごと出ません。
- 読めなかったものは元の URL のまま残り、理由が画面の下に出ます。置き場所が 404 を返した / 相手が読み取りを許していない、などです。
- 同じ URL をリンクとしても書いている場合、リンクのほうは変わりません。
- 貼り付けの断りも、理由を言うようにしました。これまでは読めていても「読み込めませんでした」と出ることがありました。

### 設定だけを別の端末へ持っていけるようになりました

- 設定 → 書き出しと片づけ →「設定を書き出す」で、見た目・鍵の割当・紙面などが 1 つのファイルになります。ノートは入りません。
- 別の端末で「設定を読み込む」から選ぶと、当てる前に何が変わるかが並びます。よければ「設定を当てる」を押してください。
- 許可(外部の画像・拡張・埋め込み)とフラグとお知らせの既読は運びません。その端末で許したものなので、運ぶと意味が変わるためです。
- 2 ペインで留めた場所も運びません。別の端末では別のノートを指してしまうためです。
- 入れたあとは、いちど読み直してください。鍵の割当も紙面も起動のときに読みます。

### 書式を「操作を探す」から入れられるようになりました

- 編集中に入れたい所を選んでから Ctrl+Shift+P を押し、「ルビ」などと打って Enter を押すと、選んだ範囲に入ります。
- これまでは一覧に出ても「いまは押せません」でした。鍵を覚えていなくても入れられます。
- ハイライト / ルビ / 圏点 / 打ち消しは書式の帯に出していないので、ここが押して入れる唯一の口です。
- 太字 / 斜体 / リンクも同じように入れられます。
- 待っている間に画面が組み直されて欄が変わったときは、入れずに理由を出します。選んでいない所に書き込まないためです。

### スマートフォルダで「まだ終わっていない仕事」を集められるようになりました

- 条件の帯に「チェック」と「未処理」が増えました。「未処理がある」を選ぶと、やり残しのあるノートだけが集まります。
- 「全部済んでいる」を選べば、片づいたノートだけを集められます。
- 中身は開くたびに数え直すので、チェックを付け外しした分はそのまま出ます。
- 条件は本文に書かれるので、書き出しにも別の端末にも一緒に付いていきます。

### スマートフォルダを、語でも絞れるようになりました

- 条件の帯に「語」の欄が増えました。打って Enter を押すと、題名か本文にその語があるノートだけが集まります。
- 探す欄と同じ探し方です。探して出てくるものは、ここでも集まります。
- 外すときは、欄を空にして Enter を押してください。
- 落として入れられるのは、条件にタグがあるときだけです。種類や更新日や語しか条件に無い入れ物へ落とすと、理由が出るようになりました。
- これまでは、そのとき何も起きず、理由も出ませんでした。条件にタグを 1 つ足せば、落として入れられます。

### 何が容量を食っているかが分かるようになりました

- 設定 → 書き出しと片づけ →「何が容量を食っているか」の「調べる」を押すと、添付の重い順にノートが並びます。
- 行を押すと、そのノートへ飛べます。1 件ずつ開いて確かめる必要がなくなりました。
- 「(共有)」と付いた行は、同じ添付を別のノートとも使っています。片方を消しても容量は減りません。
- どこからも使われていない添付が残っていれば、その合計も出ます。「使っていない添付を消す」で片づけられます。
- ブラウザが言う使用量とは数え方が違います。ここが数えるのは添付の合計だけです。

### AI に考えてもらった整理案を、まとめて当てられるようになりました

- 設定 → 書き出しと片づけ →「構成をコピー」で出した内容を AI に渡し、返ってきた案を同じ画面の「整理案を当てる」に貼ってください。
- 貼るとすぐ下に「何が起きるか」が並びます。ノートの題名で「どれを、どこへ」と書くので、当てる前に確かめられます。
- 間違いがあれば何行目がなぜ駄目かを出します。間違いが 1 行でもあると「当てる」は押せません。
- 新しいフォルダを作って、そこへまとめて移す、までを 1 回の案で書けます。
- 取り消しはありません。当てる前の下見が、その代わりです。

### 手元の Word / Excel / PowerPoint を、そのまま開いて直せるようになりました

- PKC3 をアプリとして入れていると、報告書.docx などをダブルクリックしたときに PKC3 で開けるようになります。
- 開くには、起動したあとアプリの一覧から「Office」を押してください。押したファイル名が画面の下に出ています。
- 保存すると、PKC3 のノートではなく元のファイルへ書き戻します。取り込んで書き出して置き直す、という往復が要りません。
- 元のファイルへ書き戻せない場合は、開く前にその旨をお知らせします。黙って行方不明にはしません。
- ファイルとのつながりは、その起動の間だけ覚えます。読み直すと切れるので、もう一度開いてください。

### サイドバーで、種類を選んで絞れるようになりました

- 探す欄の下に、いま入っている種類が件数つきで並びます。押すとその種類だけになり、もう一度押すと戻ります。
- 添付だけを見たい、フォルダだけを見たい、というときに題名で探さなくて済みます。
- 何も選んでいないときは全部出ます。絞っている間は右端に「解除」が出るので、そこから戻れます。
- 絞ったまま新しいノートを作ると、絞りは自動で外れます。作った物が一覧から消えないようにするためです。
- 一覧・フォルダ・予定・2 つ並べた面のどれでも同じように効きます。

### 書きながら、別のノートへのリンクを選んで入れられるようになりました

- 本文を書いている場所の上にある「ノート」を押すと、題名で探して選べます。選ぶと今いる場所にリンクが入ります。
- Ctrl+Shift+K（Mac は ⌘+Shift+K）でも開きます。開いたらそのまま題名を打って、Enter でいちばん上が入ります。
- これまでは相手のノートを開きに行って「参照をコピー」してから戻る必要があり、書いている手が止まっていました。
- 書いている自分自身は候補に出ません。ごみ箱に入れたノートも出ません。

### 大きな写真を添付すると「縮めますか」と聞くようになりました

- 長辺が 2048px を超える 1.5MB 以上の画像を添付すると、縮めるかどうかを聞きます。携帯の写真は 1 枚 5〜12MB あるので、数枚で器を圧迫していました。
- 数字は見積もりではありません。先に縮めてから、実際にできた画素数と大きさをお見せします。
- 勝手には縮めません。「そのまま」を選べば原寸で入ります。縮めると元の細かさは戻らないためです。
- 形式は変わりません（PNG は PNG のまま）。SVG と GIF、小さい画像、もともと 2048px 以下の画像は触りません。
- 縮める処理はワーカーで動くので、待っている間も画面は固まりません。

### ノートの並びを、AI に相談できる形でコピーできるようになりました

- 設定 → 書き出しと片づけ →「構成をコピー」を押すと、いまのノートとフォルダの並びが、整理コマンドの書き方つきでクリップボードに入ります。
- AI に貼って「この構成を整理する案を考えて」と頼めます。ノートが増えて 2 ペインで 1 件ずつ運ぶのが辛くなったときに使ってください。
- いまは案をもらうところまでです。返ってきた案をそのまま一括で適用する口は、まだありません。
- ノートが非常に多いときは上から 2000 件までにします。切ったときは、その旨がコピーした紙の中に書かれます。

### ハイライト・ルビ・圏点・打ち消しを、鍵で入れられるようになりました

- 書けはしたのに、押して入れる口がありませんでした。書式の帯は横に長いのでボタンは増やさず、近道キーを用意しました。
- Alt+Shift+H = ハイライト、Alt+Shift+R = ルビ、Alt+Shift+E = 圏点、Alt+Shift+X = 打ち消しです。選んでから押してください。
- もう一度押すと外れます（太字と同じです）。
- ルビは押すと読みの位置にカーソルが入るので、そのまま読みを打てます。
- 鍵は設定のショートカットで好きなものに変えられます。名前を忘れたら「操作を探す」で「ルビ」と打つと出ます。

### 操作を名前で探せるようになりました

- 左下の「操作を探す」を押すか、Ctrl+Shift+P（Mac は ⌘+Shift+P）で開きます。やりたいことの名前を打つと絞り込まれ、Enter でいちばん上が走ります。
- 説明の中の言葉でも当たります。名前をうろ覚えでも、たどり着けます。
- いま押せない操作も隠さずに出して、なぜ押せないかを並べます（「ノートを選んでいるときだけ効きます」など）。
- 各行には割り当たっている鍵も出るので、次からはその鍵で直接呼べます。
- 編集の途中でも開けます。この鍵は文字を打たないので、本文に記号は入りません。

### ノートから別のノートへ、リンクを張れるようになりました

- リンクしたいノートを開いて、右の列の「参照をコピー」を押してください。書いている本文に貼るだけでリンクになります。
- これまでは書き方（entry: の形）を知っていても、貼る材料を画面から手に入れる方法がありませんでした。
- 題名は貼った時のものが残ります。後でそのノートの題名を変えても貼った字は変わりません（リンク先は変わりません）。
- 添付の「参照をコピー」と、ノートの「参照をコピー」は、押すと短く光るようになりました。押せたかどうかが分かります。

### スマートフォルダが、タグ以外でも絞れるようになりました

- 条件の帯に「種類」「更新」「作成」「日付」の選び口が増えました。選んだ瞬間に効きます。
- 種類はノート・フォルダ・添付・表など、更新と作成は 7 日 / 30 日 / 90 日 / 1 年以内から選べます。
- タグと合わせて全部 AND です。たとえば「種類が添付」+「更新が 30 日以内」なら、その両方に当てはまるものだけが集まります。
- 「日付」が見るのは、ノートの先頭に書いた date: だけです。本文の行に書いた @2026-08-25 は、この条件では集まりません（予定のタブには出ます）。
- 日数は好きな数にできます。ノートの先頭に smart-updated: 45d のように書けば、その日数で効きます。

### 表や箇条書きの 1 行を直すとき、まわりが消えなくなりました

- 1 面で編集しているとき、表の行や箇条書きの項目を押すと、表や箇条書きが丸ごと画面から消えて、1 行の入力欄だけになっていました。
- 押した行(項目)だけが原文になり、まわりの行は描かれたまま並びます。どの行を直しているのかが見えます。
- 直す範囲はこれまでどおり押した 1 行だけです。まわりの行は変わりません。

### スマートフォルダ ── 条件に合うノートが自動で集まります

- 「新規」の種類に スマート が増えました。作ってフォルダタブで 2 回押すと中へ入れます。
- 一覧の上の条件の帯にタグを入れて「条件に足す」を押すと、そのタグが付いたノートが、どのフォルダに在っても集まります(ノートは動きません)。
- タグは何個でも付きます。2026年 8月 26日 の 3 つを付けておけば、3 つ全部を条件にした入れ物にも、2026年 だけの入れ物にも、同じノートが並びます。
- 後からタグを付け外しすると、開いたままの入れ物がその場で変わります(開き直す必要はありません)。
- ノートを掴んで行に落とすと条件のタグが付き、中で選んで「ここから外す」を押すと外れます。条件が空のうちは何も集めません。
- 条件は本文の先頭に smart-tags: の形で書かれます。書き出しや取り込みに一緒に付いていきます。

## 2026-08-25

### 2 ペインで整理するとき、場所を扱う道具が増えました

- 現在地の左に「◀ ▶」が付きました。そのタブが 1 つ前に見ていた場所へ戻れます(Alt + ← / Alt + →)。
- 現在地の右に「名前で絞る」欄が付きました。左右のペインで別々の語を打てます。欄で ↓ を押すと、そのまま行へ降りられます。
- その右の「☆」で、いまの場所を留められます。留めた場所は現在地の下に並び、押すとそこへ移ります(×で外せます)。
- 下の操作行に「F9 下見」が増えました。押すと、指している行の中身がペインの下に数行出ます。
- 件数の行に本文の文字数の合計が出るようになりました。「選んだぶん / 全体」です。
- 留めた場所と下見の出し入れは、この端末に憶えます(ノートの中には入りません)。

### リッチテキストで貼っても、書式もコードも残るようになりました

- 生成 AI チャットの回答や、書式付きの文書アプリからコピーした文字を本文に貼ると、見出し・箇条書き・表・リンク・太字・コードが記法に戻って入ります。
- これまでは、ウェブページの形を渡してこない相手から貼るとただの文字に潰れていました。生成 AI チャットにもそういう出し方が増えています。
- コードは囲みになります。コピー元が「ここはコード」と印を付けている行と、等幅で書かれている行が対象で、続いた行は 1 つの囲みにまとまります。文の途中なら行内コードになります。
- 貼り込まれた PNG / JPEG の画像は、添付として取り込みます。下線は :文字:underline: の形で入ります。
- コードの言語だけは入りません。リッチテキストが言語を持たないためです(ウェブページの形も渡してくる相手なら、そちらから入ります)。
- うまく入らないときは、設定の「貼り付け」で読み取る形を切り替えられます。フラグを点けると、何が届いてどれを使ったかも見えます(中身は出しません)。

### PKC3 ごと 1 つのファイルに入れて、持ち歩けるようになりました

- 設定の「書き出しと片づけ」に「持ち歩ける 1 枚」が増えました。アプリと、いまの中身と、添付が 1 つの .html になります。
- USB に入れて別のパソコンへ持っていき、ダブルクリックで開けます。サーバもインターネットも要りません。
- その 1 枚の中で、そのまま書けます。閲覧用 HTML は読むだけですが、こちらは続きを書ける PKC3 です。
- 書いた内容は、開いたパソコンのブラウザの中に残ります。同じ 1 枚をもう一度開けば続きが出ます。
- 1 枚ごとに別の入れ物です。2 枚に書いても中身は混ざりませんが、片方に書いたことはもう片方には出ません。
- 配られた 1 枚から、さらに持ち歩ける 1 枚は書き出せません。押すとその旨が出ます。

### アプリへ渡したノートを、アプリから書き戻せるようになりました

- ノートを選んで、右の情報ペインの「このアプリへ送る」で 1 件だけ渡せます。渡るのは押した 1 件の本文だけです。
- 渡したノートは、そのアプリが直して返せます。フォームや表のアプリで直した結果が本文に入ります。
- 書き戻せるのは、あなたが渡した物だけです。渡していないノートは、アプリが名前を知っていても書けません。
- 1 件でもおかしな依頼が混ざっていたら全部断ります。半分だけ書かれることはありません。
- 書き戻しは履歴に残るので、いつでも戻せます。PKC3 で編集中は書き戻せません。

### 面をまたいで作業が続くようになりました

- フォルダタブで 2 件以上選ぶと、帯からまとめてタグを付けられます。外すのも同じ帯からできます。
- 結果は「3 件に付けました / 1 件は既に付いていました」と 1 通で出ます。既に付いているものは書き直しません。
- 予定タブの上に「やること」の欄が増えました。打って足すと、今日のノートの末尾にチェック項目として書かれます。
- 日付の束の見出しの + を押すと、その日が日付欄に入ります。画面は切り替わらないので、予定を眺めたまま足せます。
- 左の列で掴んだまま、上のタブの上で少し止めると面が変わります。手を離さずにフォルダの行を予定の日へ持っていけます。

### 履歴で、戻す前に中身を見られるようになりました

- これまで履歴に押せるのは「復元」だけで、どれが目当ての版か押すまで分かりませんでした。題名は普通どの版も同じです。
- 行に「+12 −3」と、1 つ新しい版とのちがいが出ます。まるごと持っている版は数が出ません(0 とは書きません)。
- 行そのものを押すと、その版といまの本文のちがいが下に開きます。読むだけで、1 バイトも書きません。
- 変わっていない所は「⋯ 変わっていない N 行」と畳まれるので、長いノートでもちがいが埋もれません。
- もう一度押すか「閉じる」で畳みます。履歴を閉じると一緒に畳まれます。

### 追記を、節の中へ入れられるようになりました

- 本文の下の追記欄に「入り先」が増えました。既定は末尾で、これまでと同じです。
- 開くとそのノートの見出しが並びます。選ぶと、その見出しの節の終わりに入ります ── 本文を開かずに書けます。
- 選んだ入り先は、続けて追記してもそのままです。同じ節へ何度でも足せます。
- 追記が通ると「元に戻す」が出ます。直前に足した内容だけを、本文を開かずに取り除けます。
- 押すまでの間にその見出しが消えていたら、足さずに理由を出します。黙って末尾へ入ることはありません。

### 本文を Alt クリックすると、その行から編集に入ります

- 読んでいて「この 1 行だけ直したい」というときの近道です。押した行が開いた状態で編集に入ります。
- これまでは「編集」を押してから、もう一度その行を探して押す必要がありました。
- 素のクリックはこれまでどおり何も起きません。リンクやボタンの上でも効きません。
  - ⚠ **訂正(2026-08-28)**: この項目の **`Alt` クリック**は、いまは **`Ctrl`(mac は `⌘`)クリック**です。`Alt` クリックは**追記の入り先を指す**操作になりました(上の 2026-08-28 の項)。

### フォルダごと書き出せるようになりました

- これまでは全部か 1 件かしか選べませんでした。フォルダを選ぶと、右の情報ペインに「フォルダを書き出す」が出ます。
- そのフォルダと、中に入っているものが入れ子の奥まで入ります。フォルダの器も入るので、取り込み直すとフォルダごと戻ります。
- 添付は、中のノートが使っている物だけ入ります。外へ繋がる関係は入りません(何件落ちたかは書き出した後の帯に出ます)。
- バックアップと同じ形式なので、そのまま取り込み直せます。

### 同じものを 2 回取り込んだことが分かるようになりました

- 同じバックアップをもう一度取り込むと、これまでは何も言われずに全部が 2 部になっていました。
- 取り込んだ後の注意に「同じ内容のノートが N 件ありました」と題名つきで出ます。
- 取り込みは止めません。これまでどおり両方残るので、要らないほうを消してください。

### 書くときの手つきが 4 つ増えました

- 見出しを押すと、その下をまとめて畳めます。畳んだままでも、見えている行はそのまま直せます。
- 引用の行末で Enter を押すと、次の行も引用のまま続きます。空の引用行で Enter を押すと抜けます。
- 番号つきの箇条書きがずれたときは、書式の帯の「番号」で振り直せます。押したときだけ動きます。
- 右の情報ペインの「素の Markdown」で、PKC3 の書き方を普通の Markdown に直して写せます。他の道具に貼るためのものです。

### アプリにノートの目次を見せられるようになりました

- 取り込んだ HTML のアプリは、これまで PKC3 の中を一切見られませんでした。許可すると、ノートの一覧を読んで働けるようになります。
- 許し方は、添付を選んで「目次を見せて起動」を押すだけです。何が見えるかを確認してから開きます。
- アプリに見えるのは、ノートの題名・種類・日付・印の一覧だけです。本文と添付は渡りません。
- 一度許すと、次からは普通の「起動」でも目次が見えます。中身が変わったらまた聞きます。
- 許したアプリは、設定の「目次を見せているアプリ」に並びます。いつでも取り消せます。

### 雛形を一覧から選んで入れられるようになりました

- 短縮語を覚えていなくても雛形を呼べます。本文を書いている途中で、書式の帯の「雛形」を押してください(Ctrl と / でも開きます)。
- 一覧には、PKC3 にもとから入っている雛形(表・図・コード塊・リンク)も並びます。雛形をまだ作っていなくても空にはなりません。
- 短縮語を書いてある雛形は「題名(短縮語)」と出ます。ここで覚えれば、次からは打って Tab で入れられます。

### 自分の雛形を作って、短縮語で呼び出せるようになりました

- 左上の作成ボタンから「雛形」を選ぶと、雛形のノートが作れます。よく打つ住所・署名・議事録の型などを入れておいてください。
- 雛形の先頭に abbr: addr のように短縮語を書いておくと、本文を書いている途中で addr と打って Tab を押すだけで中身が入ります。
- 雛形の中に ${宛名} のように書いた所は、挿したあと選ばれた状態になります。打てばそのまま置き換わり、Tab で次の場所へ移れます。
- ${date} は挿した日、${time} は時刻、${datetime} は両方、${cursor} は挿したあとカーソルが来る場所になります。
- 短縮語が当たらないときの Tab は、これまでどおり次の欄へ移ります。書きかけの邪魔はしません。
- ⚠ 雛形の外(普通のノート)に ${date} と書いても、これまでどおりそのまま表示されます。展開されるのは雛形を挿すときだけです。

### 毎日・毎週・毎月・毎年の予定が書けるようになりました

- 日付のうしろに 毎日 / 毎週 / 毎月 / 毎年 と書くと、予定タブのその先の日にも札が出ます。例:- [ ] ゴミ出し @2026-08-31 毎週。曜日は書かなくてかまいません(開始した日の曜日になります)。
- いつまで繰り返すかは、これまでの期間の書き方をそのまま使います。@2026-08-31..2026-12-31 毎週 と書くと 12/31 までです。
- その日のぶんが済んだら、札の印を押してください。規則の行はそのままで、その日ぶんの行が本文に増えます。押した回だけが済んだことになるので、次の回はそのまま出ます。
- 繰り返しの札は掴んで日を動かせません。落とすと理由が画面の下に出ます。日を変えるときは本文の @日付 毎週 を書き直してください。
- 過ぎた回は出ません。先のぶんは 2 か月ぶんまで出ます。詳しくはヘルプのマニュアル、予定の「繰り返す予定」に書いてあります。
- ⚠ 日付のすぐ後ろに 毎週 などと書いてある本文は、これから予定として読まれます。そのつもりでない行は、日付から離して書いてください。

### つながりの図に、本文のリンクも出るようになりました

- 本文に書いたノートへのリンクが、つながりの図に破線で出ます。相手から自分への参照も同じ破線で出ます。
- 破線は真ん中のノートの分だけです。実線の関係のほうは、これまでどおり 2 手先まで出ます。

### つながりが図で見えるようになりました

- 右の情報ペインの関係の下に「つながり」の欄が出ます。いま選んでいるノートを真ん中に、つながっている相手が円く並びます。
- 相手を押すと、そのノートへ移れます。出るのは 2 手先(相手の相手)までです。
- 下に線の種類と本数が出ます。つながりが多いときは途中で切り、切ったことを書きます。
- つながりが 1 つも無いノートでは、この欄ごと出ません。居場所(フォルダの親子)は図に出しません。

### 見ている頁を、ブックマーク 1 つで取り込めるようになりました

- 読んでいる記事をブックマークバーのボタンで PKC に取り込めます。題名と、選んでおいた部分と、出典の URL が入ります。
- はじめに左の列の「フラグ」で「ブックマークで見ている頁を取り込めるようにする」を入にして、再起動してください。既定では取り込みません。
- ブックマークの作り方はヘルプのマニュアル、取り込むの中に書いてあります。
- 取り込んだものは編集の形で出るので、見てから保存できます。要らなければ捨ててください。元の記事のタブは退きません。

## 2026-08-24

### Word や Excel の形式のまま、Office から保存できるようになりました

- Office で Word や Excel の形式を編集して保存を押すと「一般的な I/O エラー」で失敗し、編集した内容はそこで失われていました。保存できるようになりました。
- .docx .xlsx .pptx と、古い .doc .xls .ppt、それに .rtf の 7 つで確かめました。ODF(.odt .ods .odp .odg)はこれまでどおりです。
- 保存を押すと、その形式のままにするか ODF にするかを Office が訊いてきます。どちらかを選ぶと書き込まれます。
- Office のひとそろいを入れ直すと効きます。設定 → Office 表示 の「取得して入れる」を押してください。
- 入れ直すまでは、これまでどおり開いた時点で「保存できません」の断りが出ます。

### 2 ペインで整理しながら、その場でノートを作れます

- 2 ペインで整理の画面に「ノート」が増えました。いま開いている場所に、新しいノートを作ります。鍵は Shift + F4 です。
- これまではフォルダしか作れず、1 枚メモを置くために左の一覧へ戻って作り、開き直して移す必要がありました。
- 作っても本文の面へは移りません。その場に出るので、整理を続けられます。名前は F2 でその場で打ち替えられます。

### 何日かにまたがる予定を書けるようになりました

- チェックリストの行に、日付を 2 つ .. でつないで書きます。たとえば 大阪出張 @2026-08-25..2026-08-28 です。
- その予定は、8/25 から 8/28 まで 4 日すべての束に出ます。これまでは開始の日にしか出ず、途中の日を見に来ても載っていませんでした。
- 札を掴んで別の日へ落とすと、期間ごとずれます。掴んだ日が落とした日に来るので、長さは変わりません。
- 札の右に、いつまでの予定かが出ます。日付なしへ落とすと、期間ごと外れます。
- 区切りは 〜 でも書けます。期間には時刻を付けられません。書いた時刻はそのまま行に残ります。

### Markdown の書き出しが、何のための形か分かるようになりました

- 設定の「Markdown」は、1 ノート = 1 つの .md にして zip で保存します。これまで画面には「Markdown ファイルとして保存します」としか出ておらず、何に使えるかが分かりませんでした。
- 展開したフォルダは、Pandoc など他の道具にそのまま渡せます。EPUB や LaTeX など、アプリの中に口が無い形式はこの道です。
- Word や PowerPoint や PDF は、これまでどおりアプリの中の口から出せます。そちらは値の置き換えや改頁も効きます。
- 書き出しの中身は前と同じです。変わったのは画面の説明とマニュアルだけです。

### ノートへのリンクを貼ると、押せるリンクになります

- ノートの場所を指す文字をコピーして本文に貼ると、これまでは黒い文字のまま残っていました。
- 貼った時点で、そのノートの題名が付いたリンクになります。押すとそのノートが開きます。
- 別の入れ物のノート宛と、こちらに無いノート宛は、これまでどおりそのままです。在りもしない題名を書かないためです。

### ノートを PowerPoint(.pptx)で書き出せるようになりました

- 右の情報ペインに「PowerPoint」が増えました。いま選んでいるノート 1 件が、そのままスライドになります。
- 大見出しが扉のスライド、中見出しと小見出し、それに区切り線でスライドが切れます。PKC2 と同じ切れ方です。
- 表は格子のまま、画像と図は縦横比そのままで入ります。入りきらない文字は PowerPoint 側が縮めます。
- 書き出した file は取り込み直せません。戻せる形が要るときは、これまでどおり「バックアップ」を使ってください。

## 2026-08-23

### Office で保存できない形式を、開いた時点でお知らせします

- Word や Excel の形式(.docx など)を Office で開いて編集し、保存を押すと「一般的な I/O エラー」で失敗していました。編集した内容はそこで失われます。
- 開いた時点で画面の下に断りを出し、上の帯にも「保存できません」と印を付けるようにしました。押してから気づくことがなくなります。
- ODF 形式(.odt / .ods / .odp)はこれまでどおり保存できます。実際に測って、保存できる形式だけを「できる」と言っています。

### Office で表示言語を変えたとき、開き直す道ができました

- Office の中で表示言語を変えると「すぐに再起動」というボタンが出ますが、押しても何も起きませんでした。ブラウザの中の Office は自分では再起動できないためです。
- 設定が変わったことをこちらで見つけて、画面の下に「開き直す」を出すようにしました。押すと窓を開き直し、変えた言語で立ち上がります。
- 保存していない文書があると、開き直したときに失われます。帯にもそう書いてあるので、先に保存してから押してください。

### 右の列に「参照元」が増えました

- これまで、あるノートへリンクを張ることはできても、逆から辿ることはできませんでした。書けば書くほど、書いたことが見つからなくなります。
- 右の情報ペインに「参照元」が増えました。本文からこのノートへリンクしているノートが並び、押すとそこへ移れます。
- 上の「関係」とは別物です。あちらは手で張った関係、こちらは本文のリンクから自動で拾ったものなので、消すボタンはありません。
- 本文は出たままです。変わるのは右の列だけです。

### 左の列に「今日」が増えました

- 思いついたことを書きとめたいときに、まず「どこに書くか」を決めるのが面倒でした。
- 「+ ノート」の隣に「今日」が増えました。押すと、今日の日付を題名にしたノートが開きます。まだ無い日は、その場で作ります。
- その日に何度押しても、開くのは同じ 1 件です。ただのノートなので、いつもどおり書けます。
- 予定の面には出ません。予定にしたいときは、これまでどおり本文に @2026-08-25 のように書いてください。

### 設定に「このアプリのデータ」が増えました

- ブラウザは、端末の空き容量が足りなくなったときに、アプリのデータを消すことがあります。これまでは、そうなっているかどうかを知る手立てがありませんでした。
- 設定の「表示」に「このアプリのデータ」が増えました。消さない扱いになっているかが 1 行で出ます。
- 消すことがある状態のときは、次にできることも書いてあります。このアプリをホーム画面（デスクトップ）に追加すると、消さない扱いになることがあります。
- 消さない扱いは、最初に何かを保存したときに頼みます。起動しただけでは頼みません。

### 右の列に「PDF」が増えました(紙に出す)

- ノートを選んだときに右へ出る操作に「PDF」が増えました。押すと、そのノートを紙の形に組み直して、ブラウザの印刷画面が開きます。
- そこで送り先に「PDF として保存」を選べば PDF になります。プリンタを選べばそのまま印刷できます。
- こちらからファイルは落ちません ── 保存するかどうかは印刷画面で決めます。
- 紙の大きさは設定で変えられます(はじめは A4 縦です)。本文に +++ と書いた所で改ページします。
- 一覧や右の列は紙には出ません。別の面を見ているときに押すと、そのノートを開いてから刷ります。編集中は押せません。

### 別の窓と重なったとき、板のチェックと設定が本文を消さなくなりました

- 予定の面でチェックを裏返したり、アプリの設定を変えたりする操作は、本文を読んでから書き戻しています。読んでから書くまでの間に別の窓が同じノートを書いていると、そちらの本文が消えていました。
- しかも消えた本文は履歴にも残りませんでした。どこからも戻せない形です。
- これからは、読んだときと中身が違っていれば 1 バイトも書かず、理由を画面に出します。もう一度押せば、新しい本文の上に反映されます。

### 左の列に「予定」が増えました(本文は消えません)

- 左の列のタブに「予定」が増えました。日付を書いた項目が、日ごとに並びます。上の小さな月で別の月へ移れます。
- 本文は出たままです。アプリの「カレンダー」と「やることの板」は畳んで、この「予定」にまとめました ── 予定を見るために真ん中を明け渡す必要はなくなりました。
- 札を掴んで日へ落とすと、その予定が動きます。落とし先は小さな月の升目でも、下の日の見出しでもかまいません。本文の日付だけが書き替わります。
- 「日付なし」の見出しへ落とすと、予定から外れます。項目そのものは消えません。
- 今日より前の予定は、いちばん上に出ます。済んだ予定は畳まれますが、「済んだ予定も出す」で戻せます。
- アドレスに #pkc?view=calendar や #pkc?view=kanban と書いたブックマークは、そのまま使えます。開くと「予定」が出て、どこへ移ったかを画面の下に出します。


### やることの板に出るのは、日付を書いた項目だけになりました

- これまでは、本文に書いたチェックリストが 1 行残らず板に出ていました。文章の体裁として使ったチェックリストまで並ぶので、読みにくくなっていました。
- これからは、日付を書いた行だけが出ます。書き方は「- [ ] 見積を送る @2026-08-25」です。時刻も書けます(@2026-08-25 14:00)。
- 日付を書いていない項目も見たいときは、板の上の「日付のない項目も出す」を押してください。何件あるかもそこに出ます。
- 日付は手で打たなくてかまいません。編集中に書式の帯の「日付」を押すと、今日・明日・今週末・来週から選べます(Ctrl と ; でも開きます)。

## 2026-08-22

### 別の窓の変更と重なったとき、黙って上書きしなくなりました

- 本文を書いている間に、別の窓(カレンダーややることの板)が同じノートを書き替えることがあります。これまでは保存すると黙って上書きし、画面には何も出ませんでした。
- これからは、重なったときだけ画面の下に理由が出ます。こちらで打った字はそのまま保存されます。
- 上書きされた別の窓の版は履歴に残します。右の情報から履歴を開くと、その版に戻せます。

### カレンダー・やることの板・2 ペインが、別の窓で開くようになりました

- アプリの一覧からこの 3 つを押すと、別の窓で開きます。読んでいた本文はそのまま残ります。これまでは真ん中が入れ替わり、本文が消えていました。
- 読んでいたノートは連れて行きます。別の窓のカレンダーでも同じノートが選ばれているので、そのまま日付を付けられます。
- 帰り道は面の上の「× 閉じる」です。別の窓なら窓ごと閉じます。
- 窓はいくつでも開けます。同じアプリをもう一度押すと、もう 1 枚開きます。窓の題名にはその面の名前が入るので(カレンダー — PKC3)、何枚並んでも見分けられます。
- ブラウザが新しい窓を塞いだときは、これまでどおりこの画面で開き、画面の下に理由が出ます。ポップアップの許可を出すと別の窓で開きます。
- アドレスの末尾に #pkc?view=calendar のように付けて開くと、その画面で始まります。ブックマークしておけます。書ける字と開く画面の対応は、ヘルプのマニュアル 4-1 に表があります。

### https:// の無いアドレスはリンクになりません。文中の URL は壊れなくなりました

- www.example.com や README.md のように https:// を付けずに書いたアドレスは、リンクになりません。頭に https:// を付ければ、これまでどおりリンクになります。メールアドレスは今までどおりです。
- これまでは勝手にリンクになり、押すと、まったく関係のない他人のサイトが開くことがありました。書いたままの字で出します。
- アドレスの後ろに句点や読点を続けても、そこから先はリンクに入らなくなりました。これまでは文の終わりまで飲み込んでいました。
- アドレスの後ろに、をどうぞ のような字を空けずに続けると、いまもそこまでリンクに入ります。アドレスの後ろは空けるか、句読点で区切ってください。
- クロード・モネのように中黒(・)を含むアドレスは、途中で切れず 1 本のリンクのままになります。
- 外のサイトの画像を書くときの説明にコード片を入れると、その中身だけが消えていました。画像を読み込む前に読む字なので、そのまま残ります。

### アプリの面を開いても、書きかけと読んでいた場所が消えなくなりました

- ノートの下の追記欄に打っている途中でカレンダーやヘルプを開くと、打った字が黙って消えていました。面を開いて戻っても、そのまま残ります。
- 長いノートを下まで読んでから面を開いて戻ると、先頭に戻っていました。読んでいた場所に戻ります。
- 開いた面の上に「× 閉じる」が出るようになりました。これまでは同じタイルをもう一度押すか Alt+1 を知っているしか閉じる道がありませんでした。
- 編集中にカレンダーややることの板を押すと、何も起きず理由も出ませんでした。いまは画面の下に理由が出ます。

### 文書の情報が壊れていても、黙って消さなくなりました

- 文書の情報の閉じの --- を消してしまうと、書いてあったタグが黙って消えていました。読めていないときは、情報ペインのタグの行に「読めていません」と理由が出ます。
- そのノートに日付やチェックを付けると、これまでは情報がもう 1 組前に足され、書いてあったタグは本文の側へ落ちていました。いまは閉じを補って直すので、書いた情報がそのまま残ります。
- 既に 2 組になってしまったノートは、1 組目のタグをこれまでどおり出したうえで、タグの行にマウスを乗せると「本文の先頭にもう 1 組ある」と理由が出ます。本文の上に残っている --- の行を消せば元に戻ります。
- 1 面編集では、本文のすぐ上の札にも理由が出ます。これまでは読めなくなると札ごと消えていました。

### カレンダーの日のセルと、いくつかの取りこぼしを直しました

- カレンダーで予定が増えると、その週だけ背が高くなり、下の週がずれていました。同じ所を 2 回押すと別の日に付いてしまう原因です。週の高さはもう変わりません。
- 予定が 2 件以上ある日は、日の数字の右に件数が出ます。セルに入り切らない分はセルの中でスクロールできます。印刷すると、その日の予定は全部そのまま紙に出ます。
- フォルダタブと 2 ペインで行を掴んで動かしている最中に、表が作り直されて掴んでいたものが外れることがありました。日付が入っただけでは作り直さなくなりました。
- 文書の情報を書いたノートで、本文のチェックの印を押すと、押した項目とは別の行が書き換わっていました。押した項目の行が変わります。
- 編集中にゴミ箱のボタンを押しても何も起きないことがありました。ゴミ箱は編集中でも開けます。戻す・復元・空にするは、理由を出して断ります。

### 行の色分けと、確認の色を直しました

- フォルダタブと 2 ペインの表で、マウスを乗せただけの行が、印を付けた行と同じ色に見えていました。乗せただけの行は薄い色になり、印はこれまでどおり濃い色で出ます。どの配色テーマでも見分けられます。
- 編集中に新しい版へ切り替える確認の「切り替える」に、他の戻せない操作と同じ色が付きました。編集中の内容は保存されないためです。色の付く操作は全部で 7 種類になりました。

## 2026-08-21

### 素のまま起動を一度許すと、次から聞かれなくなりました

- アプリとして登録した HTML の添付は、素のまま起動を一度許すと、その中身については次から聞かれません。ランチャーのタイルを押すだけで開きます。
- 憶えているのは中身そのものです。中身を差し替えると、同じ名前でもまた聞きます。登録していない添付は今までどおり毎回聞きます。
- 許可は設定の「素のまま起動を許したアプリ」からいつでも取り消せます。
- 確認のボタンの字を「同じ場所で開く」から「ノートを渡して開く」に変えました。窓は前から別窓で、変わるのはデータの入れ物のほうだからです。
- 素のまま動くアプリはこの許可の一覧も書き換えられます。知らない相手からもらった HTML には許可を出さないでください。

### 確認の画面が PKC 自身のものになりました

- 削除やゴミ箱を空にするときに出る確認が、ブラウザのものから PKC 自身のものに変わりました。ボタンの位置が一定になり、マウスの動きが短くなります。
- 取り消す側が左、実行する側が右です。開いたときは取り消す側が選ばれているので、Enter を押しても実行はされません。
- 戻しにくい操作は、実行する側のボタンに色が付き、何が起きるかがボタンの字に出ます ── 削除 / 空にする / 上書きする / 同じ場所で開く / 整理する / 打ち切る の 6 種類です。
  - ⚠ 訂正(2026-08-21): このうち「同じ場所で開く」は、同じ日のうちに **ノートを渡して開く** へ字を変えました(窓ではなくデータの入れ物が変わる、が伝わらなかったため)。
  - ⚠ 訂正(2026-08-22): その後 **切り替える**(編集中に新しい版へ)が加わり、色の付く操作は **7 種類**になりました(2026-08-22 の項を参照)。
- ブラウザの「このページにこれ以上ダイアログを表示させない」を選ぶと確認が出せなくなり、ボタンが効かなくなる問題がありました。これも直っています。

### 括弧を閉じると二重になっていたのを直しました

- 1 面編集で [ ( 「 などを打つと閉じが自動で入りますが、そのあと自分で閉じを打つと二重になっていました。いまは、すでにある閉じを通り抜けます。
- とくに tags: [あ, い] と打つと tags: [あ, い]] になり、そのタグが「い]」という別のタグとして黙って保存されていました。警告も出ていませんでした。
- - [ ] やること と打つと行末に ] が余るのも、同じ理由でした。どちらも直っています。
- 対になる 9 とおり(バッククォート [ ( { 「 『 (全角) 【 二重引用符)すべてで直りました。文字を選んでから打つと囲む、はこれまでどおりです。

## 2026-08-20

### 起動できなくなる不具合を直しました(本文の検索も効くようになりました)

- 「起動に失敗しました」と出てアプリが開かなくなることがありました。直りました。開けなかった間も、書いたものは無事です。
- 原因は本文検索の索引を作り直す順番でした。毎回おなじところで止まり、その手前まで全部やり直すので、中身には手が付いていません。
- 前から使っている方は、本文の検索が効いていませんでした。次に開いたときに索引を作り直すので、そのまま本文で探せるようになります。
- 索引が傷んでいるときは作り直します。作り直しても直らないときは索引ごと作り直すので、これで起動できなくなることはありません。

### やることの板で、完了したものを下に畳むようにしました

- やることの板を開いたときに見えるのは、まだ終わっていないものだけになりました。済んだものは下に畳んであります。
- 済んだものを見るときは、下の ▸ 完了(3) のところを押してください。もう一度押すと畳みます。
- 畳んでいても件数は出ます。黙って消したりはしません。
- チェックを付けた札は、その場で下へ落ちて畳まれます。消えたのではなく、完了の数が 1 つ増えています。

### カレンダーが使えるようになりました(日付が効かない穴と、面の直し)

- 文書の情報に date: と書けば、どの種類のノートでもカレンダーに出るようになりました。これまではログ・表・添付・フォームのノートだけ、書いても出ませんでした。
- そのうえ、それらのノートはカレンダーの日を押しても日付が付かず、2 回目には「本文が変わっているため反映できませんでした」という誤った理由が出ていました。どちらも直りました。
- カレンダーを開いたまま、左の一覧やフォルダからノートを選べるようになりました。これまではタブを押すとカレンダーが閉じてしまい、「ノートを選んでから日を押す」ができませんでした。
- どのノートに日付が付くかを、月送りのならびの右端に出すようにしました。選んでいないときは、何をすればよいかが出ます。
- カレンダーの見た目を直しました。曜日の列が同じ幅になり、月が画面の高さいっぱいに広がります。今日の日付に丸が付き、土日とその月の外が見分けられます。
- カレンダーを閉じるときは、アプリタブのカレンダーをもう一度押すか Alt + 1 を押してください。タブの切り替えでは閉じなくなりました。

## 2026-08-19

### 2 ペインの面を作り直しました(列・操作の並び・カーソル)

- 操作のボタンが、左右のペインの間から画面のいちばん下へ移りました。F5 写す / F6 移す / F2 名前 / F7 フォルダ / F8 ゴミ箱 の順で、いつも同じ場所にあります。
- 書いてある鍵はそのまま効きます。設定でキーを割り当て直すと、ボタンに書いてある字も一緒に変わります。
- 列に見出しが付きました。名前 / 大きさ / 更新 を押すとその列で並び、もう一度押すと逆順になります。
- 大きさの列が増えました。本文の文字数です。フォルダは — と出ます。種類の列は無くなり(行の先頭の絵で分かります)、名前の列が残りの幅を全部使うようになりました。
- カーソル(いま指している行の枠)が出るようになりました。上下キーはカーソルだけを動かし、印は動きません。Space または Insert で印を付けて 1 行下へ進みます。
- 印が 1 つも無いときは、カーソルの行が操作の相手になります。どちらが「移す元」かは、上のタブの帯の色と、下の件数の行の (ここが元) で分かります。

### やることの板が戻りました(札はチェックリストの行です)

- アプリの一覧に「やることの板」が出るようになりました。カレンダーや 2 ペインと同じ、最初から入っているアプリです。
- 本文に - [ ] やること と書いた行が、そのまま札になります。ノートの種類は問いません。1 つのノートに何個書いても、それぞれ札になります。
- 札の印を押すと、その場で「完了」の列へ移り、元のノートの本文にも書き込まれます。開いているノートではなく、その札のノートに書きます。
- 札の字を押すと、そのノートを選びます。面はそのままです。
- もう一度「やることの板」を押すと、本文の面へ戻ります。カレンダーや 2 ペインも同じです。
- 数が多いときは途中までを出し、そのことを画面の上に書きます。

### チェックリストの印が、押せるようになりました

- 本文の - [ ] を押すと、その場でチェックが付き、本文にも書き込まれます。これまでは押せませんでした。
- もう一度押すと外れます。別のノートへ移って戻っても、押した状態のままです。
- 書き換えるのは押した行の印 1 文字だけです。まわりの本文や空白の入れ方は変わりません。
- 書き出した HTML や別の窓で見ているときは、これまでどおり押せません(保存する先が無いためです)。
- 編集中は押せません。編集を終えてから押してください。

### カレンダーが戻りました(日付は文書の情報に書きます)

- アプリの一覧に「カレンダー」が出るようになりました。2 ペインや Office と同じ、最初から入っているアプリです。
- 文書の情報に date: 2026-08-19 のように日付を書くと、その日に出ます。todo でなくても、どのノートでも出せます。
- ノートを選んでから日の数字を押すと、その日付が入ります。同じ日をもう一度押すと外れます。
- 日付の形は 2026-08-19 のように 4 桁-2 桁-2 桁です。ほかの書き方は日付として読まれません(本文にはそのまま残ります)。
- これまでに書いた日付は、そのノートを一度保存すると出るようになります。

### 文書の情報(タグなど)が、編集中に消えなくなりました

- タグなどを書いた文書を開くと、本文の上に「この文書の情報」として出るようになりました。中身もそこに出ます。
- これまでは情報が本文の一部として出ていたため、見出しや線に見えて、消せてしまいました。消しても知らせはありませんでした。
- 情報は「情報を編集」から書き替えます。本文の編集や「全文を編集」では巻き込まれません。
- 書き替えたあと読めない形になっていたら、その場でお知らせします。書いた内容は本文に残ります。

### 2 ペインで、キーボードと整理の操作が使えます

- 行の上で ↑↓ を押すと送れます。Shift を押しながらで、そこまでまとめて選べます。Ctrl+A でそのペイン全部です。
- Enter でフォルダの中へ入り、Backspace で親へ戻ります。Tab を押すと反対のペインへ移ります。入るのは押したペインだけで、反対側と左の一覧は動きません(空のフォルダでもキーは効いたままです)。
- Delete、または「ゴミ箱へ」で、そのペインで選んだものを消せます(戻せます)。左の一覧の印は巻き込みません。
- 「写す」で反対側の場所へコピーできます。フォルダを選ぶと中身も一緒に写ります(元は残ります)。
- F2 または「名前を変える」で、その場で名前を打ち替えられます。「新しいフォルダ」はそのペインの場所に作ります。
- 行を掴んで反対のペインへ落とすと、そこへ移せます。フォルダの行に落とすとその中へ入ります。印を付けてあれば、掴んだペインの印ごと運びます。

### このアプリの入れ物に、端末ごとの名前が付きました

- よその PKC3 で書かれたリンク(pkc:// で始まるもの)を、自分のものと取り違えなくなりました。これまでは全部の端末が同じ名前を使っていたため、押しても無いノートを探しに行くことがありました。
- いま入っているノートはそのままです。名前を付け直すのは、新しく始める端末だけです。
- 古い版のタブを開いたままにしていると、画面の下に「古い版のタブが本体になっています」と出ることがあります。PKC3 のタブを全部閉じてから開き直すと直ります。

### 「2 ペインで整理」は、アプリの一覧から開きます

- 左の列の「アプリ」タブに「2 ペインで整理」が出るようになりました。Office と同じ、最初から入っているアプリです。
- 左の列のいちばん下にあった「2 ペイン」のボタンは無くなりました。入口はアプリの一覧に 1 つだけです。
- Alt+6 は今までどおり効きます。
- Office を入れていない端末でも「2 ペインで整理」は出ます。並びはいつも同じで、Office を入れても位置は変わりません。

## 2026-08-18

### Word に入る図が、拡大しても粗くならなくなりました

- 本文の図(mermaid)が、絵ではなく図形として Word 文書に入るようになりました。拡大しても粗くならず、Word の中で線や箱を選んで直せます。
- グラフは今までどおり絵として入ります(グラフの描き手が図形を出さないためです)。
- PKC の中の Office で開けなかった問題も、これで直りました。図の入っている Word 文書が真っ白のまま開いていた場合は、書き出し直してください。

### ショートカットキーが増え、自分で割り当て直せるようになりました

- 設定に「ショートカットキー」が増えました。操作ごとに「割り当て」を押して、使いたいキーを押すだけで変えられます。
- いま効いているキーの一覧は、ヘルプにも出ます。割当を変えると一覧もその場で変わります。
- Alt+1〜5 で本文・集計・設定・フラグ・ヘルプの面へ移れます。Ctrl+E で編集、Ctrl+F で絞り込みの欄へ移ります。
- Ctrl+\ で左のペイン、Ctrl+Shift+\ で右のペイン、Ctrl+Alt+\ で両方を畳めます(Alt+[ と Alt+] も今までどおり効きます)。
- 1 面の編集で打っている最中に Ctrl+N が効いてしまい、別のノートへ飛ぶことがあった問題も直りました。
- 面を開く鍵(F1 など)は、もう一度押すと本文に戻ります。押しボタンと同じ動きです。

### 「閲覧用 HTML」「Markdown」「使っていない添付を消す」が設定へ移りました

- 左の列のいちばん下には、よく押す「取り込む」と「バックアップ」だけを残しました。
- 移した 3 つは、設定の中の「書き出しと片づけ」に並んでいます。畳んではいないので、設定を開けば全部見えて押せます。
- 押したときの動きは何も変わっていません。

### スクリーンショットを本文にそのまま貼れるようになりました

- 画像をコピーした状態で、編集中の本文に Ctrl+V(Mac は Cmd+V)を押すと、カーソルの位置に画像が入ります。名前は貼った日時から付きます。
- クリップボードに画像が複数あるときは、全部入ります。取り消し(Ctrl+Z)も効きます。
- ファイルを画面に落として入れられるようにもなりました。編集中の本文へ落とすと本文に入り、それ以外の場所へ落とすと添付になります。
- 編集していないときに貼った画像は、これまでどおり添付のノートになります。
- 本文に入れた画像が、書いている最中にも見えるようになりました。これまでは確定するまで空の枠のままでした。
- ウェブページや AI の回答をコピーして貼ると、見出し・箇条書き・表・コードの形のまま入ります。埋め込みの画像は添付にし、リンクの画像は元の場所のまま(既定では確認してから読み込みます)。

### フォルダの表を上下キーで送れます。Enter は「読む」から始まります

- フォルダの表で ↑↓ を押すと行を送れます(送った行が選ばれます)。Shift を押しながらで、そこまでまとめて選べます。
- これでマウスを使わずに行までたどり着けます。これまでは一度クリックしないとキーが効きませんでした。
- Enter で開いたときは、まず読むところから始まります。開いてすぐ書きたいときは、設定の「開いたときの状態」で切り替えてください。
- 行を 1 回押して選んだだけでは編集に入りません(それは「選ぶ」で、「開く」ではありません)。

### 左の列が「フォルダ」で開くようになり、まとめて選べます

- 左の列は最初にフォルダのタブで開きます。前に選んでいたタブは覚えるので、一覧で使いたい人は一度選べば次からも一覧です。
- フォルダは 2 回押すと開きます。1 回押すのは「選ぶ」だけになりました。
- Ctrl(Mac は Cmd)を押しながらで印を足せます。Shift を押しながらで、そこまでをまとめて印にできます。
- 2 件以上に印を付けると「まとめてゴミ箱へ」が出ます。ゴミ箱からいつでも戻せます。
- 行を掴んでフォルダに落とすと入ります。上のパンくずに落とすと、その階層へ出せます。
- フォルダの中でも、並べ替え(更新順・題名順など)が効くようになりました。

### フォルダの操作を OS のファイラに合わせ、貼った図も残るようになりました

- ウェブページの図(SVG)をコピーして貼ると、図のまま添付として保存します。拡大しても粗くならず、書き出しにも入ります。
- フォルダの中でノートを作った直後にタブを閉じても、居場所(どのフォルダに入れたか)が残るようになりました。これまでは作り直した場所から出てしまうことがありました。
- フォルダに入れたとき、画面はその場に留まるようになりました(入れた先へ勝手に移動しません)。どこへ入れたかは画面の下に出ます。
- フォルダの表で Enter を押すと開き、Backspace で親へ戻り、Delete でゴミ箱へ入れられます。Ctrl+A でいま出ている行をぜんぶ選べます。

### 2 ペインで整理できるようになりました

- 左の列の「2 ペイン」を押すと、別の場所を左右に開ける面が出ます。Alt+6 でも開きます。
- 片方でノートを選んで、真ん中の「右へ移す」「左へ移す」を押すと、反対側が開いている場所へ入ります。
- それぞれのペインはタブを持てます。「+」で足すと、いまの場所をもう 1 枚として開きます。
- 行の選び方はフォルダのタブと同じです。Ctrl で足す、Shift でそこまでまとめて、フォルダは 2 回押すと中へ入ります。
- 入るのは押したペインだけです。左の列の現在地は動きません。
- 書きかけのまま開いて置き場を眺められます。実際に移すときだけ、先に保存するようお願いします。

## 2026-08-17

### Word の書き出しが画面の紙面に合うようになりました

- 書き出した Word 文書の紙が、設定の紙面(A4 / A3 の縦横)と同じになります。これまでは設定にかかわらず A4 縦でした。
- 改頁(+++)が Word でも改頁になります。これまでは水平線に化けていました。
- ページ番号が下の中央に入ります。
- :::if{format=docx} で囲んだ所が、Word の書き出しにだけ入るようになりました(画面には出ません)。逆に :::if{format=html} は画面と閲覧用 HTML にだけ出ます。
- 文書の先頭に書いた設定(--- で囲んだ所)が本文に出てしまう / {{vars.…}} がそのまま出る / 見出し番号が付かない、も直りました。

### Word の書き出しに画像が入るようになりました

- 本文に貼った添付の画像が、そのまま Word 文書に入ります。縦横比はそのままで、はみ出すものだけ紙幅に合わせて縮みます。
- 入れられなかった画像は、これまでどおりその場に理由が出ます(取り出せなかった / Word が読めない形式 / 大きさを読めなかった)。
- 保存した直後に「Word」やバックアップを押したときに、保存する前の本文が出ることがありました。いまは保存が終わってから書き出します。

### Word の書き出しに図とグラフが入るようになりました

- 本文に書いた図(mermaid)とグラフが、画面と同じ絵のまま Word 文書に入ります。これまでは図の代わりに図の原文が等幅の文字で出ていました。
- 表(csv)やコードの塊で、中身と原文が二重に出ていたのも直りました。コピーの記号も文書に入らなくなりました。
- 図が描けなかったときは、その場に「描けませんでした」と出ます(黙って消えません)。

### ノートを Word(.docx)で書き出せるようになりました

- 右の情報ペインの「Word」で、選んでいるノートが Word 文書になります。1 ノートが 1 文書です。
- 見出し・箇条書き・表・引用・コード・強調・外部リンクが、画面で見ているのと同じ形で入ります。
- この版では画像と図は入りません。入らなかった場所には理由を書き、書き出したあとに件数を出します。
- Word 文書は取り込み直せません。戻せる形が要るときは「バックアップ」を使ってください。

### Office で同じ文書を 2 回保存しても、ノートが増えなくなりました

- Office の窓で新しく作った文書をもう一度保存すると、1 回目と同じノートの中身が入れ替わります。これまでは保存のたびに同じ名前のノートが増えていました。
- PKC で編集している最中に保存が何度も届いた場合も、まとめて 1 件のノートになります。
- 別の窓で同じ名前の文書を保存したときは、別のノートになります(中身が混ざりません)。

## 2026-08-16

### ボタンの見た目を整えました(本文の面が広くなりました)

- 中央の上にあった開閉の帯をやめました。本文の面がそのぶん広くなります。
- 左右の列を畳むボタンは、列と列のあいだの細い帯になりました。畳んでもそこに残ります。
- 戻る・進むは「‹」「›」の 1 文字になりました。押せないときは薄くなります。
- 置換は編集中の書式の帯に移りました(閲覧中は出ません)。Ctrl と H は編集中に効きます。
- ボタンの枠を外して、左上のタブと同じ見た目に揃えました。
- 設定の Office の欄に、いま入っている一式のビルド(識別子と焼いた日時)が出るようになりました。

### PDF が読める大きさで出るようになりました(別の窓でも開けます)

- PDF の添付が、画面の高さいっぱいで出るようになりました。これまでは小さな枠に押し込まれていました。
- 拡大・頁送り・検索は、ブラウザの PDF ビューアがそのまま使えます。
- 詳細の「別の窓で見る」が PDF にも出ます。頁を読めるように大きな窓で開きます。
- 窓の題名には添付の名前が出ます。窓を閉じると使っていたメモリはすぐに返します。

### Office で保存すると、PKC のノートに残るようになりました

- Office の窓で保存すると、その中身が PKC に取り込まれます。これまでは窓を閉じると消えていました。
- PKC から開いた添付を上書き保存すると、そのノートが新しい中身に入れ替わります。
- 入れ替える前の中身はノートに控えが残ります。控えは 5 世代まで、合計 200MB までです。
- 新しく作った文書を保存すると、添付のノートが 1 件できます。同じ文書をもう一度保存すると、そのノートが入れ替わります。
- PKC で編集している最中に保存が届いたときは、編集を終えてから取り込みます。
- Office の窓が不安定になって保存が効かないときは、画面の下でお知らせします。

### 1 面編集で、行の箱の高さと上下キーを直しました

- 長い段落を開くと、折り返したぶんだけ箱が高くなります。文の先頭から見えます。
- 上下キーは箱の中でカーソルが動くだけになりました。隣の行へは飛びません。
- 隣の行へ移るときは Alt と上下キーです。箱のどこに居ても効きます。

## 2026-08-15

### 本文から探せるようになりました(並び順とタグも)

- 左上の欄が「探す」になりました。題名だけでなく本文からも探せます。
- 日本語は 3 文字以上でよく当たります。2 文字以下でも探せますが、少し大まかになります。
- 一覧の並び順を選べます。手動の順・更新が新しい順・題名順・種類順の 4 つです。
- ノートの先頭に tags: [買い物, 家事] と書くと、情報ペインに札が出ます。押すとそのタグで探せます。

### ノート同士をつなげるようになりました

- 右の情報ペインに「関係」が出ます。関連・分類・時系列・出典の 4 種類でつなげます。
- 相手の題名を打って「関係を足す」で足します。相手の名前を押すと、そのノートへ移ります。
- 消すときは、その関係の「消す」を押します。ノート自体は消えません。
- フォルダの親子はここでは作りません。フォルダ画面で移動すると付きます。
- 添付が画像のときは「別の窓で見る」が出ます。本文を書きながら画像を見られます。
- 「HTML でコピー」が、貼り先で崩れないように掃除してから渡すようになりました(図の原文や押せないボタンが付いてきません)。

### 項目ごとに束ねて数える「集計」が使えるようになりました

- 左の列に「集計」が増えました。本文の先頭に書いた項目でノートを束ねて数えます。
- 本文のいちばん上を --- で囲んで author: 佐藤 のように書くと、その項目で束ねられます。
- 束ね方は実際に書かれている項目だけが出ます。選ぶと値ごとに何件あるかが並びます。
- tags のように並べて書いた値は、どちらの組にも入ります。書いていないノートは(未設定)にまとまります。
- 数え直すのは「数え直す」を押したときだけです。選んだ束ね方は次に開いたときも残ります。

### 戻る・進むと、ペインの開閉ができるようになりました

- 中央の上に「戻る」「進む」が付きました。さっきまで見ていたノートへ 1 手で戻れます。
  - ⚠ **訂正(2026-08-18)**: 置き場は「中央の上」ではなく **左の列の上**(探す欄の左)です。
    掲示のときから誤っていました。
- キーボードなら Alt と左右の矢印です。押せないときはボタンが灰色になります。
- 同じ場所の「一覧」「情報」を押すと、左右の列を畳めます。畳んだ状態は次に開いたときも残ります。
- キーボードなら Alt と [ または ] です。ヘルプは F1 で開きます。
- 編集中に Ctrl と H で置換の欄が開きます。探す語を本文から全部置き換えます。

### 複数のタブで同時に使えるようになりました

- 2 枚目以降のタブも、一覧・閲覧・編集・新規作成がふつうにできます。別々のノートなら並行して編集できます。
- 同じノートを 2 つのタブで同時に編集することはできません。後から開いた側に理由が出ます。
- 保存の本体は 1 タブだけです。2 枚目以降には「保存は本体タブ経由です」と画面下に出ます。
- 本体のタブを閉じると、残ったタブが読み直し無しでそのまま本体を引き継ぎます。

### グラフ(chart)を本文に書けるようになりました

- コード塊の言語を chart にして JSON を書くと、グラフになります。棒・折れ線・円・ドーナツ・レーダー・散布が選べます。
- 色は選んでいる配色から取ります。本文に色を書く必要はありません。
- 図と同じく PNG にして表示します。スクロールは軽いままです。
- 書き方が違っているときは、その場に理由が出ます(黙って空になりません)。

## 2026-08-14

### アプリの一覧から Office を開けるようになりました

- Office のひとそろいを入れてある端末では、アプリの一覧に「Office」が並びます。
- 押すと Office の窓が開き、新しい文書・表計算・プレゼンテーションを作れます。
- 入れていない端末には出ません。設定の「Office 表示」からいつでも入れられます。

### Office の設定が、窓を閉じても残るようになりました

- これまで、Office の中で変えた設定(画面の言語など)は窓を閉じると消えていました。
- いまは閉じる前に自動で控えを取り、次に開いたときに戻します。
- 控えはこの端末のブラウザの中だけに置きます。外へは送りません。

### Office のメニューやドロップダウンが、マウスで選べるようになりました

- これまで、メニューの項目やドロップダウンの一覧を押しても、閉じるだけで選べないことがありました。
- 押した瞬間に一覧が消えてしまう作りの穴をふさぎました。マウスだけで選べます。
- 直っているのは新しいひとそろいのほうです。設定の「Office 表示」から入れ直してください。

### Office の画面が日本語になりました

- メニューやダイアログが日本語で出ます。これまでは文字は日本語で書けるのに、画面は英語のままでした。
- 直っているのは新しいひとそろいのほうです。設定の「Office 表示」から入れ直してください。
- 英語に戻したいときは、Office の「ツール → オプション → 言語設定」でいつでも選べます。

### 1 面で編集(ライブエディタ)が最初の設定になりました

- 編集を開くと、描画されたままの本文の上で、押した行だけが原文になります。フラグを入れる必要は無くなりました。
- これまでの 2 ペイン(原文とプレビュー)も使えます。設定の「表示」の「編集の仕方」から選べます。
- 切り替えは、次に編集を開いたときから効きます。

### 添付の説明でも、文書の寄せの宣言が効くようになりました

- 添付の説明に書いた行頭の |> や <| が、文書全体の寄せの反対側に寄ります。
- これまで添付の説明だけ align や direction の宣言が効かず、書き出した HTML と見え方が食い違っていました。

### 本文の添付参照から、その添付のノートへ飛べるようになりました

- 本文に書いた pkc:// の添付あての参照を押すと、その添付を持つノートが開きます。
- 参照先が見つからないときは理由を出します。押しても何も起きない、をやめました。

### お知らせの「閉じる」が見出しの行に移りました

- お知らせが多いときは、この枠の中を下までたどらないと「閉じる」に届きませんでした。
- 「閉じる」は見出しの右端にあります。中の文をどれだけ送っても、そこから動きません。

## 2026-08-13

### Office の窓が固まったとき、お知らせするようになりました

- Office の中身が固まると、その窓のタブはクリックもキーも受け付けなくなり、窓の中には何も出せません。
- PKC3 の画面に戻ってくると、下の行に「Office の窓が応答していません」と出るようになりました。
- 窓のタブを閉じて、もう一度「Office で開く」を押してください。

### Office でダイアログを閉じると止まる問題を直しました

- Word Count や検索と置換の窓を閉じただけで Office が停止する問題を直しました。開いている文書は失われていました。
- 原因は Office 側ではなく、その土台(Qt)が窓を片づけるときに壊れていたことでした。
- 直っているのは新しいひとそろいのほうです。設定の「Office 表示」から入れ直してください。
- ひとそろいが配布元と別の版のときは、起動したときと設定の画面でお知らせします。
- 入れ直すかどうかはあなたが決めます。勝手に取りに行くことはありません。

### Office でコピーすると操作が効かなくなる問題を直しました

- Office でコピーや切り取りをすると、その後の保存やメニューが黙って効かなくなる問題を直しました。
- 見た目は正常なまま操作だけ通らなくなるので、気づけないまま保存できていない状態が続いていました。
- Office でコピーした文字を他のアプリに貼れるようになりました。Office の中でのコピーと貼り付けも動きます。
- 他のアプリから Office へ貼り付けることは、環境によってはできません。
- Office の窓が画面にぴったり収まるようになりました。左右の 4 ピクセルのずれも無くなっています。
- 不安定になったときのお知らせは、画面の下に出るようになりました。メニューが隠れなくなります。

## 2026-08-12

### Office の窓が画面いっぱいで開くようになりました

- Office の窓が、開いた瞬間から画面いっぱいで出ます。下端のステータスバーが切れなくなり、メニューも折り返しにくくなりました。
- Office の中身が壊れて操作が効かなくなったとき、上部に帯を出してお知らせします。これまでは見た目が正常なままでした。
- 帯が出ている間は保存が効きません。帯の「読み込み直す」から開き直してください。
- Mac で Cmd+A(全選択)が効かなくなっていた問題を直しました。

### Office の窓の不具合を直しました

- Office が異常終了したとき、止まったことと理由が画面に出るようになりました。これまでは最後の絵が残り、固まったように見えていました。
- 止まったときは、その画面の「読み込み直す」から開き直せます。
- Ctrl+S や Ctrl+P がブラウザに取られなくなり、Office 側に届くようになりました。
- Mac で Ctrl と英字を押すと本文にその文字が入ってしまう問題を直しました。
- 文書の脇に「読み込んでいます…」が残る問題と、文書の上をなぞるとページの文字が選ばれる問題を直しました。
- 準備が終わっていないだけのときは、そう伝えて読み直す案内を出します。

## 2026-08-11

### Word / Excel / PowerPoint の添付を読めるようになりました

- Office の添付を開くと「Office で開く」が出ます。押すと別の窓に文書が組み上がります。
- 別の窓なので、PKC3 の編集を続けたまま資料を読めます。窓を閉じるとメモリは返ります。
- 縦書き・禁則・ルビ・圏点を含む日本語の組版がそのまま出ます。
- 使うには、設定の「Office 表示」から約 77MB のひとそろいを入れてください。配布元に届かないときは手元の zip でも入れられます。
- いまのところ Chrome や Edge などの Chromium 系でだけ動きます。動かない環境では、足りないものを名指しで出します。
- Office を動かす準備として、この端末で最初に開いたときだけ画面が 1 回読み直されます。2 回目からは起きません。

## 2026-08-08

### 本文の読み幅と印刷の紙を選べるようになりました

- 設定の「表示」に「紙面」が増えました。A4 と A3 の縦横、フル HD と 4:3 の縦横から選べます。
- 最初は A4 縦です。本文の幅はこれまでと同じですが、印刷の紙は A4 縦を指定するようになりました。
- フル HD を選ぶと本文の読み幅の上限が外れ、画面の幅いっぱいまで広がります。
- A4 と A3 を選ぶと、印刷したときの紙もその大きさになります。
- 読む面・添付の説明・編集のプレビュー・1 面の編集が、同じ幅で出るようになりました。
- 書き出した閲覧用 HTML には、書き出したときの紙面が入ります。

### フラグ画面とヘルプ画面ができました

- 開発中の切り替えは、これまで URL に直接打ち込む必要がありました。左の列の「フラグ」から切り替えられます。
- 設定とは別の画面です。設定はあなたのもの、フラグはいつか畳まれるものです。
- 左の列の「ヘルプ」から、マニュアルと過去のお知らせが読めます。オフラインでも読めます。

### 本文のコピーと、1 面編集の操作が増えました

- 読んでいるノートの上に「Markdown をコピー」「書式付きでコピー」「選択範囲をコピー」が並びます。
- 書式付きは Word やメールにそのままの体裁で貼れます。選択範囲は原文で入ります。
- 1 面で編集するとき(フラグ)、入力欄の下端で下キーを押すと次の行が開きます。上キーも同じです。
- 同じく Ctrl+S でその行を確定します。これまではブラウザの保存画面が開いていました。
- 書式パネルと Ctrl+B / I / K が、開いている行にも効くようになりました。これまでは押しても何も起きませんでした。
- 「全文を編集」ボタンで、全体を素の markdown として編集できます。Ctrl+A でも同じです。

### 本文のリンクが押せるようになりました

- 本文に書いた entry: のリンクを押すと、そのノートが開きます。これまでは押しても何も起きませんでした。
- PKC2 から取り込んだ本文のリンクも開きます。見出しまで指している場合も、そのノートまでは開きます。
- カード参照(@card)はキーボードでも押せます。Tab で移動して Enter か Space です。
- pkc:// で書いたノートあてのリンクも押せます。これまでは押せない印だけが出ていました。
- 開けないときは、理由が画面の下に出ます。編集中は移れません(保存するか取り消してください)。
- 編集中に一覧のノートを押したときも、理由が出るようになりました。これまでは何も起きませんでした。

### 本文の寄せと幅が、画面と配布物で揃いました

- 行頭の |> や <| で寄せた行は、文書全体の寄せの反対側に寄ります。align: right の文書なら左です。
- これまでは align の宣言に関わらず、流れの終わり側(左から右の文書なら右)に寄っていました。
- 文書全体を中央寄せにしているときは寄りません。中央に反対側が無いためです。
- 書き出した閲覧用 HTML の本文の幅が、アプリの画面と同じになりました。
- 表・図・コードは横幅いっぱいを使います。横に広いほど読めるためです。
- 印刷したときの本文の幅も、アプリからの印刷と揃いました。
