/**
 * スマホ用画面の**判定だけ**(#632 段①)。
 *
 * 設計 doc: `docs/development/mobile-screen-design-2026-09.md`
 * (user 裁定 2026-08-30「**スマホの幅なら、スマホ用画面に切り替える。それ以下なら
 * 対応外とする。スマホ用画面がないなら、作る**」)。
 *
 * 🔴 **数字はここ 1 か所**(CLAUDE.md §7)── CSS には `720px` / `359px` を
 * **1 文字も書かない**。CSS が読むのは `data-pkc-layout` / `data-pkc-page` の
 * 2 属性だけで、幅を見るのは `adapter/ui/render/phone-layout.ts` の
 * `matchMedia` 2 本である。⚠ 両方に数字を書くと、片方だけ変えた日に
 * **JS と CSS が別の幅で切り替わる**(誰も気づけない形で版面が 2 つに割れる)。
 * `tests/adapter/phone-layout.test.ts` が「app.css に `720px` が 0 件」を pin する
 * (CSS を読む検査は adapter 側に在る ── この file の test は判定だけを見る)。
 *
 * ⚠ **この file はブラウザの API を 1 つも呼ばない** ── 呼ぶと unit で
 * 全分岐を通せなくなる(画面を作らないと判定が走らない形になる)。
 */
import type { ViewMode } from '@adapter/state/app-state';

/**
 * 🔴 **スマホ用画面へ切り替える上限**(設計 doc §2-1)。
 *
 * ⚠ この値は「app.css が 1 列に折っていた境目」と**同じ数字**である ──
 *   同じにするから版面が増えない(3 列 / ≤1100 の 2 列 / スマホ の 3 つ)。
 * ⚠ 721px 以上は今までどおりの 2 列版面 ── ただし**高さが `PHONE_MAX_HEIGHT_PX` 以下**
 *   なら幅に依らずスマホ用画面(下)。
 */
export const PHONE_MAX_PX = 720;

/**
 * 🔴 **高さでも切る上限**(#663、推薦 ①)。
 *
 * 横向きのスマホは幅 720 を超えるが、**高さは 320〜430 しか無い**(844×390 / 667×375)。
 * 幅だけで見ると 2 列版面に落ち、一覧 186px に帯 4 本(タブ / 探す / 作る / まとめ)が
 * 折り返して一覧の丈を超え、はみ出した「操作を探す」が下に寝た情報ペインに覆われる
 * (`scripts/phone-probe.mjs` の実測:844×390 と 800×400 で 4 行とも押せない)。
 * 🔑 高さ 480 以下は**縦の予算が縮んだスマホ**として扱う ── 版面を増やさず、
 *   既に在るスマホ用画面へ寄せる(1 枚ずつなら帯の折り返しが本文を食わない)。
 * ⚠ 読むのは `adapter/ui/render/phone-layout.ts` の `matchMedia` 1 本
 *   (`(max-width: 720px), (max-height: 480px)` の **OR**)── CSS には書かない(上と同じ理由)。
 * ⚠ 1280×500 のような低い PC 窓は**外側**(500 > 480)── `context-menu.smoke` が
 *   1280×500 で 2 列版面を前提にしており、そこは変えない。
 */
export const PHONE_MAX_HEIGHT_PX = 480;

/**
 * 🔴 **タブレットの版面の上限**(#703。user 裁定 2026-09-04 案 A)。
 *
 * `PHONE_MAX_PX + 1`〜この幅(かつ高さが `PHONE_MAX_HEIGHT_PX` を超える)では、右の情報ペインを
 * **畳んだ状態で開き**、本文の上の帯の「情報」で本文の上に重ねて出す(もう一度で閉じる)。
 * ⚠ 直す前は情報ペインが本文の**下に寝て** 30vh を常に取り、768×1024 で本文 592px(58%)/
 *   1024×768 で 424px(55%)だった(実測 2026-09-05)。
 * ⚠ **この数字は CSS の `@media (max-width: 1100px)` と同じでなければならない** ── あちらは
 *   2 列の版面(一覧 + 本文)を組む境目で、こちらは情報の出し方の境目。片方だけ動かすと
 *   「2 列なのに情報が右の列に居ない」幅ができる。`tests/adapter/phone-layout.test.ts` が
 *   CSS の数字と等値で pin する(CSS に数字を書かない規律の**例外**で、既存の `@media` を
 *   属性へ移すのは版面 8 つの書き直しになるため、ここでは等値 pin で結ぶ)。
 */
export const TABLET_MAX_PX = 1100;

/**
 * 🔴 **対応外の下限**(設計 doc §2-2)。⚠ **画面は変えないし、操作も塞がない** ──
 *   状態の行に 1 度だけ理由を出すだけである(塞ぐと、その幅で開いた user が
 *   自分のノートを取り出せなくなる)。判定を使うのは段③。
 * ⚠ **いまはどこからも読まれていない** ── 使い道のない seam を先に置かない規律と
 *   わずかに擦れるが、**数字の正本を割らない**ためにここへ置く(段③ で CSS でも
 *   `matchMedia` でもなく、この定数から `359px` を組む)。
 */
export const PHONE_MIN_PX = 360;

/**
 * スマホ用画面で「いま見えている 1 枚」。
 *
 * 🔴 **`pane` を `note` と分ける**(2026-09-02、実装して分かった)。どちらも
 *   中央の器を出すので 1 稿目は同じ `note` にしていたが、**帯を出すかが逆**である
 *   ── 設定・フラグ・ヘルプ・2 ペイン・集計は自分の帯(`pane-bar` の「× 閉じる」)を
 *   持つので、ページの帯を重ねると**戻る口が 2 本並ぶ**。
 * ⚠ 分けないと CSS からは区別できず、「帯のぶんの場所を空ける」規則が
 *   **帯の出ない面にも 36px の隙間を作る**(user には理由の無い余白に見える)。
 */
export type PhonePage = 'list' | 'note' | 'info' | 'pane';

/**
 * 判定に要る材料。⚠ **`AppState` を丸ごと取らない** ── 取ると
 * 「この 2 つ以外は判定に効かない」が読めなくなり、次に足した誰かが
 * 3 つ目の条件をここへ書く。
 */
export interface PhoneShape {
  readonly selectedLid: string | null;
  readonly viewMode: ViewMode;
  /**
   * 🔴 **編集中か**(着地前レビュー 5。2026-09-02 に足した)。
   *
   * ⚠ これが無いと、**情報ページで編集を始めると画面が 1 ドットも動かない** ──
   *   中央は `visibility: hidden` のままなので、**打てているのに何も見えない**。
   *   しかも「← 一覧」は編集中なので断られる = 出口も無い。
   * 🔑 編集中は**必ず本文の面を出す**(打っている物が見えない状態を作らない)。
   */
  readonly editing: boolean;
}

/**
 * 🔴 **本文の代わりに、いま何を見せているか**(設計 doc §2-8 / user 裁定 2026-09-02)。
 *
 * 🔑 **1 つの field に畳む**のが肝である。1 稿目は情報用と一覧用の 2 つを別々に
 *   持っていたが、⚠ それだと「**両方立っている**」状態が型として作れてしまい、
 *   どちらを優先するかという**判定が 1 つ増える** ── しかも普段はどちらか一方しか
 *   立たないので、優先順を取り違えても**test は緑のまま**である(CLAUDE.md §1
 *   「救い手が同じ式のもう一方の項」)。1 つにすれば**順番そのものが消える**。
 *
 * 🔑 **真偽値ではなく lid を持つ**(設計 doc §2-8)。「`selectedLid` が変わる /
 *   削除で後継に移る、で**自動的に閉じる**(閉じる code を書かず判定に含める)」を
 *   `open.lid !== selectedLid` の 1 行で満たす ── 閉じる副作用を別の場所に書くと、
 *   書き忘れた日に「別のノートを開いたのに前のノートの情報が出たまま」になる。
 */
export type PhoneOpen = { readonly kind: 'info' | 'list'; readonly lid: string } | null;

/**
 * 🔴 **いま出すページ**(設計 doc §2-5 / §2-8)。
 *
 * @param open いま本文の代わりに見せている物(見せていなければ `null`)。
 *
 * 🔑 **一覧も同じ器で持つ**(user 裁定 2026-09-02「**開いたままにし、一覧の上に
 *   「ノートへ →」を出す**」)── 設計 doc §2-6 は「一覧を出したまま選択を保つ bit」を
 *   **一度は棄却**していたが、棄却の理由は *同じ行をもう一度押したときに倒せない*
 *   (`dispatcher` は state が変わったときしか鳴らない)であって、bit そのものでは
 *   なかった。⚠ その dead tap は **binder が押された時に直に倒す**ことで消える。
 *   同じ doc が「その場合は帯の『ノートへ →』で補う」と**覆る条件まで書いていた**。
 *
 * ⚠ **順番に意味がある**:本文以外の面(設定・フラグ・ヘルプ・2 ペイン・集計)は
 *   情報ページより**強い** ── 面を開いた瞬間に情報は畳む(面と情報が同じセルに
 *   重なっているので、両方見せる場所が無い)。
 */
export function phonePageOf(st: PhoneShape, open: PhoneOpen): PhonePage {
  // 中央が自分の面を出している ── 戻る口はその面の帯の「× 閉じる」
  if (st.viewMode !== 'detail') return 'pane';
  if (st.selectedLid === null) return 'list';
  // 🔴 打っている物が見えない状態を作らない(上の `editing` の docstring)
  if (st.editing) return 'note';
  if (open !== null && open.lid === st.selectedLid) return open.kind;
  return 'note';
}

/**
 * 🔴 **一覧の上に「ノートへ →」を出すか**(user 裁定 2026-09-02)。
 *
 * 🔑 **判定を書き直さない ── `phonePageOf` に聞く**(CLAUDE.md §7)。
 *   `phonePageOf` が **ノートを開いたまま `'list'` を返す**のは
 *   `open.kind === 'list'` の枝**だけ**である(`selectedLid === null` の枝は
 *   その名のとおり開いているノートが無い)── だから
 *   「**開いているノートが在って、いま一覧を見ている**」がそのまま条件になり、
 *   `open` の中身をここでもう一度読む必要が無い。
 * ⚠ もう一度読むと、削除で `selectedLid` が消えた回に **`open` が古いまま**
 *   残って「無いノートへ戻る行」を出す(実際 1 稿目でその形を書いた)。
 */
export function phoneReturnShown(st: PhoneShape, open: PhoneOpen): boolean {
  return st.selectedLid !== null && phonePageOf(st, open) === 'list';
}

/**
 * 🔴 **ページの帯(← 一覧 ｜ 題名 ｜ 情報 ｜ ⋯)を出すか**(設計 doc §2-5)。
 *
 * 🔑 **ページから導く**(state をもう一度読まない)── 2 か所で数えると、
 *   「帯は出ているのに場所が空いていない」が静かに生まれる(CLAUDE.md §7)。
 *   CSS も同じ `data-pkc-page` から場所を空けるので、判定は 1 つで足りる。
 * ⚠ 本文以外の面では出さない ── その面は自分の帯に「× 閉じる」を持っている。
 * ⚠ 一覧ページでも出さない ── 「← 一覧」を一覧の上に出しても押す先が無い。
 */
export function phoneBandShown(page: PhonePage): boolean {
  return page === 'note' || page === 'info';
}
