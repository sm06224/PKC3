/**
 * 🔴 **お知らせの登記表**(P11 段⑤。user 指示 2026-08-07)。
 *
 * > 「**PKC3 にも PKC2 のようにお知らせポップアップをつけてください /
 * > お知らせ掲載内容は過去のお知らせとして、最大 10 件を…ヘルプ画面から
 * > 参照できるようにしてください**」
 *
 * ## PKC2 の失敗を繰り返さない(実測で確かめた 9 件のうち、ここに効く分)
 *
 * | PKC2 で起きたこと | ここでの対処 |
 * |---|---|
 * | 6 件あるのに**先頭 1 件しか読まれず**、残りを毎ビルド配っていた | **過去の一覧**を持ち、`recentNotices` が読む。読まれない entry を作らない |
 * | `date` field を宣言して**一度も読まなかった** | **field を持たない**。日付は id から引く(`noticeDate`)── 二重表現にしない |
 * | `textContent` で描くのに本文が `**強調**` で書かれ、**アスタリスクが見えていた** | **素のテキストと決める**。記法を書いたら test が落とす |
 * | 1 entry 22 項目・1 項目 200 字超の壁ができた | **上限を数で持つ**(件数・項目数・字数)。散文の規律にしない |
 * | 表示上限が複数箇所に散る | **切るのは `recentNotices` 1 か所だけ** |
 *
 * ⚠ **pure module**。browser API を使わない(既読は adapter 側が持つ)。
 */

/** お知らせ 1 件。⚠ **日付の field を持たない**(id が持っている)。 */
export interface Notice {
  /**
   * `YYYY-MM-DD-slug`。⚠ **既読の鍵**であり、**並び順の鍵**でもある。
   * 日付が前置きなので、文字列の降順がそのまま新しい順になる。
   */
  readonly id: string;
  readonly title: string;
  /** 本文。⚠ **素のテキスト**(markdown 記法を書かない ── 描画は textContent)。 */
  readonly items: readonly string[];
}

/**
 * 🔴 **ヘルプ画面に出す件数**(user 裁定 2026-08-07 Q1)。
 * ⚠ **切るのはここ 1 か所**。面ごとに `slice` を書くと、帯とヘルプで件数が食い違う。
 */
export const NOTICE_SHOW_MAX = 10;

/**
 * 🔴 **登記表に残す件数の上限**(裁定 Q1)。
 * 🔴 **表示上限と同じ数にする**(2026-08-29)。⚠ 直す前は **2 倍(20)**で、理由は
 *   「**原本は残す**」だった ── ところが**その役目はいま `CHANGELOG.md` が担っている**
 *   (2026-08-18 から)ので、11 件目より後ろは**原本でもなく画面にも出ない、
 *   配るだけの重り**になっていた(実測 10 件)。
 * 🔑 揃えると不変条件が 1 つ増える:**登記表 = 画面に出るもの**。
 *   ⚠ user から見える所は **1px も変わらない**(その 10 件はもともと出ていない)。
 *   落ちた分は今までどおり `CHANGELOG.md` が受ける。
 */
export const NOTICE_KEEP_MAX = 10;

/**
 * 🔴 **既読の集合に残す id の件数**(`adapter/platform/notice-store.ts`)。
 *
 * ⚠ **登記表の上限とは別物である。** 2026-08-29 まで `NOTICE_KEEP_MAX` を使い回して
 *   いたが、登記表を 20 → 10 へ下げた瞬間に**読んだお知らせが毎起動よみがえる**
 *   ところだった(着地前レビューが拾った。再現は
 *   `tests/adapter/notice-store.test.ts`「登記表から落ちた id が…」)。
 *
 * ## なぜ表示件数より大きくないといけないか
 *
 * 既読の集合には**登記表から落ちた id も残る**(user はそれを読んだので)。
 * だから必要な席は「いま出る 10 件」+「落ちた id のうち user が読んだ分」であり、
 * 表示件数と同じにすると**落ちた id が生きている id を押し出す**。
 * 押し出された 1 件は未読へ戻り、閉じても `same` 判定で書込が起きないので
 * **同じ 1 件が毎起動出続ける**(localStorage を消すまで止まらない)。
 *
 * 🔑 席の数だけに頼らず、`markSeen` は**登記表に在る id を先に残す**
 *   ── 席が足りなくなる形そのものを消してある(CLAUDE.md
 *   「衝突は、検出するより起こらなくするほうが強い」)。ここは**余裕**である。
 */
export const NOTICE_SEEN_MAX = NOTICE_SHOW_MAX * 2;

/** 1 entry の項目数の上限(PKC2 は 22 項目の壁ができた)。 */
export const NOTICE_ITEMS_MAX = 6;
/** 1 項目の字数(下限も置く ── 空の行が user に出るのを止める)。 */
export const NOTICE_ITEM_CHARS_MAX = 120;
export const NOTICE_ITEM_CHARS_MIN = 4;

/**
 * 🔴 **新しい順に並べて、上限まで切る**。
 * ⚠ **ここだけが切る**(上の `NOTICE_SHOW_MAX` の注記)。
 *
 * 🔴 **同じ日のものは、登記表に書いた順で出す**(2026-08-29 の動線レビュー 欠陥 5)。
 * ⚠ 直す前は **id 全体**で比べていたので、日付が同じ 14 件は
 *   **英語スラッグの綴り順**で並んでいた ── いちばん実害の大きい知らせ
 *   (「押しても何も起きなかったのが直った」)が **7 番目**に回り、
 *   user は「次へ」を **6 回**押さないと読めなかった。
 * 🔑 日付だけで比べれば、`Array.sort` は**安定**なので配列の順が残る
 *   ── 登記表は**先頭に足す**規約(`notice-writing` skill)なので、それが「新しい順」である。
 */
export function recentNotices(
  all: readonly Notice[],
  limit = NOTICE_SHOW_MAX,
): readonly Notice[] {
  return [...all]
    .sort((a, b) => {
      const [x, y] = [noticeDate(a.id), noticeDate(b.id)];
      return x < y ? 1 : x > y ? -1 : 0;
    })
    .slice(0, limit);
}

/**
 * まだ見ていないものを新しい順に返す。
 * ⚠ 既読は **id の集合**で渡す ── 「最後に閉じた 1 件」と比べると、
 *   旧ビルドへ戻ったときに**既読が巻き戻る**(PKC2 の F5)。
 */
export function unreadNotices(
  all: readonly Notice[],
  seen: readonly string[],
): readonly Notice[] {
  const seenSet = new Set(seen);
  return recentNotices(all).filter((n) => !seenSet.has(n.id));
}

/** id の先頭から日付を取り出す(`YYYY-MM-DD-slug` → `YYYY-MM-DD`)。 */
export function noticeDate(id: string): string {
  return /^(\d{4}-\d{2}-\d{2})/.exec(id)?.[1] ?? '';
}

/**
 * ── 登記表 ──────────────────────────────────────────────
 *
 * 🔴 **新しいものを先頭に足す。** 書式は `.claude/skills/notice-writing/SKILL.md`。
 *
 * ⚠ **書いた約束は取り消せない**(PKC2 は掲示済みの挙動が後の既定変更を縛った)。
 *   「これから〜します」ではなく「〜できるようになりました」だけを書く。
 * ⚠ **記法を書かない**(`**` / `` ` `` / `[]()`)── 素のテキストとして出る。
 * ⚠ **事実だけ書く。** 起きていないことを書くと、user は同じ画面で嘘に気づく。
 */
export const NOTICES: readonly Notice[] = [
  {
    id: '2026-09-07-csv-empty-cell',
    title: '表の空いているセルにも、押して打てるようになりました',
    items: [
      'csv の表で、セルの数が足りない行の空いている所や、空の行のセルを押すと欄が開き、打った字がそのまま入ります。これまでは押せて打てるのに、確定すると「本文が変わっているため反映できませんでした」と出て消えていました。',
      '足りない区切り(,)は自動で補います。1 という行の 3 列目に x と打つと 1,,x になります。',
      '行と列の ＋ × も効くようになりました。セルの数が足りない行が 1 つあるだけで列の ＋ × が全部押せなくなる、という状態も直っています。',
      '表の幅を超えるセルは打てません(そこにはセルが出ていないためです)。',
    ],
  },
  {
    id: '2026-09-07-append-scroll',
    title: '追記すると足した所へ画面が動き、別の窓は動かなくなりました',
    items: [
      '追記すると、足した字が見える所まで画面が動くようになりました。入り先に見出しを選んだときはその章の下へ、末尾のときは本文のいちばん下へ動きます。',
      '足した字が最初から画面に入っているときは、画面は動きません(読んでいる所を邪魔しません)。',
      '別の窓(小窓や別のタブ)で同じノートを開いているとき、どちらかで追記すると、もう一方の窓が本文の先頭へ飛んでいました。飛ばなくなり、読んでいた場所のまま残ります。',
      'これは追記だけでなく、別の窓が保存や書き換えをしたときにも起きていました。まとめて直っています。',
    ],
  },
  {
    id: '2026-09-07-inline-calc',
    title: '本文でそのまま計算できるようになりました(= を打って Enter)',
    items: [
      '本文に 2+3= のように打って Enter を押すと、その場が 2+3=5 になって次の行へ進みます。打った所がそのまま計算欄です。日本語入力のまま ２＋３＝ と全角で打っても計算し、式ごと半角に直ります。',
      '使えるのは + - * / % と丸括弧と数です。% は余りです(パーセントではありません)。1200*1.1= は 1320、0.1+0.2= は 0.3 と出ます(端数のごみは出しません)。',
      '箇条書きの行でも、前に文があっても計算します。- 1+2= は 3、合計 3*4= は 12 になります。本文の欄でも、下の追記欄でも打てます。',
      '計算にならないときは、ただ改行するだけです。打った字は 1 つも変わりません。締切= のように式でないとき、1/0= のように 0 で割るとき、1,000= md5= 2^3= のように数や英数字の語の途中のときです。',
      '出た答えは Ctrl + Z(Mac は ⌘ + Z)で、押した分だけ戻せます。引用(>)の行でも戻せるようになりました(これまでは引用の行だけ取り消しが効きませんでした)。',
      '表の升の計算(=SUM のような書き方)とは別物です。あちらは式のまま残って表示のたびに計算し直しますが、こちらは答えが本文に書き込まれて後から変わりません。',
    ],
  },
  {
    id: '2026-09-06-touch-cell-mark',
    title: '指で触る端末で、表のどの升が打てるか見えるようになりました',
    items: [
      '表の升の右下に小さな三角の印が出るようになりました。その升は押すと打てます。これまでは、押せることを知らせる合図がマウスを乗せたときにしか出ていませんでした(指で触る端末には出ません)。',
      'マウスやトラックパッドのある端末では、見え方は 1px も変わりません。印が出るのは指で触る端末だけです。',
      'リンクだけが入った升を打ちたいときは、升の中の字の外(左右の余白)を押してください。字が升いっぱいのときは、表の右上の ▾ から csv の表に書き換えると、どこを押しても欄が開きます。',
    ],
  },
  {
    id: '2026-09-06-math',
    title: '数式が書けるようになりました($ で囲みます)',
    items: [
      '本文に $E = mc^2$ のように $ で囲むと、行の中の数式として出ます。行頭に $$ だけの行を置いて囲むと、中央寄せの数式になります。LaTeX の書き方です。',
      'お金と雛形の差し込みは数式になりません。$100 のように $ の直後が数字のとき、${date} や ${宛名} のように直後が { のとき、逆引用符で囲んだときは、そのままの字で出ます。それでも数式になるときは \\$ と書いてください。',
      '本文の上の書式パネルに「数式」が増えました。押すと $$ の囲みが入り、間にカーソルが来ます。',
      '式を書き間違えると、打った字がそのまま残り、横に小さく「式が読めません」と出ます。金額や ${…} のようにそもそも数式として読まれなかったものには、この断りは出ません。',
      ':::equation の中に書くと「式 1」の番号と説明が付き、[@id] で本文から参照できます。',
      '書き出すと、数式は打った字のまま出ます(閲覧用 HTML・持ち歩ける 1 枚・Word・PowerPoint・コピー)。読む側に数式の道具を積まないためです。',
    ],
  },
  {
    id: '2026-09-06-help-manual-first',
    title: 'ヘルプが開きやすくなり、選んでいるタブが見分けやすくなりました',
    items: [
      'ヘルプの並びが「マニュアル(目次つき)→ ショートカットキー → これまでのお知らせ」になりました。これまでは、お知らせ 11 件をスクロールしないとマニュアルに届きませんでした。',
      'マニュアルの上に目次が付きました。行を押すと、その節へ飛びます。版(pkc3 v…)も目次のすぐ上に出ます。',
      'これまでのお知らせは題名だけ並ぶようになりました。押すと中身が開きます。中身は落としていません。',
      '左の列で選んでいるタブ(一覧・フォルダ・アプリ・予定・連絡先)の下線が、本文と同じ色の線になりました。色が見分けにくい方にも、どれを選んでいるかが分かります。',
      'マニュアルの章立てを組み直しました。全体の 40% を占めていた「画面のならび」を解体し、作る・開く / 書く / 探す・数える / 整理する / 画面を組み替える など 16 章に割り直しています。',
      '章と節の番号(4-2 のような数字)と、見出しに入っていた日付を外しました。探すのは目次と「マニュアルの中を探す」なので、番号は要りません。説明の本文は 1 行も書き換えていません。',
    ],
  },
  {
    id: '2026-09-06-primary-action',
    title: 'その画面で「次に押す物」が 1 つだけ濃くなりました',
    items: [
      '読んでいるときは「編集」、一覧では「+ ノート」、編集中は「保存」の地と字が入れ替わります(明るい配色では濃い地に白い字、暗い配色では明るい地に暗い字)。話が進むボタンが一目で分かります。',
      '目立つのは、左の列で 1 つと中央で 1 つです(スマホ幅や小窓では 1 つ)。色は増やしていません ── 本文と同じ 2 色を入れ替えているだけです。',
      'ボタンの並びも大きさも動いていません。',
      '編集中は左の列の「+ ノート」が薄くなり、押せないことが見た目で分かるようになりました。マウスを乗せると理由が出ます。',
      '編集中に Ctrl + N を押したときも、打つ欄から離れていれば同じ理由が画面の下に出ます。これまでは押しても何も起きず、理由も出ませんでした。本文を打っている最中は今までどおり何も起きません。',
    ],
  },
  {
    id: '2026-09-06-table-convert-touch',
    title: '表の右上の「▾」から、持ち出しも形の書き換えもできるようになりました(指で触る端末でも)',
    items: [
      '表の右上に「▾」が増えました。押すと小窓が出て、表計算に貼る(TSV)・Markdown の表・HTML・CSV から選べます。csv の表にも ｜ の表にも出ます。',
      '「.csv で保存」を選ぶと、その表だけがファイルとして 1 つ落ちてきます(名前は「ノートの題名-日付.csv」)。Excel で開けます。これまでどおり「⧉」の 1 押しでも、表計算に貼れる形が入ります。',
      '同じ小窓の区切りの下から、表の形そのものを書き換えられます ──「本文を CSV の表に書き換える」。押すとその表だけが作り変わり、行と列を足す ＋ × と式が使えます。右クリックの無い端末でも「▾」から同じことができます。',
      'csv の表にすると、行と列を足す ＋ × と式(=SUM のような書き方)が使えます。Markdown の表に戻すと、よそへ貼りやすい形になります。',
      '戻すには同じ所をもう一度開いて反対の 1 行を押します。桁揃えは CSV に書けないので落ちます。式や升の中に改行がある表は、何行目の何列目かを出して断ります。',
      '引用(>)の中の表も、::: の囲みの中の表も書き換えられます。作り変えても引用や囲みの中に居たままで、外へは出てきません。戻すときも同じ所を開くだけです。',
    ],
  },
  {
    id: '2026-09-06-reading-column',
    title: '本文が画面の中央に置かれるようになりました(紙面の上限が掛かっているとき)',
    items: [
      '本文の左右の余白が同じになりました。1440 ピクセルの窓では、これまで右側だけに 269 ピクセルの空きが残っていました。',
      '表・図・コードも段落と同じ左端に揃います。文章と表が 1 本の列に見えます。そのぶん、横に広い表は左の余白のぶんだけ狭くなります。',
      '設定 → 紙面 で「フル HD」(上限なし)を選んでいるときは、いままでどおり幅いっぱいです。見え方は変わりません。',
    ],
  },
  {
    id: '2026-09-06-table-cell-fixes',
    title: '表のセルを押して、鍵だけで続けて打てるようになりました(｜ の表も csv の表も)',
    items: [
      '本文に | で書いた表のセルも、押すとその場で打てるようになりました。行や列を足す ＋ × は csv の表だけなので、表の右上の ▾ から「本文を CSV の表に書き換える」を押すと使えます。',
      'セルに打ったあと、Tab で右、Enter で下のセルへ、入力欄が開いたまま移ります。Shift を足すと逆(左・上)へ戻れます。打ち終わって閉じるときは Ctrl と Enter です(mac は コマンドキー)。',
      '日本語の変換中は、これらの鍵が効かなくなりました。前は変換を確定する Enter で下のセルへ飛び、続けて打った字がそのセルの中身を消していました。',
      '入力欄を開いても表の列幅が変わらなくなりました。前は隣のセルが縮み、その真ん中が「＋」ボタンになっていました(押すと欄が開かず、列が増えていました)。',
      '入力欄には、本文に書いたとおりの字が出ます。{{vars.名前}} や %%内緒%% を書いたセルも、その書き方のまま出ます。区切りの行(|---|)は押せません。',
      '引用(>)の中の表も csv の囲みも、セルを押して打てます。打った字は引用の中に入り、行頭の > は 1 文字も動きません。ほかに、打った直後に隣を押しても欄が消えない件など 6 つ直しました。',
    ],
  },
];

/**
 * ⚠ **落としたぶんの原本は `CHANGELOG.md` に在る**。⚠ 落ちるのは**自動ではない** ──
 * `NOTICE_KEEP_MAX` を超えたら**人が古い 1 件を消す**(手順は `.claude/skills/notice-writing/`)。
 * 突合は `tests/docs-parity.test.ts` の `DROPPED`(落とした題名の既知リスト)が持つ ──
 * 「登記表 + 落ちた分 = CHANGELOG」を**等値**で見るので、落としたのに書き忘れると CI が落ちる。
 *
 * 🔴 **ここに id の写しを持たない**(2026-08-29)。⚠ 以前はこのコメントが落とした id を
 *   並べていたが、**66 件で更新が止まり、実際は 148 件**だった ── 誰も読まない 3 つ目の
 *   台帳になっていた(CLAUDE.md §7「同じ値が複数の場所にある」)。
 *   🔑 **育つ一覧は、機械が突き合わせている所にだけ置く。**
 */
