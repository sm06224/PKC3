/**
 * 🔴 **お知らせの登記表**(P11 段⑤。user 指示 2026-08-07)。
 *
 * > 「**PKC3 にも PKC2 のようにお知らせポップアップをつけてください /
 * > お知らせ掲載内容は過去のお知らせとして、最大 10 件を…ヘルプ画面から
 * > 参照できるようにしてください**」
 *
 * ## PKC2 の失敗を繰り返さない(実測で確かめた 9 件のうち、ここに効く分)
 *
 * | PKC2 で起きたこと | ここでの対処 |
 * |---|---|
 * | 6 件あるのに**先頭 1 件しか読まれず**、残りを毎ビルド配っていた | **過去の一覧**を持ち、`recentNotices` が読む。読まれない entry を作らない |
 * | `date` field を宣言して**一度も読まなかった** | **field を持たない**。日付は id から引く(`noticeDate`)── 二重表現にしない |
 * | `textContent` で描くのに本文が `**強調**` で書かれ、**アスタリスクが見えていた** | **素のテキストと決める**。記法を書いたら test が落とす |
 * | 1 entry 22 項目・1 項目 200 字超の壁ができた | **上限を数で持つ**(件数・項目数・字数)。散文の規律にしない |
 * | 表示上限が複数箇所に散る | **切るのは `recentNotices` 1 か所だけ** |
 *
 * ⚠ **pure module**。browser API を使わない(既読は adapter 側が持つ)。
 */

/** お知らせ 1 件。⚠ **日付の field を持たない**(id が持っている)。 */
export interface Notice {
  /**
   * `YYYY-MM-DD-slug`。⚠ **既読の鍵**であり、**並び順の鍵**でもある。
   * 日付が前置きなので、文字列の降順がそのまま新しい順になる。
   */
  readonly id: string;
  readonly title: string;
  /** 本文。⚠ **素のテキスト**(markdown 記法を書かない ── 描画は textContent)。 */
  readonly items: readonly string[];
}

/**
 * 🔴 **ヘルプ画面に出す件数**(user 裁定 2026-08-07 Q1)。
 * ⚠ **切るのはここ 1 か所**。面ごとに `slice` を書くと、帯とヘルプで件数が食い違う。
 */
export const NOTICE_SHOW_MAX = 10;

/**
 * 🔴 **登記表に残す件数の上限**(裁定 Q1)。
 * 🔴 **表示上限と同じ数にする**(2026-08-29)。⚠ 直す前は **2 倍(20)**で、理由は
 *   「**原本は残す**」だった ── ところが**その役目はいま `CHANGELOG.md` が担っている**
 *   (2026-08-18 から)ので、11 件目より後ろは**原本でもなく画面にも出ない、
 *   配るだけの重り**になっていた(実測 10 件)。
 * 🔑 揃えると不変条件が 1 つ増える:**登記表 = 画面に出るもの**。
 *   ⚠ user から見える所は **1px も変わらない**(その 10 件はもともと出ていない)。
 *   落ちた分は今までどおり `CHANGELOG.md` が受ける。
 */
export const NOTICE_KEEP_MAX = 10;

/**
 * 🔴 **既読の集合に残す id の件数**(`adapter/platform/notice-store.ts`)。
 *
 * ⚠ **登記表の上限とは別物である。** 2026-08-29 まで `NOTICE_KEEP_MAX` を使い回して
 *   いたが、登記表を 20 → 10 へ下げた瞬間に**読んだお知らせが毎起動よみがえる**
 *   ところだった(着地前レビューが拾った。再現は
 *   `tests/adapter/notice-store.test.ts`「登記表から落ちた id が…」)。
 *
 * ## なぜ表示件数より大きくないといけないか
 *
 * 既読の集合には**登記表から落ちた id も残る**(user はそれを読んだので)。
 * だから必要な席は「いま出る 10 件」+「落ちた id のうち user が読んだ分」であり、
 * 表示件数と同じにすると**落ちた id が生きている id を押し出す**。
 * 押し出された 1 件は未読へ戻り、閉じても `same` 判定で書込が起きないので
 * **同じ 1 件が毎起動出続ける**(localStorage を消すまで止まらない)。
 *
 * 🔑 席の数だけに頼らず、`markSeen` は**登記表に在る id を先に残す**
 *   ── 席が足りなくなる形そのものを消してある(CLAUDE.md
 *   「衝突は、検出するより起こらなくするほうが強い」)。ここは**余裕**である。
 */
export const NOTICE_SEEN_MAX = NOTICE_SHOW_MAX * 2;

/** 1 entry の項目数の上限(PKC2 は 22 項目の壁ができた)。 */
export const NOTICE_ITEMS_MAX = 6;
/** 1 項目の字数(下限も置く ── 空の行が user に出るのを止める)。 */
export const NOTICE_ITEM_CHARS_MAX = 120;
export const NOTICE_ITEM_CHARS_MIN = 4;

/**
 * 🔴 **新しい順に並べて、上限まで切る**。
 * ⚠ **ここだけが切る**(上の `NOTICE_SHOW_MAX` の注記)。
 *
 * 🔴 **同じ日のものは、登記表に書いた順で出す**(2026-08-29 の動線レビュー 欠陥 5)。
 * ⚠ 直す前は **id 全体**で比べていたので、日付が同じ 14 件は
 *   **英語スラッグの綴り順**で並んでいた ── いちばん実害の大きい知らせ
 *   (「押しても何も起きなかったのが直った」)が **7 番目**に回り、
 *   user は「次へ」を **6 回**押さないと読めなかった。
 * 🔑 日付だけで比べれば、`Array.sort` は**安定**なので配列の順が残る
 *   ── 登記表は**先頭に足す**規約(`notice-writing` skill)なので、それが「新しい順」である。
 */
export function recentNotices(
  all: readonly Notice[],
  limit = NOTICE_SHOW_MAX,
): readonly Notice[] {
  return [...all]
    .sort((a, b) => {
      const [x, y] = [noticeDate(a.id), noticeDate(b.id)];
      return x < y ? 1 : x > y ? -1 : 0;
    })
    .slice(0, limit);
}

/**
 * まだ見ていないものを新しい順に返す。
 * ⚠ 既読は **id の集合**で渡す ── 「最後に閉じた 1 件」と比べると、
 *   旧ビルドへ戻ったときに**既読が巻き戻る**(PKC2 の F5)。
 */
export function unreadNotices(
  all: readonly Notice[],
  seen: readonly string[],
): readonly Notice[] {
  const seenSet = new Set(seen);
  return recentNotices(all).filter((n) => !seenSet.has(n.id));
}

/** id の先頭から日付を取り出す(`YYYY-MM-DD-slug` → `YYYY-MM-DD`)。 */
export function noticeDate(id: string): string {
  return /^(\d{4}-\d{2}-\d{2})/.exec(id)?.[1] ?? '';
}

/**
 * ── 登記表 ──────────────────────────────────────────────
 *
 * 🔴 **新しいものを先頭に足す。** 書式は `.claude/skills/notice-writing/SKILL.md`。
 *
 * ⚠ **書いた約束は取り消せない**(PKC2 は掲示済みの挙動が後の既定変更を縛った)。
 *   「これから〜します」ではなく「〜できるようになりました」だけを書く。
 * ⚠ **記法を書かない**(`**` / `` ` `` / `[]()`)── 素のテキストとして出る。
 * ⚠ **事実だけ書く。** 起きていないことを書くと、user は同じ画面で嘘に気づく。
 */
export const NOTICES: readonly Notice[] = [
  {
    id: '2026-09-03-phone-narrow',
    title: 'スマホの細い端末と、2 ペインでの整理が使えるようになりました',
    items: [
      '幅が 360 ピクセルより狭い端末では、画面の下に「この幅には対応していません」と 1 度だけ出るようになりました。画面は止めないので、そのまま書けます。',
      'スマホで「2 ペインで整理」を開くと、左右ではなく上下に並ぶようになりました。これまでは 1 枚が 184 ピクセルしかなく、いまどのフォルダに居るかの道すじが 1 ピクセルも見えませんでした。',
      'スマホでは「幅が足りないので畳みました」と出なくなりました。あの画面はもともと 1 枚ずつ出す作りなので、幅のせいで畳んでいるのではありません。',
    ],
  },
  {
    id: '2026-09-03-touch-devices',
    title: '指で触る端末で、マウスを乗せないと出なかったものが出るようになりました',
    items: [
      'コードの囲みと表の右上のコピーと、図の原文と描画の切替が、最初から見えるようになりました。これまではマウスを乗せたときだけ出ていたので、指では一度も見えませんでした。',
      '打つ欄の字が 16 ピクセルを下回らなくなりました。iPhone は小さい欄に触れると画面ごと勝手に拡大するためです。読む字の大きさは変えていません。',
      '右クリックのメニューは、項目に触れた瞬間に下の説明が出るようになりました。指には「乗せる」がないためです。',
      '走っているタイマー・収録・予定の帯のボタンが、指で押せる丈になりました。これまでは 26 ピクセルで、隣を押していました。',
      '見えていなくても押せる状態だったので、囲みや図の右上に触ると、黙ってコピーされたり絵が原文の字に変わったりしていました。もう起きません。',
      '走っているタイマーや予定が並びきらないときは、折り返して全部見えるようになりました。これまでは横に流れていて、2 本目や 3 本目の「止める」が画面の外にありました。',
    ],
  },
  {
    id: '2026-09-02-attach-into-note',
    title: '「添付」で取り込んだファイルが、読んでいたノートの本文に入るようになりました',
    items: [
      '「添付」でファイルを取り込むと、読んでいたノートの本文のいちばん下に 1 行入るようになりました。画面もそのノートのままです。',
      'これまでは添付だけが独立したノートになり、画面もそちらへ移って読んでいたノートが見えなくなっていました。録音・画面録画は前から本文に入っていました。',
      '画像は本文に絵が出る形で、それ以外はリンクの形で入ります。追記できない種類を開いているときは、添付にはなりますが本文には入りません。',
    ],
  },
  {
    id: '2026-09-02-phone-return',
    title: 'スマホで一覧へ戻っても、読んでいたノートが開いたままになりました',
    items: [
      '「← 一覧」を押しても、読んでいたノートは開いたままです。一覧のいちばん上に「ノートへ →」と題名が出るので、押せば読んでいた所へ帰れます。',
      'これまでは一覧へ戻ると選んでいたノートが外れ、戻る道はもう一度探すしかありませんでした。一覧で同じ行を押しても帰れます。',
      'スマホでは「⋯」の並びを変えました。毎日使う添付・録音・画面録画・時間を計るが上に来ます。削除はこれまでどおりいちばん下です。',
      'その 4 つは、ノートを開いていないと押した時点で断ります。これまで録音と画面録画は録り終わってから断られていました。パソコンでも同じです。',
      'スマホの幅では、起動直後のお知らせを画面いっぱいに出します。読み終えたら「閉じる」、これから要らなければ「今後は出さない」を押してください。',
    ],
  },
  {
    id: '2026-09-02-phone-layout',
    title: 'スマホの幅では、一覧・本文・情報を 1 枚ずつ出すようになりました',
    items: [
      '窓の幅が 720 ピクセル以下になると、一覧・本文・情報のどれか 1 枚だけを画面いっぱいに出します。パソコンで窓を細くしたときも同じで、広げれば元に戻ります。',
      '本文の上に帯が 1 本出ます。左から「← 一覧」、ノートの題名、「情報」、「⋯」です。「情報」を押すと画面全体がタグ・目次・関係・書き出すに変わり、「← ノート」で戻ります。',
      '「⋯」からは、行を右クリックしたときと同じ操作に加えて、添付・録音・画面録画・時間を計る・操作を探すが押せます。これまで左の列にしか無かったものです。',
      '編集中に「← 一覧」や「情報」を押すと、保存するか取り消してから戻ってくださいと画面の下に出ます。',
      'これまで狭い画面では 3 つが縦に積まれ、起動直後のお知らせを開いたままだと本文が数十ピクセルまで潰れていました。1 枚ずつ出すので潰れません。',
      '一覧や情報の列を畳んだままスマホの幅で開いても、一覧は出ます。畳んだ設定は消えないので、窓を広げれば元どおり効きます。',
    ],
  },
  {
    id: '2026-09-02-stack-bar',
    title: '横に留めたノートが「スタック」になり、本文の上の帯から降ろせるようになりました',
    items: [
      '本文の上に帯が出て、載せたノートが札で並びます。新しく載せた物がいちばん左(本文のすぐ隣)に来て、それまで隣に在った物は右へずれます。',
      '札の名前を押すと、そのノートが一番上に上がって本文のすぐ隣に出ます。札の × で降ろせます。',
      '画面が狭くて枠が畳まれても、帯は残るので降ろせます。これまでは外す口が画面のどこにも無くなり、窓を広げるまで戻せませんでした。',
      '載せられるのは 20 件までになりました。横に出るのはこれまでどおり 3 件までで、出ないぶんは帯の札で残ります。',
      '枠の上の字を「× 降ろす」に変え、「← 左で開く」を足しました。その枠のノートを主の枠に出せます。',
      '古い版の PKC3 を同じ端末で開くと、載せた並びは先頭の 3 件に切り詰められます。',
    ],
  },
  {
    id: '2026-09-02-tag-enter',
    title: 'タグを打つ 3 つの欄が、どれも同じ作法になりました',
    items: [
      'まとめてタグを付ける帯と、スマートフォルダの条件の欄でも、打って Enter で足せます。これまでは押しても何も起きず、理由も出ませんでした。',
      'スマートフォルダの条件の欄にも、すでに使っているタグが候補に出ます。ここは 1 文字違うと 1 件も集まらないので、候補から選べます。',
      '日本語を変換している最中の Enter では足しません(変換の確定と取り違えないためです)。',
    ],
  },
  {
    id: '2026-09-02-tag-limit-reason',
    title: 'タグが付かなかったとき、理由とタグの名前が出るようになりました',
    items: [
      '選んだノートにまとめてタグを付けたとき、1 つのノートに付けられる上限(32 個)で付かなかったものを、これまでは「既に付いていました」と言っていました。付いていないのに付いていると言う字だったので、別の字にしました。',
      'スマートフォルダの条件にタグを足して上限(8 つ)に当たったときは、どのタグが入らなかったのかが名前で出ます。これまでは名前が出ず、打った字も消えていたので打ち直しになっていました。',
    ],
  },
  {
    id: '2026-09-02-manual-window-front',
    title: 'マニュアルのウィンドウを入れ替えた回も、ウィンドウが前に出るようになりました',
    items: [
      'PKC3 が新しくなったあとにマニュアルのボタンを押すと、ウィンドウを入れ替えましたと出るのに、そのウィンドウが後ろに居たままのことがありました。前に出ます。',
      'ブラウザの都合で前に出せないときのために、その知らせにも「見えないときは、ウィンドウを切り替えてください」を足しました。',
    ],
  },
  {
    id: '2026-09-02-tags-two-lines',
    title: 'タグの行を 2 行書いたときに、消えたことが分かるようになりました',
    items: [
      '本文のいちばん上の tags: の行を 2 行に分けて書くと、これまでは後の行だけが読まれ、先に書いたタグは何も出ないまま消えていました。',
      'いまは右の情報ペインの「タグ」の行にマウスを乗せると、無視されている鍵の名前が出ます。',
      'tags: の下に箇条書きでタグを並べたノートに、あとからタグを足したときの直しも入れました。これまでは元の箇条書きが本文の情報の中に残り、あとで消したはずのタグが戻ることがありました。',
    ],
  },
];

/**
 * ⚠ **落としたぶんの原本は `CHANGELOG.md` に在る**。⚠ 落ちるのは**自動ではない** ──
 * `NOTICE_KEEP_MAX` を超えたら**人が古い 1 件を消す**(手順は `.claude/skills/notice-writing/`)。
 * 突合は `tests/docs-parity.test.ts` の `DROPPED`(落とした題名の既知リスト)が持つ ──
 * 「登記表 + 落ちた分 = CHANGELOG」を**等値**で見るので、落としたのに書き忘れると CI が落ちる。
 *
 * 🔴 **ここに id の写しを持たない**(2026-08-29)。⚠ 以前はこのコメントが落とした id を
 *   並べていたが、**66 件で更新が止まり、実際は 148 件**だった ── 誰も読まない 3 つ目の
 *   台帳になっていた(CLAUDE.md §7「同じ値が複数の場所にある」)。
 *   🔑 **育つ一覧は、機械が突き合わせている所にだけ置く。**
 */
