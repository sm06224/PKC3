/**
 * 🔴 **お知らせの登記表**(P11 段⑤。user 指示 2026-08-07)。
 *
 * > 「**PKC3 にも PKC2 のようにお知らせポップアップをつけてください /
 * > お知らせ掲載内容は過去のお知らせとして、最大 10 件を…ヘルプ画面から
 * > 参照できるようにしてください**」
 *
 * ## PKC2 の失敗を繰り返さない(実測で確かめた 9 件のうち、ここに効く分)
 *
 * | PKC2 で起きたこと | ここでの対処 |
 * |---|---|
 * | 6 件あるのに**先頭 1 件しか読まれず**、残りを毎ビルド配っていた | **過去の一覧**を持ち、`recentNotices` が読む。読まれない entry を作らない |
 * | `date` field を宣言して**一度も読まなかった** | **field を持たない**。日付は id から引く(`noticeDate`)── 二重表現にしない |
 * | `textContent` で描くのに本文が `**強調**` で書かれ、**アスタリスクが見えていた** | **素のテキストと決める**。記法を書いたら test が落とす |
 * | 1 entry 22 項目・1 項目 200 字超の壁ができた | **上限を数で持つ**(件数・項目数・字数)。散文の規律にしない |
 * | 表示上限が複数箇所に散る | **切るのは `recentNotices` 1 か所だけ** |
 *
 * ⚠ **pure module**。browser API を使わない(既読は adapter 側が持つ)。
 */

/** お知らせ 1 件。⚠ **日付の field を持たない**(id が持っている)。 */
export interface Notice {
  /**
   * `YYYY-MM-DD-slug`。⚠ **既読の鍵**であり、**並び順の鍵**でもある。
   * 日付が前置きなので、文字列の降順がそのまま新しい順になる。
   */
  readonly id: string;
  readonly title: string;
  /** 本文。⚠ **素のテキスト**(markdown 記法を書かない ── 描画は textContent)。 */
  readonly items: readonly string[];
}

/**
 * 🔴 **ヘルプ画面に出す件数**(user 裁定 2026-08-07 Q1)。
 * ⚠ **切るのはここ 1 か所**。面ごとに `slice` を書くと、帯とヘルプで件数が食い違う。
 */
export const NOTICE_SHOW_MAX = 10;

/**
 * 🔴 **登記表に残す件数の上限**(裁定 Q1)。
 * 🔴 **表示上限と同じ数にする**(2026-08-29)。⚠ 直す前は **2 倍(20)**で、理由は
 *   「**原本は残す**」だった ── ところが**その役目はいま `CHANGELOG.md` が担っている**
 *   (2026-08-18 から)ので、11 件目より後ろは**原本でもなく画面にも出ない、
 *   配るだけの重り**になっていた(実測 10 件)。
 * 🔑 揃えると不変条件が 1 つ増える:**登記表 = 画面に出るもの**。
 *   ⚠ user から見える所は **1px も変わらない**(その 10 件はもともと出ていない)。
 *   落ちた分は今までどおり `CHANGELOG.md` が受ける。
 */
export const NOTICE_KEEP_MAX = 10;

/**
 * 🔴 **既読の集合に残す id の件数**(`adapter/platform/notice-store.ts`)。
 *
 * ⚠ **登記表の上限とは別物である。** 2026-08-29 まで `NOTICE_KEEP_MAX` を使い回して
 *   いたが、登記表を 20 → 10 へ下げた瞬間に**読んだお知らせが毎起動よみがえる**
 *   ところだった(着地前レビューが拾った。再現は
 *   `tests/adapter/notice-store.test.ts`「登記表から落ちた id が…」)。
 *
 * ## なぜ表示件数より大きくないといけないか
 *
 * 既読の集合には**登記表から落ちた id も残る**(user はそれを読んだので)。
 * だから必要な席は「いま出る 10 件」+「落ちた id のうち user が読んだ分」であり、
 * 表示件数と同じにすると**落ちた id が生きている id を押し出す**。
 * 押し出された 1 件は未読へ戻り、閉じても `same` 判定で書込が起きないので
 * **同じ 1 件が毎起動出続ける**(localStorage を消すまで止まらない)。
 *
 * 🔑 席の数だけに頼らず、`markSeen` は**登記表に在る id を先に残す**
 *   ── 席が足りなくなる形そのものを消してある(CLAUDE.md
 *   「衝突は、検出するより起こらなくするほうが強い」)。ここは**余裕**である。
 */
export const NOTICE_SEEN_MAX = NOTICE_SHOW_MAX * 2;

/** 1 entry の項目数の上限(PKC2 は 22 項目の壁ができた)。 */
export const NOTICE_ITEMS_MAX = 6;
/** 1 項目の字数(下限も置く ── 空の行が user に出るのを止める)。 */
export const NOTICE_ITEM_CHARS_MAX = 120;
export const NOTICE_ITEM_CHARS_MIN = 4;

/**
 * 🔴 **新しい順に並べて、上限まで切る**。
 * ⚠ **ここだけが切る**(上の `NOTICE_SHOW_MAX` の注記)。
 *
 * 🔴 **同じ日のものは、登記表に書いた順で出す**(2026-08-29 の動線レビュー 欠陥 5)。
 * ⚠ 直す前は **id 全体**で比べていたので、日付が同じ 14 件は
 *   **英語スラッグの綴り順**で並んでいた ── いちばん実害の大きい知らせ
 *   (「押しても何も起きなかったのが直った」)が **7 番目**に回り、
 *   user は「次へ」を **6 回**押さないと読めなかった。
 * 🔑 日付だけで比べれば、`Array.sort` は**安定**なので配列の順が残る
 *   ── 登記表は**先頭に足す**規約(`notice-writing` skill)なので、それが「新しい順」である。
 */
export function recentNotices(
  all: readonly Notice[],
  limit = NOTICE_SHOW_MAX,
): readonly Notice[] {
  return [...all]
    .sort((a, b) => {
      const [x, y] = [noticeDate(a.id), noticeDate(b.id)];
      return x < y ? 1 : x > y ? -1 : 0;
    })
    .slice(0, limit);
}

/**
 * まだ見ていないものを新しい順に返す。
 * ⚠ 既読は **id の集合**で渡す ── 「最後に閉じた 1 件」と比べると、
 *   旧ビルドへ戻ったときに**既読が巻き戻る**(PKC2 の F5)。
 */
export function unreadNotices(
  all: readonly Notice[],
  seen: readonly string[],
): readonly Notice[] {
  const seenSet = new Set(seen);
  return recentNotices(all).filter((n) => !seenSet.has(n.id));
}

/** id の先頭から日付を取り出す(`YYYY-MM-DD-slug` → `YYYY-MM-DD`)。 */
export function noticeDate(id: string): string {
  return /^(\d{4}-\d{2}-\d{2})/.exec(id)?.[1] ?? '';
}

/**
 * ── 登記表 ──────────────────────────────────────────────
 *
 * 🔴 **新しいものを先頭に足す。** 書式は `.claude/skills/notice-writing/SKILL.md`。
 *
 * ⚠ **書いた約束は取り消せない**(PKC2 は掲示済みの挙動が後の既定変更を縛った)。
 *   「これから〜します」ではなく「〜できるようになりました」だけを書く。
 * ⚠ **記法を書かない**(`**` / `` ` `` / `[]()`)── 素のテキストとして出る。
 * ⚠ **事実だけ書く。** 起きていないことを書くと、user は同じ画面で嘘に気づく。
 */
export const NOTICES: readonly Notice[] = [
  {
    id: '2026-09-05-editing-buttons-and-labels',
    title: '編集中に押せないボタンが薄く見え、保存・キャンセルの字と書式ボタンの説明が揃いました',
    items: [
      '本文を編集している間、右の列の操作のボタンが薄くなり、その上に「編集中は使えません(保存するか、キャンセルすると戻ります)」と出ます。これまでは押せないのに見た目が同じでした。',
      '追記欄の場所に出る編集の出口が、中央の帯と同じ「保存」「キャンセル」の字になりました。これまでは「保存して解放」「編集を破棄」で、別の操作に見えました。押した結果は同じです。',
      '書式バーのボタンにマウスを乗せると、何が起きるかの説明が出ます。近道キーがあるもの(太字・斜体・リンク)は、いまの割当も一緒に出ます。',
      '書式バーに「番号」が 2 つ並んでいたので、右のほうを「番号を振り直す」にしました。左の「番号」は行を番号付きリストにするボタンで、動きは変わりません。',
    ],
  },
  {
    id: '2026-09-05-local-dates',
    title: 'ノートの作成日・更新日が、お使いの端末の日付で出るようになりました',
    items: [
      '深夜から朝に書いたノートの作成日・更新日が、書いた日(お使いの端末の日付)で出ます。これまでは世界標準時の日付でした。',
      '日本では、朝 9 時までに書いたものが前日の日付で出ていました。右の列の「作成」「更新」と、一覧の右端の日付が直ります。',
      '設定や連絡先を書き出したファイルの名前に入る日付も、端末の日付になりました。バックアップの名前と同じ日になります。',
    ],
  },
  {
    id: '2026-09-05-note-window-polish',
    title: 'ノートを開く別のウィンドウ(小窓)が、追記欄つきで開き、書いている最中でも開けるようになりました',
    items: [
      'ノートを別のウィンドウで開いた窓を、この版から「小窓」と呼びます(板の上に置く「付箋」と区別するため)。ボタンの字は「別のウィンドウで開く」に揃え、メニューの並びは 参照をコピー・素の Markdown・別のウィンドウで開く、の順です。',
      '小窓は、本体で追記欄を畳んでいても追記欄が出た状態で開きます。開いた直後のカーソルは追記欄にあるので、そのまま書き始められます。小窓で畳んでも本体の畳み方は変わりません。',
      '本文を編集している最中でも、一覧の行を右クリックすると「別のウィンドウで開く」だけのメニューが出ます。下書きも中央のノートも動きません。',
      '小窓で目次や脚注を押して見出しへ飛んでも、窓の題名とアドレスはそのノートのままです。これまでは押した瞬間に題名が「PKC3」に戻り、読み込み直すとノートが出ませんでした。',
      'すでに開いているノートをもう一度開こうとしたときの断り文に、その窓の題名が入ります。小窓を細くしすぎたときの断り書きは「ウィンドウを広げると直ります」になりました。',
      '「操作を探す」で「ウィンドウ」「小窓」「付箋」と打つと「別のウィンドウで開く」が出ます。ノートを行き来しても読んでいた場所から出るようになりました(その窓を開いている間だけ憶えます)。',
    ],
  },
  {
    id: '2026-09-05-apps-in-windows',
    title: '予定表と連絡先が、組み込みアプリの別ウィンドウで開けるようになりました',
    items: [
      '一覧の「アプリ」に「予定表」と「連絡先」のタイルが並びます。押すと別のウィンドウが開いて、そこに予定表(または連絡先)が画面いっぱいに出ます。本文はもとのウィンドウに残ります。',
      '左の列の「予定」「連絡先」のタブはそのままです。どちらからでも同じものを見られます。ウィンドウがブラウザに止められたときは、左の列のタブで開きます。',
      '連絡先に「足す」欄ができました。名前と電話・メール・所属を打って「足す」を押すと、連絡先のノートができて一覧に並びます。これまでは名前のノートを開いて本文を手で書くしかありませんでした。',
      'ウィンドウで開いた予定表や連絡先から書き込んだものは、本体のウィンドウにもそのまま届きます。',
    ],
  },
  {
    id: '2026-09-05-phone-dual-polish',
    title: 'スマホの「2 ペインで整理」で、行き先と居場所が読め、長押しで印を足せるようになりました',
    items: [
      '切り替えのボタンに相手のペインが開いているフォルダの名前が入ります。「右のペインへ → 2026 請求」のように、押す前に行き先が読めます。',
      'タブの帯の左端に「左」「右」の 1 文字が出て、いまどちらのペインを見ているかが分かります。',
      'もう片方のペインに印が残っているときは、「左のペインに 1 件の印が残っています」と出ます。そこでコピー・移すを押しても、その印は動きません。',
      '行を長押しすると印を足せます。指だけの端末で 2 件以上を選べるようになりました。押した 1 件だけを選ぶ動きは変わりません。',
      '幅が狭いときの断り書きは、「OK」を押すと次に開いても出ません。設定の「表示」にある「狭い画面の断り書き」で戻せます。',
      'スマホを横に倒したときも(高さ 480 ピクセル以下)、縦のときと同じ 1 枚ずつの画面になります。これまでは横向きだと「操作を探す」が押せませんでした。',
    ],
  },
  {
    id: '2026-09-05-attach-polish',
    title: '添付を取り込んだとき、どこに増えたかが分かり、編集中でも預かるようになりました',
    items: [
      '本文に入れられない種類(フォルダなど)を開いたまま添付すると、「開いているのは『フォルダ』なので本文には入れていません。本文に入れられるのはノートとログだけです」と出て、隣の「開く」でその添付へ行けます。',
      '編集中に「添付」を押しても断りません。預かって、編集を終えたら本文に入ります。録音と同じ動きです。',
      '3 枚まとめて入れた添付は、「元に戻す」1 回でまとめて戻ります。これまでは最後の 1 行しか戻りませんでした。',
      '添付を作っても、「探す」に打った字と種類の札はそのまま残ります。これまでは添付するたびに一覧が全件に戻っていました。',
      '入れた後の知らせが「3 件を本文に入れました(3枚目.jpg ほか)」「「猫.png」を本文のいちばん下に入れました」のように、件数と場所を言うようになりました。',
    ],
  },
  {
    id: '2026-09-05-blocks-and-boards',
    title: '本文の右クリックから、章や囲みや板をまるごとコピーしたり、板を置いたり消したりできるようになりました',
    items: [
      '見出しを右クリックすると「この章をコピー」が出ます。その見出しから次の同じ大きさの見出しの手前までの原文が、そのまま入ります。囲み(コロン 3 つの塊)や板の上では「この塊をコピー」「この板をコピー」です。',
      '別の PKC へ貼ったときは、添付の絵は出ません。同じ PKC の別のノートなら出ます。',
      '本文の何もない所を右クリックすると「ここに板を置く」が出て、押した位置に板が置かれます。板の右クリックには「前へ出す」「この板を消す」が出ます(消す前に確認が出ます)。',
      '板の右下の角を掴むと大きさを変えられます。持ち手に焦点があるときは矢印キーでも動かせます(Shift を押しながらだと 10 ピクセルずつ)。',
      '見出しの右クリックの項目には、右に薄く近道が出ます(「ここから編集する」には Ctrl とクリック、「ここに追記する」には Alt とクリック)。「書き戻す」の説明に「元の内容は戻せません」が入りました。',
    ],
  },
  {
    id: '2026-09-05-manual-window-polish',
    title: 'マニュアルのウィンドウが、本体と同じ字で、持ち歩ける 1 枚からも同じように開けるようになりました',
    items: [
      'マニュアルのウィンドウの字が、本体と同じ書体・大きさ・行間になりました。題名は「マニュアル — PKC3」で、他のウィンドウと同じ並びです。',
      '文字の大きさで「標準」を選ぶと、選んでいない状態に戻ります。マニュアルのウィンドウの字も元に戻ります。',
      '節の印が見出しの字から作られるようになりました。見出しが増えた版でも、控えたリンクが隣の節を指すことはありません。ブラウザのブックマークに登録すると、その節ごと控えられます。',
      '持ち歩ける 1 枚(書き出した 1 個の HTML)でも、マニュアルが同じウィンドウで開き、読み込み直しても白紙になりません。選んだ配色も効きます。',
    ],
  },
  {
    id: '2026-09-05-office-macros-keep',
    title: 'Office で書いたマクロが、ウィンドウを閉じても残るようになりました',
    items: [
      'Office のウィンドウで書いたマクロは、この端末のブラウザに残ります。次に開いても「マクロの実行」の一覧に並びます。これまでは閉じると消えていました。',
      '他の端末には行きません。「Office の設定を初期化」を押すと、設定と一緒にマクロも消えます。',
    ],
  },
  {
    id: '2026-09-05-diagram-picker',
    title: '書式バーの「図」を押すと、5 種類の図から選べるようになりました',
    items: [
      '「図」を押すと、フローチャート・クラス図・シーケンス図・状態遷移図・ER 図の一覧が出て、選んだ雛形が入ります。これまではフローチャートの 2 行が必ず入っていました。',
      '一覧は矢印キーでも動き、外を押すと何も入れずに閉じます。「雛形を入れる」の一覧も同じ動きになりました。',
    ],
  },
];

/**
 * ⚠ **落としたぶんの原本は `CHANGELOG.md` に在る**。⚠ 落ちるのは**自動ではない** ──
 * `NOTICE_KEEP_MAX` を超えたら**人が古い 1 件を消す**(手順は `.claude/skills/notice-writing/`)。
 * 突合は `tests/docs-parity.test.ts` の `DROPPED`(落とした題名の既知リスト)が持つ ──
 * 「登記表 + 落ちた分 = CHANGELOG」を**等値**で見るので、落としたのに書き忘れると CI が落ちる。
 *
 * 🔴 **ここに id の写しを持たない**(2026-08-29)。⚠ 以前はこのコメントが落とした id を
 *   並べていたが、**66 件で更新が止まり、実際は 148 件**だった ── 誰も読まない 3 つ目の
 *   台帳になっていた(CLAUDE.md §7「同じ値が複数の場所にある」)。
 *   🔑 **育つ一覧は、機械が突き合わせている所にだけ置く。**
 */
