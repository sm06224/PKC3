/**
 * 🔴 **お知らせの登記表**(P11 段⑤。user 指示 2026-08-07)。
 *
 * > 「**PKC3 にも PKC2 のようにお知らせポップアップをつけてください /
 * > お知らせ掲載内容は過去のお知らせとして、最大 10 件を…ヘルプ画面から
 * > 参照できるようにしてください**」
 *
 * ## PKC2 の失敗を繰り返さない(実測で確かめた 9 件のうち、ここに効く分)
 *
 * | PKC2 で起きたこと | ここでの対処 |
 * |---|---|
 * | 6 件あるのに**先頭 1 件しか読まれず**、残りを毎ビルド配っていた | **過去の一覧**を持ち、`recentNotices` が読む。読まれない entry を作らない |
 * | `date` field を宣言して**一度も読まなかった** | **field を持たない**。日付は id から引く(`noticeDate`)── 二重表現にしない |
 * | `textContent` で描くのに本文が `**強調**` で書かれ、**アスタリスクが見えていた** | **素のテキストと決める**。記法を書いたら test が落とす |
 * | 1 entry 22 項目・1 項目 200 字超の壁ができた | **上限を数で持つ**(件数・項目数・字数)。散文の規律にしない |
 * | 表示上限が複数箇所に散る | **切るのは `recentNotices` 1 か所だけ** |
 *
 * ⚠ **pure module**。browser API を使わない(既読は adapter 側が持つ)。
 */

/** お知らせ 1 件。⚠ **日付の field を持たない**(id が持っている)。 */
export interface Notice {
  /**
   * `YYYY-MM-DD-slug`。⚠ **既読の鍵**であり、**並び順の鍵**でもある。
   * 日付が前置きなので、文字列の降順がそのまま新しい順になる。
   */
  readonly id: string;
  readonly title: string;
  /** 本文。⚠ **素のテキスト**(markdown 記法を書かない ── 描画は textContent)。 */
  readonly items: readonly string[];
}

/**
 * 🔴 **ヘルプ画面に出す件数**(user 裁定 2026-08-07 Q1)。
 * ⚠ **切るのはここ 1 か所**。面ごとに `slice` を書くと、帯とヘルプで件数が食い違う。
 */
export const NOTICE_SHOW_MAX = 10;

/**
 * 🔴 **登記表に残す件数の上限**(裁定 Q1)。
 * 🔴 **表示上限と同じ数にする**(2026-08-29)。⚠ 直す前は **2 倍(20)**で、理由は
 *   「**原本は残す**」だった ── ところが**その役目はいま `CHANGELOG.md` が担っている**
 *   (2026-08-18 から)ので、11 件目より後ろは**原本でもなく画面にも出ない、
 *   配るだけの重り**になっていた(実測 10 件)。
 * 🔑 揃えると不変条件が 1 つ増える:**登記表 = 画面に出るもの**。
 *   ⚠ user から見える所は **1px も変わらない**(その 10 件はもともと出ていない)。
 *   落ちた分は今までどおり `CHANGELOG.md` が受ける。
 */
export const NOTICE_KEEP_MAX = 10;

/**
 * 🔴 **既読の集合に残す id の件数**(`adapter/platform/notice-store.ts`)。
 *
 * ⚠ **登記表の上限とは別物である。** 2026-08-29 まで `NOTICE_KEEP_MAX` を使い回して
 *   いたが、登記表を 20 → 10 へ下げた瞬間に**読んだお知らせが毎起動よみがえる**
 *   ところだった(着地前レビューが拾った。再現は
 *   `tests/adapter/notice-store.test.ts`「登記表から落ちた id が…」)。
 *
 * ## なぜ表示件数より大きくないといけないか
 *
 * 既読の集合には**登記表から落ちた id も残る**(user はそれを読んだので)。
 * だから必要な席は「いま出る 10 件」+「落ちた id のうち user が読んだ分」であり、
 * 表示件数と同じにすると**落ちた id が生きている id を押し出す**。
 * 押し出された 1 件は未読へ戻り、閉じても `same` 判定で書込が起きないので
 * **同じ 1 件が毎起動出続ける**(localStorage を消すまで止まらない)。
 *
 * 🔑 席の数だけに頼らず、`markSeen` は**登記表に在る id を先に残す**
 *   ── 席が足りなくなる形そのものを消してある(CLAUDE.md
 *   「衝突は、検出するより起こらなくするほうが強い」)。ここは**余裕**である。
 */
export const NOTICE_SEEN_MAX = NOTICE_SHOW_MAX * 2;

/** 1 entry の項目数の上限(PKC2 は 22 項目の壁ができた)。 */
export const NOTICE_ITEMS_MAX = 6;
/** 1 項目の字数(下限も置く ── 空の行が user に出るのを止める)。 */
export const NOTICE_ITEM_CHARS_MAX = 120;
export const NOTICE_ITEM_CHARS_MIN = 4;

/**
 * 🔴 **新しい順に並べて、上限まで切る**。
 * ⚠ **ここだけが切る**(上の `NOTICE_SHOW_MAX` の注記)。
 *
 * 🔴 **同じ日のものは、登記表に書いた順で出す**(2026-08-29 の動線レビュー 欠陥 5)。
 * ⚠ 直す前は **id 全体**で比べていたので、日付が同じ 14 件は
 *   **英語スラッグの綴り順**で並んでいた ── いちばん実害の大きい知らせ
 *   (「押しても何も起きなかったのが直った」)が **7 番目**に回り、
 *   user は「次へ」を **6 回**押さないと読めなかった。
 * 🔑 日付だけで比べれば、`Array.sort` は**安定**なので配列の順が残る
 *   ── 登記表は**先頭に足す**規約(`notice-writing` skill)なので、それが「新しい順」である。
 */
export function recentNotices(
  all: readonly Notice[],
  limit = NOTICE_SHOW_MAX,
): readonly Notice[] {
  return [...all]
    .sort((a, b) => {
      const [x, y] = [noticeDate(a.id), noticeDate(b.id)];
      return x < y ? 1 : x > y ? -1 : 0;
    })
    .slice(0, limit);
}

/**
 * まだ見ていないものを新しい順に返す。
 * ⚠ 既読は **id の集合**で渡す ── 「最後に閉じた 1 件」と比べると、
 *   旧ビルドへ戻ったときに**既読が巻き戻る**(PKC2 の F5)。
 */
export function unreadNotices(
  all: readonly Notice[],
  seen: readonly string[],
): readonly Notice[] {
  const seenSet = new Set(seen);
  return recentNotices(all).filter((n) => !seenSet.has(n.id));
}

/** id の先頭から日付を取り出す(`YYYY-MM-DD-slug` → `YYYY-MM-DD`)。 */
export function noticeDate(id: string): string {
  return /^(\d{4}-\d{2}-\d{2})/.exec(id)?.[1] ?? '';
}

/**
 * ── 登記表 ──────────────────────────────────────────────
 *
 * 🔴 **新しいものを先頭に足す。** 書式は `.claude/skills/notice-writing/SKILL.md`。
 *
 * ⚠ **書いた約束は取り消せない**(PKC2 は掲示済みの挙動が後の既定変更を縛った)。
 *   「これから〜します」ではなく「〜できるようになりました」だけを書く。
 * ⚠ **記法を書かない**(`**` / `` ` `` / `[]()`)── 素のテキストとして出る。
 * ⚠ **事実だけ書く。** 起きていないことを書くと、user は同じ画面で嘘に気づく。
 */
export const NOTICES: readonly Notice[] = [
  {
    id: '2026-09-02-menu-hint-box',
    title: '右クリックのメニューの下に、指している項目の説明が出るようになりました',
    items: [
      '一覧やフォルダの行を右クリックして出るメニューのいちばん下に、マウスを乗せている項目の説明が 2 行で出ます。待たずに読めます。',
      'キーで項目を移しても同じ説明が出ます。マウスを持たない人にも届きます。',
      'これまでのマウスを乗せて 1 秒待つと出る小さな箱はやめました。下の項目に重なるためです。',
      '説明の文は 2 行に収まるように短くしました。右の情報ペインの説明も同じ文です。',
      '見出しや本文を右クリックしたメニューには、まだ説明はありません。',
    ],
  },
  {
    id: '2026-09-02-append-opens',
    title: '追記欄を畳んでいても、「ここに追記する」でそのまま打てるようになりました',
    items: [
      '見出しを右クリックして「ここに追記する」を押すと、追記欄を畳んであっても開いて、打つ欄に文字カーソルが入ります。これまでは入り先が変わるだけで、打つ所が画面に出ませんでした。',
      'Alt を押しながら本文をクリックしたときも同じです。',
      '開いたときは画面の下に「追記欄を開きました」と出ます。左右の列の畳み方は変わりません。',
    ],
  },
  {
    id: '2026-09-02-split-restore',
    title: '横に留めたノートが、開き直しても留まったままになりました',
    items: [
      '本文を右クリックして「このノートを横に留める」で並べた枠は、PKC3 を閉じて開き直しても同じ並びで出るはずでしたが、実際には開き直すたびに外れていました。',
      '開き直しても留まったまま出るようになりました。外して閉じたときは、外れたままで開きます。',
      '憶えているのはこの端末のブラウザです。別の端末で開いたときは、その端末で留めた並びになります。',
      'マニュアルだけのウィンドウで、設定を何も変えずにもう一度ボタンを押すと、文字がわずかに小さくなることがありました。文字の大きさを選んでいない場合に起きていました。動かなくなりました。',
    ],
  },
  {
    id: '2026-09-02-manual-page',
    title: 'マニュアルのウィンドウが、再読み込みしても消えなくなりました',
    items: [
      'マニュアルだけのウィンドウで F5 を押しても、白紙にならずにそのまま読み直せます。これまでは白紙になり、閉じて開き直す必要がありました。',
      '設定で選んだ配色(GitHub、Solarized、Dracula など)が、マニュアルのウィンドウにも効きます。これまでは明るい・暗いの 2 つだけでした。',
      'ウィンドウの中身が manual.html という 1 枚のページになったので、ブックマークできます。目次を押すと URL の末尾にその節の印が付きます。',
      '設定で選んだ文字の大きさも、この窓に効きます。窓で Ctrl + P を押すと、本文が全部の頁に出ます。',
      '設定で配色や文字の大きさを変えたあとは、もう一度ボタンを押すと、読んでいた所のまま窓が新しい設定になります。',
      '持ち歩ける 1 枚(書き出した 1 個の HTML)で開いた窓だけは、これまでどおりです。F5 で白紙になったら、閉じてもう一度押してください。',
    ],
  },
  {
    id: '2026-08-31-manual-window',
    title: 'マニュアルを、マニュアルだけのウィンドウで読めるようになりました',
    items: [
      'ヘルプの「マニュアル」の下にある「マニュアルを別のウィンドウで開く」を押すと、左に目次、右に本文がウィンドウいっぱいに出ます。',
      'これまではヘルプの画面の中の、高さが画面の 6 割ほどの箱に入っていました。3600 行をその箱から覗く形でした。',
      '左の目次には見出しが 160 本ぶん並びます。押すとその節まで送られます。',
      'この窓の中では Ctrl + F(Mac は コマンド + F)がブラウザの「ページ内を検索」としてそのまま使えます。',
      '左の列の「アプリ」タブにも「マニュアル」のタイルが出ます。ヘルプを開かずに、そこからも開けます。',
      '開いても、それまで読んでいた本文は消えません。',
    ],
  },
  {
    id: '2026-08-31-manual-repair',
    title: 'マニュアルの抜けを直しました',
    items: [
      '書き出しの一覧が 1 行しか出ていませんでした。残りの 9 行が別の節の末尾に落ちて、記号の並んだ行として出ていました。10 行そろって表で出ます。',
      'ショートカットの表も同じように切れていて、左右のペインを畳む・戻る・進むの 4 行が記号のまま出ていました。表に戻しました。',
      'コードブロックに添付を入れる書き方を、アセットや埋め込みという言葉でも探せるようにしました。',
      '添付を読めなかったときに画面へ出る字と、マニュアルに書いてある字が違っていました。そろえました。',
      'Word と PowerPoint で書き出すとき、読めなかった添付が注意に出るようになりました。これまでは閲覧用 HTML だけでした。',
      '添付から中身を取っている表は、セルを押しても打てません。そう書いていなかったので書きました。',
    ],
  },
  {
    id: '2026-08-31-help-find',
    title: 'ヘルプでマニュアルの中を探せるようになりました',
    items: [
      'ヘルプの「マニュアル」の見出しのすぐ下に、探す欄を置きました。語を打つと、その語が出てくる節が下に並びます。押すとその節まで送ります。',
      '打つそばから絞られます。Esc で打った字を消せます。',
      'マニュアル本体は畳みません。当たらなかった所もそのまま出したままです。',
      'ヘルプを開いている間は、Ctrl + F(mac は コマンド + F)がブラウザの「ページ内を検索」になります。これまでは左の一覧の絞り込み欄へ焦点が飛び、畳んでいた左の列まで開いていました。',
      'ヘルプ以外の画面では、これまでどおり左の一覧の絞り込み欄へ移ります。',
    ],
  },
  {
    id: '2026-08-31-codeblock-naming',
    title: 'コードの囲みの呼び名を「コードブロック」にそろえました',
    items: [
      '画面に出ている 1 個のかたまりは「コードブロック」、それを作る記法そのものは「コードフェンス」と呼ぶようにしました。',
      'これまでは「囲み」という同じ言葉が 3 つの別のものを指していました。コードのかたまりと、コロン 3 つで囲む箱と、本文のいちばん上の情報欄です。これからは「囲み」はコロン 3 つの箱だけを指します。',
      '書式パネルのボタンは「コード塊」から「コードブロック」になりました。押したときに入るものは変わっていません。',
      '貼り付けの設定にある「ウェブページの形をそのまま」の説明も、同じ言い方にそろえました。',
      'マニュアルの言い方もそろえたので、画面で見た言葉でマニュアルを探せます。',
    ],
  },
  {
    id: '2026-08-31-multi-tags',
    title: 'タグを 2 つ以上まとめて打てるようになりました',
    items: [
      'タグの欄に #買い物 #家事 のように井桁を付けて並べると、2 つまとめて付きます。半角空白でも全角空白でもかまいません。',
      'これまでは「買い物 #家事」という 1 つの名前として付いていました。本文の中に同じ字を書いたときは 2 つになるので、場所によって結果が違っていました。',
      'これまでに 1 つの名前として付いてしまったタグも、開き直すと 2 つとして読めるようになりました。ノートを書き替える必要はありません。',
      '同じ書き方が 4 か所で効きます。情報ペインのタグの欄、まとめて付ける帯、スマートフォルダの条件の欄、本文のいちばん上の tags: の行です。',
      '本文のいちばん上に tags: #買い物 #家事 と手で書いたときも読めるようになりました。これまでは 1 つも読めていませんでした。',
      '井桁を付けずに 買い物 家事 と書いたときは、これまでどおり空白を含む 1 つの名前です。',
    ],
  },
  {
    id: '2026-08-31-office-settings-reset',
    title: 'Office の設定を初期状態に戻せるようになりました',
    items: [
      'Office のウィンドウの中で変えた設定(ツールバーの形や表示言語)は、この端末が憶えていて次に開いたときも同じ形で始まります。',
      'その設定のせいで開けなくなったとき、これまでは戻す手立てがありませんでした。一式を削除して入れ直しても、設定のほうは残ります。',
      '設定の Office 表示に「Office の設定を初期化」を置きました。押すと、次に開いたときから素の状態で始まります。ノートも一式も消えません。',
      'Office が停止したときに出る帯にも「設定を初期化して開き直す」を置きました。困っているその場から戻せます。',
    ],
  },
];

/**
 * ⚠ **落としたぶんの原本は `CHANGELOG.md` に在る**。⚠ 落ちるのは**自動ではない** ──
 * `NOTICE_KEEP_MAX` を超えたら**人が古い 1 件を消す**(手順は `.claude/skills/notice-writing/`)。
 * 突合は `tests/docs-parity.test.ts` の `DROPPED`(落とした題名の既知リスト)が持つ ──
 * 「登記表 + 落ちた分 = CHANGELOG」を**等値**で見るので、落としたのに書き忘れると CI が落ちる。
 *
 * 🔴 **ここに id の写しを持たない**(2026-08-29)。⚠ 以前はこのコメントが落とした id を
 *   並べていたが、**66 件で更新が止まり、実際は 148 件**だった ── 誰も読まない 3 つ目の
 *   台帳になっていた(CLAUDE.md §7「同じ値が複数の場所にある」)。
 *   🔑 **育つ一覧は、機械が突き合わせている所にだけ置く。**
 */
