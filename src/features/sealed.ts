/**
 * 封印(user 指示 2026-08-03)。
 *
 * > 「**todo とカンバンとカレンダーは一旦封印したい** / PKC2 では使い物にならないから
 * > ほぼ使ってなかった / **form エントリと一緒、機能を煮詰める前に破綻したから**、
 * > 封印しておきたい」
 *
 * 🔴 **消すのではなく畳む**。理由は 3 つある:
 * 1. **既存データを壊さない** ── 取り込んだ container に todo の entry があっても、
 *    本文は PKC-Markdown なので**普通のノートとして読めて編集できる**
 *    (PKC3 は「全 body = PKC-Markdown、アーキタイプはフレーバー」)。
 *    抽出列(status / date / archived)も落とさない ── 落とすと再開時に戻せない。
 * 2. **戻せること** ── 描画器・抽出列・flavor をすべて残してあるので、
 *    解くのは**書き直しではなく導線の付け直し**で済む。
 *    ⚠ ただし **「配列から 1 語消すだけ」で戻るのは `todo` だけ**である
 *    (2026-08-04、引き継ぎ先の指摘で判明。初版はここに「1 語消すだけ」と
 *    書いていたが、それは 4 つのうち 1 つにしか当てはまらない):
 *    - `todo` … **作る導線**は配列から消すだけで戻る(`shell.ts` の `CREATE_BUTTONS` に
 *      項目が残っていて、この配列がそれを濾しているだけだから)。
 *      ⚠ **ただし状態を切り替える押し口は 2026-08-19 に無くなった**(#277 段②-b)──
 *      `toggle-todo` を焼いていた唯一の面が旧カンバンで、その面は
 *      **チェック項目の板**へ作り替わったからである。
 *      🔑 **機構は畳んであるだけで生きている**(binder の `toggle-todo` /
 *      reducer の `TOGGLE_TODO_STATUS` / `REQUEST_BODY_REWRITE` の経路 /
 *      抽出列 `status`)── 解くときに要るのは**押し口を 1 つ置くこと**だけで、
 *      書き直しにはならない。⚠ 逆に言えば、いま `todo` を解いても
 *      **status は画面から変えられない**(そこまでやって初めて戻る)
 *    - `form` … 消しても**何も戻らない**。作成導線がそもそも無い
 *      (PKC2 の 2026-04-26 audit で撤去済みの判断を継いだ)── `CREATE_BUTTONS`
 *      への追加が要る
 *    - `kanban` … 消しても**何も戻らなかった**(2026-08-19 に #277 で解いた)。
 *      P8 段⑤ で上の帯から面の切替を外してあるので濾す対象が無く、
 *      **組み込みタイル**(`kanbanTile`)を作り、`SEALED_TEST_NOTES` の smoke を
 *      戻し、盤面の中身そのものを**チェック項目**へ作り替えて初めて戻った。
 *      🔑 ここが「配列から 1 語消すだけでは戻らない」の**実例**である。
 *      ⚠ **その面は 2026-08-23 に畳み直した**(#292 段⑤)── 封印へ戻したの
 *      ではなく、**左の列の「予定」タブへ引っ越した**(user 指示
 *      「ちゃんとした導線に作り直しなさい」)。中身(チェック項目を札にする
 *      規則)はそのまま生きている ── 器だけが替わった
 * 3. **うっかり復活しないこと** ── 導線を消すだけだと、次に UI を触った人が
 *    善意で戻してしまう。2 つの test が機械的に見張る:
 *    - `tests/docs-parity.test.ts` … 封印中のものが**導線に出ない**こと
 *    - `tests/features/sealed.test.ts` … **戻せる形が保たれている**こと
 *      (描画器・flavor・抽出列が生きている / 解いたときに何が戻り、何が戻らないか)
 *
 * ⚠ **flag ではない**。flag は「切り替えて試すもの」で、これは「畳んで凍らせるもの」。
 * flag 枠(15)を食わせないし、URL から有効化できてもいけない。
 */

/**
 * 封印中のアーキタイプ ── **作る導線を出さない**。
 * ⚠ 既存 entry の表示・編集・書き出しは**通常どおり**(本文は Markdown なので、
 * 何も特別扱いせずに読める)。
 */
export const SEALED_ARCHETYPES: readonly string[] = ['todo', 'form'] as const;

/**
 * 封印中のビュー ── **切り替える導線を出さない**。
 *
 * 🔴 **2026-08-19 に 2 つとも解いた。いまここは空である**(#276 / #277。
 * user 指示「かつて無くしたカレンダーとカンバンはここで生きてきます /
 * 発想を変え、frontmatter でのカレンダー情報付与や…で復活させるのです」)。
 *
 * 🔑 `SEAL_REASON` が求める「何をもって煮詰まったと言えるか」への答えは、
 *   **2 つとも同じ形**だった ── **封印中の `todo` アーキタイプに寄せるのを
 *   やめ、user が本文に書いている物を情報源にする**:
 *   - カレンダー(#276)… frontmatter の `date`
 *   - カンバン(#277)… 本文の**チェック項目**(`- [ ] …` の行)
 *   ⚠ 破綻の形は「封印中の archetype に依存していたこと」なので、
 *   **導線だけ戻しても盤面は永久に空**である(実際そうだった)。
 * ⚠ 解いた形は**導線の付け直し**であり、切替を上の帯へ戻したのではない ──
 *   組み込みタイルから開く(#241 の形)にした。
 * 🔴 **その導線は 2026-08-23 に作り直した**(#292 段⑤。user 指示
 *   「ユーザーはもう一つ PKC が開いて混乱すると思う / ちゃんとした導線に
 *   作り直しなさい」)── **中央の面をやめ、左の列の「予定」タブ**へ引っ越した。
 *   ⚠ 描画器(`kanban.ts` / `calendar.ts`)は**この段で落とした** ── 中身の規則
 *   (frontmatter の `date` / 本文のチェック項目)は `features/schedule/` と
 *   `ui/render/schedule.ts` が持っているので、**戻すときに書き直しになるのは
 *   器だけ**である(封印が守ろうとしていた「規則」は失われていない)。
 * ⚠ **空でもこの配列は消さない** ── 次に何かを畳むときの受け皿であり、
 *   `tests/docs-parity.test.ts` / `tests/features/sealed.test.ts` の口でもある。
 */
export const SEALED_VIEWS: readonly string[] = [] as const;

/**
 * なぜ封印したか(解くときに読む)。⚠ **「使われなかった」ではない** ──
 * 「**煮詰める前に作り込んで破綻した**」が理由なので、解くときは
 * 「何をもって煮詰まったと言えるか」を先に決めること。
 */
export const SEAL_REASON =
  '機能を煮詰める前に作り込んで破綻したため(user 指示 2026-08-03)。解くときは、先に「何をもって煮詰まったと言えるか」を決める。';

export function isSealedArchetype(archetype: string): boolean {
  return SEALED_ARCHETYPES.includes(archetype);
}

export function isSealedView(view: string): boolean {
  return SEALED_VIEWS.includes(view);
}

/**
 * ⚠ 封印で**畳んだ test** の記録(解くときに戻す先)。
 *
 * - `tests/smoke/kanban.smoke.spec.ts` / `tests/smoke/calendar.smoke.spec.ts` …
 *   一度は復活させた(#277 段②-b)が、**2026-08-23 に落とした**(#292 段⑤)──
 *   面ごと左の列の「予定」タブへ引っ越したので、実クリックで駆動する先が
 *   `tests/smoke/schedule.smoke.spec.ts` に**移った**。
 *   ⚠ 「封印して畳んだ」のではない ── **同じことを別の場所で見ている**。
 * - `tests/adapter/kanban-calendar-view.test.ts` … 同じ理由で落とした。
 *   ⚠ 面に依らない主張(本文のチェックの印 / 面の帯の × 閉じる)は
 *   `tests/adapter/center-pane.test.ts` へ、札の作法は
 *   `tests/adapter/schedule-view.test.ts` へ**移してある**。
 * - `tests/smoke/layout.smoke.spec.ts` … かんばん / カレンダーの見た目を見ていた
 *   assertion を、3 列と編集中の配置を見るものへ置き換えた(戻していない ──
 *   いまの版面の主張はそちらのほうが強い)。
 */
export const SEALED_TEST_NOTES =
  'かんばん / カレンダーの smoke は #292 段⑤ で tests/smoke/schedule.smoke.spec.ts へ引っ越し済み(いま封印中の面は無い)';
