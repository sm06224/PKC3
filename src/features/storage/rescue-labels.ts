/**
 * 🔴 **壊れたときのボタンの字を、1 か所で持つ**(#986 段③、2026-09-17)。
 *
 * ## なぜ要るのか(着地前レビュー 2 本が独立に挙げた)
 *
 * ⚠ 直す前、`拾って、戻せる形で書き出す` は **3 か所に手で書かれていた** ──
 * 実物のボタン(`commands.ts`)/ 押す前の説明(`commands.ts`)/ 捨てる窓の字
 * (`container-reset.ts`)。🔴 誰かがボタンを改名した日、**案内の 2 つは古い字を
 * 指したまま CI も緑**になる ── #996 で踏んだ「**画面に無いボタン名を探させる**」
 * のと同じ型である(#996 を直した翌日に、同じ形をもう一度作っていた)。
 *
 * ## ⚠ ここは features に置く
 *
 * 断り文(`db-corruption.ts`)も捨てる窓(`container-reset.ts`)も features なので、
 * 字が adapter(`commands.ts`)に在ると**引けない** ── 引けないと、
 * 「手で書く」しか道が無くなる。🔑 **引ける所へ置くのが直し**である。
 *
 * ## ⚠ 定数にしただけでは足りない
 *
 * 定数を**画面が本当に使っているか**は、これだけでは分からない。
 * 🔑 だから `tests/features/db-corruption.test.ts` が
 * **描いたボタンの `textContent`** と突き合わせる(DOM = 独立した観測)。
 *
 * ## 🔴 2026-09-21(#1017 段④b)に、専用の 2 つのボタンを退役させた
 *
 * `RESCUE_ARCHIVE_LABEL`(「拾って、戻せる形で書き出す」)/ `RESCUE_TEXT_LABEL`
 * (「拾って、読める形で書き出す」)は**操作としては無くした** ──
 * 保存領域に問題があるときは、いつもの**バックアップ**(`export-archive`)と
 * **Markdown**(`export-markdown`)が自動で読める分だけを集めて
 * `.pkc3-part.zip` で落とす(`src/adapter/ui/actions/export-archive.ts`)。
 * 🔑 だから「押してください」と言う文言は、いまは**この `BACKUP_LABEL`** を指す。
 */

/**
 * 🔴 **コレクション全体のバックアップの呼び名**(#1017 段④b)。
 *
 * ⚠ `commands.ts` の `export-archive` 操作の label と**同じ字**にする ──
 * 断り文・捨てる窓・作り直しの案内など、features 側から「あのボタンを
 * 押してください」と言うときはここから引く(2 か所に手で書くと、
 * 改名した日に片方だけ古い字のまま残る)。
 */
export const BACKUP_LABEL = 'バックアップ';

/** 壊れている所があるかを調べる。 */
export const DB_CHECK_LABEL = '壊れていないか調べる';

/**
 * 🔑 **拾った中身で、その場に建て直す**（#1006。user 裁定 2026-09-18）。
 *
 * ⚠ 字の選び方に理由がある ── 押す前にいちばん知りたいのは
 *   「**消えない**」だからである（壊れたと聞いた直後の人は、
 *   何を押しても消えるのではと怖がって手が止まる）。
 * 🔑 すぐ下の `CONTAINER_RESET_LABEL` と**対になって読める**（残す / 捨てる）。
 */
export const CONTAINER_REBUILD_LABEL = '中身を残して、作り直す';

/** 🔴 入れ物ごと捨てる(取り消せない)。 */
export const CONTAINER_RESET_LABEL = '中身を捨てる';
