{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "noEmit": true,
    // Vite の native config loader は config からの import に拡張子を要求する
    // (無いと毎回 stderr に警告が出る ── 規律は「全スイートの stderr 0 行」)。
    // `noEmit` なので `.ts` 拡張子つき import を許して構わない
    "allowImportingTsExtensions": true,
    "skipLibCheck": true,
    "isolatedModules": true,
    "verbatimModuleSyntax": true,
    "types": ["vite/client"],
    "paths": {
      "@core/*": ["./src/core/*"],
      "@features/*": ["./src/features/*"],
      "@adapter/*": ["./src/adapter/*"],
      "@runtime/*": ["./src/runtime/*"]
    }
  },
  "include": ["src", "tests", "vite.config.ts"],
  // harvest-from-pkc2.ts は PKC2 リポジトリ(CI に無い)から import する採取スクリプト
  "exclude": ["tests/fixtures/markdown-goldens/harvest-from-pkc2.ts"]
}
