# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository (PKC3).

## Language Policy

- Internal reasoning MUST be in American English
- Final output MUST be in Japanese

## 会話・提示ルール(user 確立、PKC2 から継承。必ず遵守)

- **AskUserQuestion ツールは使わない**。質問・確認は必ず会話文で行う
- **成果物は GitHub URL(rendered)で提示**。diff の貼り付けでは伝わらない
- **doc-first**: 実装前に設計 doc → GitHub URL で提示 → user 裁定 → 実装
- **意図を読む**: user の発言が事実と食い違っていても、関連する実態を探して会話で聞き返す
- 🔴 **渡す URL は、渡す前に実在と中身を確かめる**(2026-08-08 に 2 回間違えた)。
  「PR #93 に着地させました」と報告したが、**#93 は dependabot の別 PR** で、
  自分の branch には open PR が 1 つも無かった。⚠ **成果物の提示が主要な出口**
  (上の「GitHub URL で提示」)である以上、番号の取り違えは**成果ごと届かない**。
  `list_pull_requests` / `search_pull_requests` で **branch から引いて**確かめる

## 🔒 機密資料の取り扱い(user 指示 2026-08-15。不可侵・削除禁止)

> 「**なお、この中身についてはリポジトリやスクショを通じて情報を漏らすことを禁じます /
> 固い契約を残してください / 類推すらさせないために、一切を使用後に廃棄してください**」

user が「漏らすな」と言って渡した資料(以下 **機密資料**)には、次を**必ず**守る。
⚠ **例外を作らない。**「これくらいなら」と一部を出すのが漏洩である。

1. 🔴 **中身をどこにも出さない。** repo(commit / issue / PR / doc / test fixture /
   お知らせ)にも、スクリーンショットにも、会話の要約にも出さない。
   ⚠ 禁じられるのは本文だけではない ── **題名・見出し・部品の名前・寸法・語の一部・
   言い換え・要約**も「類推させる材料」なので同じ扱いにする
2. 🔴 **作業は repo の外でやる。** 専用ディレクトリ(`/tmp/…`)に置き、`git add` の
   届かない所で完結させる。⚠ 作業用の probe も**その外**に書く
3. 🔴 **観測点は中身に依存しないものだけ。** スクショを撮らない / 画素を読まない /
   本文を読まない / console は**非 ASCII の行を丸ごと捨てる**(本文が混じる唯一の経路)。
   構造(部品の数・大きさ)を見るのは可、**中身を開くのは不可**
4. 🔑 **原因は「自作の対照群」で言い直してから報告する。** 機密資料でしか言えない主張を
   残さない ── 2026-08-15 の実例では、最終的に**自作 3KB の .docx** で全部再現でき、
   issue #199 は user の資料に 1 文字も触れずに書けた。
   ⚠ これは礼儀ではなく**手法として正しい**(自作で再現できない主張は、そもそも弱い)
5. 🔴 **使い終わったら痕跡ごと廃棄する。** 資料本体・派生 file(部分 zip / 変換物)・
   ブラウザ profile・log・JSON・スクショを**まとめて消す**。⚠ 「後で使うかも」は残す理由に
   ならない ── 必要になったら user から貰い直す
6. ⚠ **スクショを撮ってよいのは自作の file を開いたときだけ。** 同じハーネスで機密資料を
   扱う日は、撮影の口を**呼ばない**のではなく**渡さない**(引数を与えなければ撮れない形にする)

## 方針(founding。user 裁定 2026-07-30)

> **北極星: 速く、安く、必要十分、利便性最大。「最強のノートアプリにするんだー」**

- **正本 doc** = `docs/development/pkc3-major-upgrade-design-2026-07.md`(P0 裁定済み・実装 go。
  §12 が裁定記録)。まずこれを読む
- 🚫 **PKC2(sm06224/PKC2)は read-only 参照のみ。手を出さない**(user 指示 2026-07-30)。
  実測・設計の根拠 doc 群は PKC2 側にある(正本 doc §13 にリンク)
- **流用 + 総合的見直し。丸写し禁止**(user 指示 2026-07-30)── 古い設計・積み上げで
  遅くなる実装を持ち込まない。PKC2 はそれで失敗した
- **全 body = PKC-Markdown、アーキタイプ = フレーバー**(見せ方・編集の仕方)。
  JSON 文字列 body を作らない
- **storage**: wasm-sqlite(OPFS SAHPool・専用 worker)+ IDB Blob のハイブリッド。
  ゼロコピー・生成物のライフサイクル終端での即破棄(user 指示 2026-07-27、不可侵)
- **flags は最大 15 個(CI test で pin)+ 各 flag に畳む条件の宣言必須。正規設定(settings)と
  分離する**(user 指示 2026-07-30)
- **新機能を盛り込みすぎない**。将来領域(フォーム→ダッシュボード・帳票 / Graph API・OneNote)は
  正本 doc §10 の拡張点のみ。着手は user の明示 go
- **PKC3 export は PKC2 にインポートしない**(一方通行、user 裁定 2026-07-30)。
  import は PKC2 全形式を受理して新スキーマへ変換
- **計測規律は PKC2 から継承**: boot 窓だけで定常を語らない / 対照群を揃える /
  persistent profile で測る / fixture のゼロ件次元は「測っていない次元」/
  倍率より向き・百分率は分母を書く
- 🔴 **配る量は気にしない。効くのは定常**(user 指示 2026-08-03。不可侵):
  「**mermaid に関しては前にも言ったけど、配布サイズは気にしないで欲しい /
  初回起動が遅くとも、そこは許容 / その後の動作がメモリくったり、もっさりだと
  嫌なだけです**」── size cap(`scripts/check-dist.mjs`)は**手違いの検出**であって、
  サイズを守らせる規律ではない。通常増加で cap に触れたら**引き上げてよい**
  (⚠ 撤廃はしない ── 重い依存の誤取込・生成物の取り違えを止める tripwire)。
  **「重いから入れない」を判断理由にしてはならない**。測って報告すべきは配る量ではなく、
  **継続使用の常駐メモリ**と**操作の応答**(long task / 入力から反映までの時間)である
- 🔴 **図は「描いたら焼く」**(user 指示 2026-08-03):
  「**mermaid 図のエクスポートをさせるとき以外は PNG ラスタをキャッシュして、
  GPU レンダリングで表示して欲しい / レンダリングは表示タイミングでいいけど、
  スクロール追従が悪いなら、1 ドキュメント開いている場合は関連をあらかじめ
  レンダリングしておくとか工夫して欲しい**」
  ── 画面に置くのは **SVG ではなく PNG の `<img>` 1 枚**(合成だけで済み、
  スクロールが GPU に乗る)。SVG(ベクタ)は**書き出しのときだけ**。
  描くのは表示時でよいが、**追従が悪ければ同一ドキュメント内を先読みする**。
  ⚠ キャッシュ鍵には **図の原文 + テーマ + 幅 + devicePixelRatio** を含める
  (テーマや拡大で焼き直しが要る)。⚠ bytes は IDB Blob(heap に載せない)、
  ObjectURL は表示の寿命終端で revoke(2026-07-27 の不可侵指示)
- 🔴 **重い処理はワーカーへ。ワーカーは使い捨てにする**(user 指示 2026-08-03。不可侵):
  「**基本的に重い処理はワーカーにしてください / ワーカーはしばらくつかわれないなら、
  キルと解放し、ワーカーへのジョブ発行をバッファして、ワーカーにディスパッチします**」
  ── 既定は「重い処理 = メインスレッドの外」。⚠ これは 3 つの規律が同時に要る:
  ① **遅延起動**(要るまで作らない)② **ジョブのバッファ**(起動待ちの間に来た依頼を
  落とさない ── 起動してからまとめてディスパッチ)③ **アイドルで kill と解放**
  (使われなくなったら terminate して常駐メモリを返す)。
  ⚠ **飛んでいるジョブがある間は kill しない**。⚠ terminate 時は待っている依頼を
  **必ず reject する**(永久 hang を作らない)。⚠ 受け渡しは transfer(ゼロコピー)──
  2026-07-27 の不可侵指示と同じ向き。
  🔑 storage worker(sqlite)は**この規律の対象外** ── DB の lease を握るので
  常駐が必要。対象は**計算のワーカー**(描画・圧縮・符号化)である。
  機構は `src/adapter/platform/worker-lease.ts`
- 🔴 **目指す姿は「業務画面」**(user 指示 2026-08-03):
  「**一応、目指してたのはシンプルかつ高機能さ**」── 高密度だが詰まっていない /
  同じものが常に同じ場所にある(モードで配置が変わらない)/ 1 画面で完結する /
  マウスだけで完結し、キーボードは近道 / **地は無彩色、色は情報にだけ使う**。
  ⚠ 「端末風の緑を地に敷く」初期の見た目はこの方針と逆であり、**テーマの 1 つ**へ降ろす
- 🔴 **記法を減らすことは、user の動線を減らすことである**(user 裁定 2026-08-07。不可侵):
  「**記法の縮小と簡単に言うが、ユーザーの動線を縮小することを忘れるなよ**」
  ── これは 2026-08-06 の「正規形を iR のために残す必要がなければ縮小版で実装 OK」を
  **縮小しない側へ確定させた裁定**である(全数調査の結論と同じ向き:
  `docs/development/notation-shrink-survey-2026-08.md`)。
  ⚠ **記法 1 つは user の書き方 1 つ**なので、落とせば「書けたものが書けなくなる」。
  段数・維持コスト・一貫性は**こちら側の都合**であり、動線と交換してよい物ではない。
  ⚠ 「PKC3 に未実装」を「記法として不要」と読み替えない ── 移植の穴が台帳から消え、
  復元の手がかりが失われる(調査で実際に 6 件やった)。
  🔑 **買ってよいのは「記法を 1 つも失わない整理」だけ** ── 本文に無い記法の処理段を
  **素通りさせる**(出力は 1 バイトも変えない)/ 判定を 1 か所へ寄せる /
  釣り合いの崩れを直して**落ちていた動線を戻す**。この向きなら「効果が小さい」は
  棄却理由にしない(founding の「積み上げた先に価値があるなら積む」)

- 🔴 **URL クエリパラメータの切替は「フラグ」である**(user 指示 2026-08-07。不可侵):
  「**URL クエリパラメータ切り替えはフラグ扱いである / 起動前から必要なパラメータが
  ある場合、それはフラグ画面に出てきて、フラグ画面から再起動した際にパラメータありで
  再起動する必要がある / クエリパラメータを抜け穴にしてはいけない / 本来は PKC 内部の
  パーマネントリンクやディープリンク以外にクエリパラメータを使用してはいけない**」
  ── ⚠ **「計測用だから枠を食わない」は禁じ手**。それが抜け穴である。
  切替である以上 **15 枠に数え、`foldWhen` を宣言し、フラグ画面に出す**。
  🔑 **起動前に要るものは、フラグ画面から「パラメータ付きで再起動」させる**
  (user に URL を手で打たせない)。
  ⚠ クエリパラメータを読んでよいのは **flag の解決**と**パーマリンク / ディープリンク**
  だけ ── それ以外は `tests/features/flags.test.ts` の全数検査が落とす。

## 🔴 進め方の方針(user 指示 2026-08-07。不可侵)

> 「**PKC3 はより発展させてサブエージェントを大量にスケールさせながら
> 物量と試行と思考で推し勝つ方針でいきます**」

- **既定は「1 人で考える」ではなく「大量に投げて突き合わせる」**。調査・レビュー・
  記法網羅・設計案の比較は**並列に何本も**走らせ、突き合わせて決める
- ⚠ これは「たくさん投げれば当たる」ではない。**物量 × 試行 × 思考**の 3 つが揃って
  初めて勝つ ── 投げた結果を**鵜呑みにしない**のが「思考」の側である
  (2026-08-07 実測: レビュー 2 巡で 13 件が正しく、**2 件は前提か結論がずれていた**)
- 🔴 **書き込み権のあるエージェントは worktree を分けて走らせる**
  (`isolation: "worktree"`)── 自分の作業ツリーで並行に走らせない。
  2026-08-04 に編集中の file を変異させられた。read-only なら同居してよい
- 手順は `.claude/skills/subagent-scale/SKILL.md`(`/fanout` は明示版)

## プロセス指示(user 指示 2026-07-30。必ず遵守)

- **必要な時にやる**: 常時監視・儀式的な定期実行はしない。予約・監視は「未完の作業が
  残っているとき」だけ張り、着地したら畳む
- **CI を長くしない**(PKC2 の「CI 長すぎ問題」の再発防止): PR gate は**速い lane に限定**
  (typecheck / lint / unit / build ── 目標 5 分以内、workflow の timeout 10 分 = tripwire)。
  重い検証(視覚テスト全量・ベンチ・全 matrix・カバレッジ集計)は main push / nightly /
  手動 dispatch に逃がす。**gate を足すときは「これは PR で走る必要があるか」を毎回問う**
- **品質はサブエージェント・スキルで守る**: 実装 PR は着地前に code review(サブエージェント)を
  回す(`.claude/agents/pkc3-reviewer.md` / `/review`)。性能の主張は**測ってから言う**
  (規律の正本は本 file 冒頭の計測規律。**手順**は `.claude/skills/perf-measurement/SKILL.md`
  / `/measure`)。視覚を持つ変更は smoke(`.claude/skills/smoke-testing/`)
- **視覚テスト**: PKC2 の視覚テスト資産(playwright-visual / visual-parity / shinsatsu)を
  **遅くなりすぎないように改修して**使う。UI 実装が始まる P3 で導入 ──
  PR gate には最小 smoke(数 spec・秒オーダー)のみ、全量は nightly

### 🔴 回すものの粒度(user 指示 2026-08-10。不可侵)

> 「**コレから先、適宜回すアクションは、粒度が適切になるように気をつけるように
> お願いします**」

**自分の判断で回すもの(CI の dispatch / サブエージェント / 常駐の待ち)は、
毎回「この粒度で正しいか」を問う。** 粒度は 2 方向に間違える ──

- 🔴 **太すぎ**: 1 つの job が 2 つのことを主張すると、**赤緑が意味を失う**。
  実例(2026-08-10): ビルド job に起動 probe を同居させたため、
  **undefined symbol 0 件で成果物も揃っているのに job は赤**だった。
  🔑 **1 job = 1 主張**。落ちたときに何が壊れたか、名前で言える形にする
- 🔴 **細すぎ・散らしすぎ**: 同じ種類の出力を複数の step に散らすと**読めない**。
  実例(同日): 診断を make 直後・probe 直後・JSPI 報告と 3 か所に置き、
  API のログが末尾しか返さないので**どれも読めず 3 回転を捨てた** → 出口を 1 つに統合した
- 🔴 **場所が違う**: 「遅いランナーで回す理由が無いもの」を CI に置かない。
  実例(同日): 起動 probe を CI に置いていた理由は「成果物を手元へ落とせないから」
  だけで、prerelease 経由の取得が通った時点で**理屈が消えていた**のに惰性で回していた。
  🔑 **理由が消えた仕掛けは、消えた時点で外す**

⚠ サブエージェントも同じ ── **投げる範囲を切らないと空振りする**。
実例(2026-08-10): 「どこでビルドしているか」を書かずに 4 本投げ、
**3 本が同じ誤前提**(手元ツリーを見て「1 つもコンパイルしていない」)に乗った。

判断のとき自分に問う 3 つ: **①これは 1 つの主張か ②ここで回す理由が今もあるか
③落ちたとき原因が名前で分かるか**

### 🔴 下流(cowork / user 実機)へ流す粒度(user 指示 2026-08-14。不可侵)

> 「**全てを実装とテストまでこなしなさい / そこまでやって初めて、cowark にテスト指示が
> 出せると認識してください / あなたは甘すぎる / 仕事を次の工程に流す際に細かすぎる粒度で
> 流して、cowark が本来は発見しなくてもいいような不具合を見つけている**」

- **裁定済みの案件は束で完了させる** ── 実装 + 自前の検証(unit / 変異試験 / 実ブラウザ
  smoke / headless probe)まで全部済ませ、**そのうえで cowork への検証依頼を 1 本**出す。
  1 件直すたびに検証依頼を書かない
- ⚠ cowork が見つけてよいのは**こちらの計器では原理的に届かない物**(実機 DPR / 実 IME /
  実フォントの許可状態・OS 差)だけ。自前の headless で拾える不具合を cowork に拾わせたら、
  それは**こちらのテスト不足**である(実例 2026-08-14: レポート #10 の Options ペイン非描画・
  サイドバー停止・Start Center dead click は、全部 headless の面走査で拾えた)
- 🔑 実装を「済んだ」と言う前に、**その機能が触る面を headless で一巡**する(1 面 1 操作で
  よい ── 開く・押す・閉じるが素通りするか)。cowork へは「残っている疑問」ではなく
  「裏取りしてほしい確定主張」を渡す

### 🔴 実ブラウザ検証を外部へ頼むときは、指示プロンプトを書く(user 指示 2026-08-12。不可侵)

> 「**cowark に実ブラウザを用いた検証等を依頼したい場合は、cowark 向けの指示プロンプトを
> 書くように教訓としてください**」

**「見てほしい」だけ投げない。** 確認点・観測点・報告様式を**こちらが書く** ──
書かないと、要らない情報が返り、要る情報が返らない。
⚠ とくに **①「どのビルドを見ているか」を先に確定させる手順**を必ず頭に置く
(古い一式が残っていると、直した症状がそのまま再現して**存在しない不具合を追う**)。
🔑 **こちらの申告を裏取りさせる**と、自分の見落としが返ってくる(2026-08-12 の実績:
「ぴったり」と報告した窓が外寸ではみ出していた / 自分のキー処理が別の動線を殺していた)。
手順は `.claude/skills/cowork-verification/SKILL.md`。

### 🔴 判断の出し方(user 指示 2026-08-08。不可侵)

> 「**このフラットに考えて提案すること、調査をしてベストプラクティスに寄せた
> 設計提案をすることを教訓としてください**」

**選択肢を並べて user に丸投げしない。調べて、自分の推薦を 1 つ出す。**

- 🔑 **フラットに考える** = 立場を持たずに見る。とくに**自分が書いたもの**と
  **既に裁定されたもの**を優遇しない。既存の doc・過去の裁定・自分の実装が
  相手でも、**根拠のほうが強ければそちらを推す**(⚠ ただし勝手に変えない ──
  「裁定を覆す提案です」と明記して user に出す)
- 🔑 **調べてから言う。** 「A と B があります、どちらにしますか」は調査を
  user に押し付けている。**先に確かめられることは確かめて**から出す ──
  2026-08-08 の実例:「編集中に面を開けるか」は意見が割れる話に見えたが、
  **面が `hidden` で常駐し `render` が早期 return する**ことを読んだ時点で
  「止めている理由が無い」と分かった(= 意見ではなく事実で決まった)
- 🔑 **相手の理由の「中身」を見る。** 引用された根拠をそのまま受け取らない ──
  同じ 2026-08-08 の実例: 「PKC2 は 90+ spec を壊した」は *automation ゲートが
  無かったから*ではなく *帯が本当に版面を押し下げるから*であり、ゲートは
  **症状を隠すだけ**だった。**理由を 1 段掘ると結論が反転することがある**
- 出す形: **推薦 1 つ + その理由 + 「これが分かったら覆る」という条件**。
  ⚠ 判断が割れるところだけを user に出す(全部出すのは丸投げと同じ)
- ⚠ これは**勝手に決めてよい**という意味ではない。user に見える仕様・裁定の
  変更は**必ず会話で確認する**(下の「user に見える仕様を…勝手に決めない」)。
  変えるのは**提案の質**であって、決定権の所在ではない

## Build & Development Commands

```bash
npm run dev        # Vite dev server
npm run build      # Vite build → dist/(マルチファイル)
npm test           # vitest run
npm run typecheck  # tsc --noEmit
npm run lint       # eslint src tests build scripts
```

⚠ **lint の範囲は `src` / `tests` だけではない**(2026-08-14 に訂正)。`build/` と
`scripts/` も見る ── ここが「`src` と `tests` だけ」と書いてあったせいで、
`build/office-wasm/*.mjs` に出た赤を「CI に関係ない」と切り捨てて CI を赤くした。

## 段階(正本 doc §11)

P1 bootstrap → P2 計測 + sqlite core → P3 app 層の総合的見直し + リーン集約 →
P4 assets → P5 revisions → P6 import/export → P7 v3.0.0(Pages product + PWA 仕上げ)。
各段階が単独で着地し、単独で計測できる。「効果が小さい」は棄却理由にしない。

## 検証の規律(2026-08-02 に確立。P6c〜P6f の実測から)

> **通っている test は、何も保証していないかもしれない。**
> この期間にレビューが見つけた欠陥は、**ほぼ全部「test が緑のまま壊れていた」**。

**踏んだ罠は 9 つの型に畳める。同じ型は日付が違うだけで同じ顔をしている ── 引くときは型から引く。**
1 空振り / 2 未実行の経路 / 3 変異試験そのもの / 4 観測点 / 5 環境差 /
6 shell と CI / 7 複数の場所 / 8 上流の取り込み / 9 編集の作法。

### 1. 検査が空振りする ── 緑が「別の理由」で成立している

🔑 **ガードは代替物で満たせない条件にする**。「それらしいものが在るか」ではなく
**「参照されているものが実在するか」**で書く。以下は全部その顔違いである。

- 🔴 **救い手が変わっただけ**(2026-08-02、P7 段① で 2 ラウンド連続)。1 巡目「`.js` が
  1 件でもある」は `sw.js`(public の静的コピー)に救われ、**アプリ本体を消しても緑**だった。
  「index.html の参照を突合」へ直したら、2 巡目は**空振り防止のガード自体**が
  `manifest.webmanifest` / `icon.svg`(Vite が書き換えない public の静的参照)に満たされ、
  `--base /` にすると**やはり本体を消しても緑**。🔑 **空振りを直したら「今度は何に
  救われていないか」を問う** ── 条件は「参照が 1 件でもある」ではなく
  「**hash 付き生成物への参照が 1 件でもある**」
- 🔴 **主張そのものが成り立たない**(= 検査の「主張そのもの」が間違っている。2026-08-08)。
  🔑 **空振りを直す前に、その条件が成り立ちうるか**を確かめる。ヘルプ smoke が「document 内に
  **id の重複が 0 件**」を要求していたが、面は `hidden` で同一 document に常駐するので、
  **user がマニュアルと同じ見出しを書けば必ず重複する** ── 守れない条件だった(しかも
  ノートを 1 件も作っておらず**空振りでもあり、だから緑だった**)。🔑 **実害の形へ書き直す**:
  重複は許し「`#slug` が**本文の面**に当たること」を見る。
  ⚠ 「重複 0 件」のような**きれいな条件**ほど、成り立たないまま素通りしやすい
- 🔴 **後条件は、確かめた事実の上にだけ書く**(2026-08-14)。⚠ 上と**近縁だが別**である ──
  あちらは**論理的に成り立ちえない**条件、こちらは**経験的に未確認**の条件を、確かめる前に
  固定したもの。`--disable-wasm-strip` を渡せば locale の剥がしが止まる**はず**という期待を
  そのまま `grep -q '^export ENABLE_WASM_STRIP_LOCALES=$' config_host.mk` に固め、
  `tests/workflow-steps.test.ts` で**字面ごと pin** した。
  ⚠ 落ちたとき「**実装が悪いのか、期待が間違いか**」が区別できない ── 実際 3 回とも
  「ビルドが失敗した」と読み、**期待の側を疑うのが毎回最後**だった(`WITH_LANG` に `ja` →
  存在しない option `--disable-wasm-strip-locales` → 本件)。
  🔑 **未確認は assert ではなく診断で出す**(値を印字して次の回転で読む)。**通ったのを
  見てから**後条件へ昇格させる。⚠ 1 度の実験が **2 つを主張していた**のも効いた ──
  `--with-lang` と `--disable-wasm-strip` を同じ configure で渡したので、どちらが効いたか
  単独では読めない。§「回すものの粒度」の**「1 job = 1 主張」の実験版**である。
  - ⚠ **コメントに書いた因果も、検査の主張と同じく間違う**(同日)。`min-height: 0` に
    「これが無いと本文が縮まず、面が 30vh を超えて見出しごと押し出される」と書いていたが、
    **外しても本文は h=0 まで縮んだ**(実測値まで一致)── 同じ規則の `overflow: auto` が
    flex の automatic minimum size を 0 に解決するので、その 1 行は **no-op** だった
    (`src/styles/app.css`)。🔑 **「これが無いと壊れる」と書いたら、外して壊れることを
    1 度は見る。** 見ないなら書かない
- 🔴 **範囲が広すぎて無関係な散文に満たされる**(2026-08-13、CI で同じ日に 2 度)。
  ①「頼んだときだけ `SAFE_HEAP` が入る」を **file 全体**(`mark in text`)で見ていたが、上流に
  `# avoid -s SAFE_HEAP=1 …` という**コメントが実在** ──「頼んでいないのに在る」が常に真で、
  ⚠ **配布ビルドを焼くたびに落ちる**状態だった。② 順序の guard を
  `indexOf('qtbase-patch-*.py')` で書いたら **cache 鍵のほうに当たっていた**(鍵は file の
  上に在るので、step をどこへ動かしても常に「前」になる)。🔑 見るのは**自分が書き換えた行**
  (①なら `INITIAL_MEMORY` の行)、位置を採るなら**実行する行そのもの**で採る
  - 🔴 **3 度目・4 度目は CSS で踏んだ**(2026-08-14)。CSS を読む test で
    `indexOf(selector + ' {')` と書いたら**版面の `grid-area` の規則**に当たって落ち、
    直した 2 稿目は `"${sel} {"` で探したので**選択子リスト**(`A,\nB {`)に書いた規則を
    **1 つも拾えなかった**。⚠ **同じ file の 40 行上の test がまさにこれを戒めていた**
    (`tests/adapter/announce.test.ts`)。🔑 CSS は**構文で拾う** ── `選択子 { 宣言 }` を
    全部読み、選択子リストを `,` で割って**丸ごと一致**を見る。⚠ `@media` の中まで拾うと、
    印刷や狭い版面だけの規則で**画面の規則を消しても緑**になる ── 最初の `@media` で切る
  - 🔴 **5 度目は、この教訓を書いている最中に踏んだ**(同日)。「効かない
    `--disable-wasm-strip` が戻っていない」を **yml 全体**で `not.toContain` したところ、
    **直上に書いた自分の解説コメントに満たされて**必ず落ちた ── `SAFE_HEAP` の件と
    **同じ罠**である。🔑 見るのは**実行する行** ── heredoc を抜き出し、その中だけを見る
    (空振り防止に「抜き出せていること」も併せて assert する)
- 🔴 **guard を file 名指しで書かない**(2026-08-08)。「版は 1 か所」を守るはずの検査が
  **`settings.ts` に `APP_VERSION` が無いこと**しか見ておらず、他のどの面に生やしても素通り
  ── 題名は「2 か所に出ていない」なのに、守っていたのは「settings.ts に出ていない」だけ。
  **面の file を全数走査して等値で pin する**形に直したら、`main.ts` に**2 か所目が既に在った**
  (`(dev)` と「(開発版)」の 2 系統が同時に出ていた)
- ⚠ **「名前が在るか」の検査は、中身が空でも通る**(2026-08-08)。`repo-hygiene` の
  「受け手のいない `data-pkc-action` が無い」は**名前を消す変異は殺すが、ハンドラの中身を
  空にする変異は殺さない** ── 非対称である(中身は別の test が見る)。⚠ 逆に**等値 pin の
  既知リスト**(`KNOWN_DEAD`)は良く効いた ── 直したら消さないと落ちるので**忘れられない**
- ⚠ **文言は「押した場所」と対で pin する**(2026-08-08)。断り文に「編集」が含まれるか
  だけ見ていたので、**呼び名(`ノート` / `リンク先`)を取り違える変異が生き延びた**。
  行を押したのに「リンク先を開いてください」と出ると、user は**別のものを探す**
- 🔴 **tripwire は上限だけでなく下限も置く**。取り違え・欠落は**縮む方向**にも起きる ──
  size cap しか無いと、entry chunk を **0 バイト**にしても「配る量が減った」だけで通る(実証)。
  **件数を数える検査も同型** ── `--sourcemap inline` は `.map` を 1 件も出さないので、
  4.3MB の map を出荷しながら「map 0 件」と報告した
- 🔴 **壊れを検出する材料を捨てない**。`content_hash` のような照合材料を落とすと、誤りが
  **自己証明されて固定される**(書込側が計算し直すので以後の検査も通る)。⚠ **検査を書いた
  だけでは足りない** ── 材料が実際に届いていることを pin する(optional な field は writer が
  代入を落としても tsc が黙る = 全件で無効化される)
- 🔑 **検品する側・test する側も変異試験の対象**にする(→ §3)── 検品が壊れると
  「通った」という事実だけが残る

### 2. 経路が「一度も通っていない」── 弱いのではなく走っていない

🔑 空振り(§1)は**条件が別物に満たされる**、こちらは**そもそも実行されない**。
生き延びた・緑だったときは、まず**通っているか**を疑う。

- 🔴 **`SURVIVED` の半分は「弱い」ではなく「通っていない」**(2026-08-07)。変異は当たったのに
  **test がその行を 1 度も実行していない**ことがある ── 履歴パネルのガードを消す変異が
  生き延びた原因は assert ではなく、`render()` が**指紋(selectedLid / body / phase /
  revisionPanel)が同じなら何もせず返る**ことで、test は `entryMetas` だけ動かしていたため
  **その関数に入っていなかった**。🔑 生き延びたら **assert を足す前に「通っているか」を疑う**
  ── 壊す代わりに `throw` を置いて、落ちなければ**誰も通っていない**。
  手順は `.claude/skills/mutation-testing/SKILL.md`
- 🔴 **「両方向を検めている」と書いた検査が、片方向しか走っていない**(2026-08-13)。§1 の
  `SAFE_HEAP` の後条件は「頼んだのに入らない」も見るつもりで書いたが、**実際に走ったのは
  `=1` の側だけ**だった ── 最初に回した調査ビルドが `safe_heap=true` で、**配布側の経路は
  1 度も通っていなかった**。⚠ **変異試験 7 件が KILLED でも突けない**(変異が当たる場所と、
  経路が通っていない場所が別である)。🔑 **分岐を書いたら、分岐の数だけ「実際に走らせた記録」を
  持つ** ──「両方向」「N 通り」と書いた瞬間が、その全数を**手で通す**合図である
  (この件は上流の実 file を落として 4 通り当てたら 5 分で片付いた)
- 🔴 **fixture のゼロ件の次元は「測っていない次元」**。本文が短いと逆向きパッチが一度も
  生まれず、パッチ経路を壊す変異が素通りした。🔑 **その次元が非ゼロか**を test 自身に
  assert させる(`expect(chain.some((r) => r.kind === 'patch')).toBe(true)`)
- 🔴 **どの test からも実行されない file に、判断を書かない**(2026-08-08)。`src/main.ts` は
  **原文を `readFileSync` で読む test しか無い** ── 配線をそこへ直書きしたら、「設定を切った
  だけの user を既読にする」型の取り違えが**全 2385 tests 緑のまま**通った。🔑 **取り出せば
  test できる**(`update-card.ts` が同じ理由で取り出されている前例)。⚠ 取り出せないもの
  (boot の呼び出し順)は原文 pin で妥協するが、**弱いと自覚して使う**
- **worker は node で動く**(`tests/adapter/storage-worker.test.ts` の手法: `self` /
  `postMessage` を差して実物を dynamic import、`:memory:` fallback)。「worker の中だから
  unit では届かない」は誤り ── smoke 1 本に頼ると、向き・題名・上限の変異が誰にも守られない
- 🔴 **表に載っている件数は「誰かが数えた分」でしかない**(2026-08-07 ── 未実行の**全数版**)。
  ライブエディタの釣り合いは `ok:false` が **4 件**と記録されていたが、外 14 形 × 内 8 形を
  **全数**当たると **48 / 112 通り**壊れていた ── 数えていなかったから記録も無かった。
  🔑 「既知の欠陥は N 件」を出発点にしない。**組み合わせが有限なら全部当てる**
  (当てた表がそのまま回帰 test になる)

### 3. 変異試験そのものの罠 ── 「殺せた / 生き延びた」を信じる前に

- 🔴 **着地前に変異試験を回す**。実装を意図的に壊し、test が落ちることを確かめる
  (`cp` でバックアップ、`git checkout` は使わない)。落ちなければ、その test は**その機構を
  守っていない**。実績: 1 PR あたり 1〜3 件が生き残り、その多くが「既に壊れているのに
  鳴っていない」だった。⚠ **検品する側・test する側も対象**にする(§1)
  ⚠ **2026-08-14 にこの戒めそのものを踏んだ** ── 変異試験の**後始末**に `git checkout` を
  使い、未 commit の workflow 変更を失った(入れ直した)。⚠ CLAUDE.md と
  `.claude/skills/mutation-testing/SKILL.md` の**2 か所に書いてあって止まらなかった**。
  🔑 文言を 3 か所目にせず、**戻しをハーネスの `finally` に閉じ込める**(手で戻す手順を残さない)
- 🔴 **結果は `KILLED` / `SURVIVED` / `NOT-APPLIED` の 3 値で出す**
  (= **「当たらなかった変異」と「生き延びた変異」を区別する**。2026-08-04、1 セッションで
  2 度踏んだ)。変異が**適用されていない**とき、結果は「生存」と見分けがつかない ──
  **空振りを合格と読む**。① shell の引用で python が SyntaxError になり変異が当たらなかった
  ② `  armDpr();` という置換文字列が、狙った初回呼び出しではなく `onDpr` の中の
  **4 字下げの別行に部分一致**して先にヒットした。🔑 走りは**その場の shell ではなく file に
  出す**/置換前に「元の文字列が在ること」を assert して**当たったことを確かめる**。
  ⚠ 置換文字列は**前後を含めて一意にする**(`  x();` のような短い断片は必ず別行に刺さる)
- 🔴 **smoke に変異を当てるには build が要る**(2026-08-05 ── 上の**別の顔**。当たらない原因は
  shell のクォートだけでなく、**検査対象が生成物であること**もある)。`tests/smoke` は
  `vite preview` で **`dist/` を配信**するので、source を書き換えただけでは**一切届かない**。
  印刷 CSS の変異 6 件が全部「SURVIVED」に見えて「@media print は誰も守っていない」と
  読み違えかけた(真相は 6 件とも NOT-APPLIED)。🔑 **`npm run build` を挟み、
  `dist/assets/*.js` に「変異後の文字列が在り、変異前の文字列が消えた」ことを確かめてから**
  test を回す
- 🔴 **ハーネスが、直したばかりの実装を巻き戻すことがある**(2026-08-10)。バックアップ用の
  ディレクトリを**使い回した**ため、スイープの最後に**修正前のコピーが書き戻され**、その状態の
  まま commit して CI が赤くなった(typecheck 3 件 + flags の全数検査)。手元では
  **スイープの前**に緑を見ていたので「通ったのに落ちた」に見える。🔑 ① バックアップは
  **変異を当てる直前に取り直す**(使い回さない・終わったら消す)② **スイープの後にもう一度**
  typecheck / lint / test を回してから commit する ── 変異試験は**作業ツリーを触る操作**である
- ⚠ **変異自体を疑う**。発火しない形で当てて素通りしたことがある
- ⚠ 「添付が ZIP に入るか」のような**下流の結果**だけを見る test は、別経路が救って変異を
  見逃す。**壊れる当の振る舞い**(書き換えが起きるか等)を直接見る
- 🔴 **stub は本物の意味論を真似る**。stub が実装より正しいとバグが隠れる(fake の
  `getRevision` が保存形をそのまま返し、本物が materialize する食い違いを隠していた ──
  「鎖のまま往復する」test が壊れた実装でも通った)

### 4. 観測点の選び方 ── 何を見れば「届いた」と言えるか

- 🔴 **観測点が「放っておいても変わる」なら、変化は届いた証拠にならない**(2026-08-13)。
  canvas しか無い相手(LO wasm)で「絵の hash が変わったか」を届いた判定に使ったが、Writer には
  **点滅するカーソル**が在るので、**何もしていないキー**でも `landed: true` になった。
  🔑 **間隔をあけて数枚撮り、集合ごと入れ替わったときだけ**「変わった」と言う。⚠ 逆側も要る
  ── **手順の先頭に「効くはずの一手」を対照群として置く**(この件では「ただの文字を打つ」)。
  それが届いていない回は、**以降の判定が全部無意味**である。実際これが無ければ
  「ダイアログが開かないのは LO wasm の制限だ」と**存在しない結論**を書いていた
  (真相は合成 focus では入力先が決まらないこと)
  - ⚠ **逆向きも同じ**(2026-08-14)。窓の**枚数**で「命令が効いたか」を見たら、
    メニューが閉じる(−1)とダイアログが開く(+1)が**相殺して 0** になり、効いているのに
    「効かなかった」と読んだ(`build/office-wasm/io-layer-probe.mjs`)。
    🔑 **増減が同時に起きる相手では、数ではなく個体**(題名 / id)**で見る。**
    ⚠ そして観測点の生死は**窓ごとに違う** ── 同じ `textContent` が、LO の主窓では
    `Enable Screen Reader` 固定なのに、Qt のダイアログ窓では題名が読める。
    「この観測点は死んでいる」と書くときは**どの面でか**まで書く
- 🔴 **一致の件数は、因果の証拠ではない**(2026-08-14)。#117 の fault と「メニューの項目が
  効かない」が **7/7 で一致**したので、「fault が LO の命令発行を殺している」を**確定として
  扱い**、その線で probe を 2 本書いた。⚠ ところが user の実機では `Cmd+A` と `Cmd+B` が
  **両方効いた** ── **7/7 は別の現象を数えていた**。⚠ しかも支える側は **fault 有り 2
  サンプル**しかなく、「7/7」の見かけの強さはほとんど**対照群 5 回**のほうから来ていた。
  🔑 一致を見たら、次に測るのは**「仮説が真なら必ず壊れる 1 手」**である。
  ⚠ **既存の記述と一致した**ことを補強に数えない ── その記述自体が同じ推測から
  生まれていることがある
- 🔴 **自分が測った量と、user が見る量が違うことがある**(2026-08-12)。Office の窓を
  「器いっぱいにした」と実測つきで報告したが、測っていたのは**客域(canvas)だけ**だった ──
  Qt はその**外側に**枠 8px と疑似タイトルバー 22px を描くので、**窓全体は 30px はみ出し、
  LO のステータスバーが画面外に落ちていた**。数字は正しく、主張が的を外していた。
  🔑 **「直した対象」ではなく「user が見る面」を測る** ── 内寸を直したなら**外寸**を、
  1 経路を直したなら**画面に出る最終形**を。⚠ 実測を添えると説得力が出るぶん、
  **的を外した実測は嘘より始末が悪い**(こちらも相手も「確かめた」と思ってしまう)
  - 🔴 **「配った先」でも同じ型が起きた**(2026-08-14)。Impress を焼いて PKC3 の prerelease
    `lo-wasm-dev` に出し、「user に Impress が届きます」と報告した ── ⚠ **user が取る先は
    そこではない。** アプリが読むのは同一 origin の `/office-pack/pack.json` で、
    **GitHub の release 資産は CORS で原理的に取れない**(ACAO 0 件 / OPTIONS 405 を実測)。
    しかも配布 Pages を組む `make-pages-bundle.mjs` は **PKC3 の CI から 1 度も呼ばれていない**
    ── **配布は 2 段**で、2 段目は別 repo(`sm06224/office-pack`)の Pages である。
    ⚠ 露見したのは外部の検証レポートが**古い目録のまま**だったからで、こちらの計器は
    1 つも鳴らなかった。🔑 **「焼けた」と「届いた」の間に何段あるかを数えてから報告する。**
    ⚠ 根は**名前**に在った ── workflow 自身が `lo-wasm-dev` を「**配布 tag**」と呼んでいる。
    **取れない先を配布と呼ぶと、次に読む人も同じ読み違えをする**
- 🔴 **ネットワークの event を「飛んだ / 止まった」の判定に使わない**(2026-08-06、外部画像の
  同意で踏んだ ── 環境差(§5)の**3 つ目の顔**)。CSP が止めた試行も `page.on('request')` に
  **上がる**(直後に `requestfailed { errorText: 'csp' }` が続き、応答は 1 度も返らない =
  bytes は出ていない)。ところが**その到着の時期がビルドでまるで違う**:
  `chromium_headless_shell` は assert より先に上がり、`chromium` は 5 秒待っても上がらない
  ことがある ── `request` を数えれば**「外へ飛んだ」という誤った読み**、`requestfailed` を
  待てば**永遠に来ない**(どちらに寄せても片方のブラウザで落ちる)。🔑 観測点は
  **①アプリ自身の信号**(この件では「確認の帯が出たこと」= 箱の中で違反が実際に起きた証拠)
  + **②応答が返らないこと**の 2 つにする。⚠ ②だけでは**空振りでも通る**(箱の script が
  動かなくても応答は来ない)── だから①が要る。⚠ 逆に「試行そのものが起きない」を主張する
  test(`src` を持たない `<img>`)では `request` が正しい観測点である ──
  **主張が違えば観測点も違う**
- ⚠ **環境の性質をアプリの不具合と読み違えない**。この headless Chromium は**非 ASCII の
  `<a download>` 名を丸ごと捨てて `"download"` にする**。実データの題名はほぼ日本語なので、
  `download.suggestedFilename()` を観測点にすると必ず詰まる ── **アプリが制御している値**
  (属性そのもの)を見る

### 5. 環境差 ── CI と手元で別のものが動いている

- 🔴 **CI と手元で別のブラウザが動いている**(2026-08-05)。`tests/smoke` の config は同梱の
  `/opt/pw-browsers/chromium`(= フル Chromium)を優先し、無ければ playwright 既定に落ちる ──
  **PR gate は後者 = `chromium_headless_shell`**。`window.print()` が chrome では
  `beforeprint` のみ、headless_shell では **`beforeprint` + `afterprint` を同期発火**するため、
  「押した直後に組み上がっている」を見ていた smoke が**手元で緑・CI で赤**になった
  (しかも CI 側が正しい)。🔑 実ブラウザ依存の挙動に触れる spec は**両方のバイナリで通してから
  push**(`PKC3_CHROMIUM=/opt/pw-browsers/chromium_headless_shell-1194/chrome-linux/headless_shell`)。
  🔑 そして観測点を**環境差に強い側**へ寄せる(→ §4)── この件では「押した直後」ではなく
  **`beforeprint`(印刷が始まる瞬間)**が、どちらのビルドでも成立する唯一の点だった
- 🔴 **2026-08-07 追記: この規律は nightly では破れていた。** nightly が probe 用の
  `PKC3_CHROMIUM` を **`$GITHUB_ENV` へ**書いており、以降の全 step ── smoke を含む ── に
  効いていたため、**nightly の smoke だけフル chromium** で走っていた(「両方で通してから
  push」の規律が、**CI 側で静かに裏切られていた**)。いまは probe の step にだけ `env:` で渡し、
  **nightly が smoke を 2 つのブラウザ両方で回す**(突き合わせる場所は nightly しか無い)。
  🔑 **`$GITHUB_ENV` は「以降の全 step」に効く** ── 1 つの step のために書いた値が、無関係な
  step の**検査対象を変える**。step 単位で効かせたいなら `env:` を使う
- 🔴 **環境差の調査が本物のバグを見つけることがある**(同 2026-08-05)。上の件を headless_shell で
  追ったら `Not allowed to load local resource: blob:null/…` が出た ── **画像の読み込みが
  終わる前に印刷が完了し、`afterprint` の revoke が読み込み中の blob URL を消していた**
  (= 紙から画像が落ちる実バグ)。chrome では `afterprint` が来ないので**永久に露見しなかった**。
  「CI だけで落ちる」を環境のせいにして test 側だけ緩めていたら、バグごと埋めていた

### 6. shell と CI が「失敗した」を食べる

🔑 どちらも**構文が「失敗した」という事実を食べてしまう**同じ罠の 2 つの顔である。

- 🔴 **`| tail` に通すと、落ちたことが消える**(2026-08-13)。パイプラインの終了コードは
  **最後の命令のもの**なので、`node probe.mjs … | tail -40` は probe が例外で死んでも
  **exit 0** を返す。⚠ 背景実行の完了通知にも「exit code 0」と出るので、**「測れた」と
  読んでしまう**(実際に読みかけた ── 中身に `TimeoutError` が在った)。🔑 出力を絞りたいなら
  **file に落としてから読む**(`cmd > out.log 2>&1; echo "exit=$?"`)。
  ⚠ `set -o pipefail` に頼らない(対話の shell では効いていないことがある)
- 🔴 **`&&` と `||` は同順位・左結合**。CI の条件付き実行を `[ -f X ] && cmd || true` と書くと
  `(([ -f X ] && cmd) || true)` になり、**「無いとき」ではなく「失敗したとき」も飛ばす**。
  実証: 検品が `✗` を出した直後に deploy 行へ到達し **step は exit 0**(不良物を配って job は
  green)。`if [ -f X ]; then cmd; fi` と書く

### 7. 同じ値・同じ判定が複数の場所にある

- 🔴 **同じ値を複数の描画経路へ渡すものは、経路ごとに pin する**(2026-08-06、外部画像の同意の
  変異試験で 4 件が生き延びて判明 ── **生き延びた 4 件は全部これだった**)。1 つの設定が効く先が
  複数あるとき、**代表 1 経路の test は他の経路を 1 度も通らない**。実例(どれも渡し忘れが
  緑のまま通った): ① **csv fence のセル**は文書の `env` ではなく**使い捨ての env** で描く
  (脚注が漏れる事故を防ぐための作り)── markdown の表の test はこの経路を通らない
  ② **添付の説明文**は本文とは別に、その場で同期に描く ③ **設定画面の値の同期** ── 器は
  1 度しか組まないので、映さないと古い値が見える。画面上その操作が起きないもの
  (「常にオフ」中に同意が来る等)も**誰も見ていない**。🔑 「この値を読む場所」を `grep` で
  数え上げ、**数えた数だけ test を持つ**。⚠ 「主要な経路で効いているから大丈夫」は、この型の
  欠陥をそのまま出荷する言い方である
- 🔴 **判定を増やさない。誤差の向きを決めて、両側に使い回さない**。「どれを含めるか」は
  false-keep 側(広く拾う)、「どこを書き換えるか」は誤爆しない側(狭く当てる)── 片方の規則を
  もう片方に流用すると、誤差が**データ欠損の向き**へ反転する。同じ判定が 2 か所に生えたら、
  **規則を 1 つに寄せ、「A が keep するものは B にも必ず入る」parity test** を置く
  (実例: `features/asset/asset-ref-scan.ts` / `frontmatter.ts` の原文 splice)
- ⚠ **同じ「参照を拾う」でも、場所によって受け方を変える**(2026-08-02、
  `scripts/dist-inspect.mjs`)。**構造化された場所**(HTML 属性 / JSON field)は散文が
  混じらないので緩く、**コードの中**は狭く当てる ── 出荷 bundle には
  ``…invoked from`,`client-level…`` のような散文が実在し、「hash らしき形」で拾うと
  `sqlite3-vfs-opfs.js` / `markdown-it-footnote.js` を誤検知して**release を偽の理由で止める**。
  形ではなく**構文**(`new URL(…)` / `import(…)`)で拾う
- 🔑 修正側の顔は下節の「**片側を直したら、対称の反対側を必ず疑う**」(2026-08-07)である ──
  1 か所直したら、同型の残りを `grep` で数え上げる

### 8. 上流と生成物を取り込む場所

- 🔴 **上流の「入れる物の一覧」は、上流自身の変更に遅れる**(2026-08-13、#135 / #144)。
  LibreOffice は表の自動書式を `autotbl.fmt` → `tablestyles.xml` へ移したのに、wasm の
  詰め込み一覧は**古いほうを入れたまま**だった。読み手は開けなければ `SAL_WARN` して
  `return false` ── ⚠ **無言で空になる**ので、症状は「一覧が空 → 添字 `-1` → 範囲外アクセス」
  という**遠く離れた形**でしか出ない。🔑 **上流の成果物を取り込む場所では、「コードが読む
  path」と「配ったものの中身」を全数で突き合わせる** ── 1 件の症状から 1 件だけ直さない
  (数えたら **4 件**欠けており、`share/svx/` と `share/calc/` は**ディレクトリごと**入って
  いなかった)。⚠ 上流の既定値も同じ ── `--with-wasm-module` の既定が `'calc writer'` だった
  ため **Impress は 1 file も入っていなかった**(#145)。「入っているはず」を確かめる
- 🔴 **上流の実物は clone して読む。名前も既定値も、推測で渡さない**(2026-08-14、#158 で
  **7 回転**を溶かした)。日本語 UI を建てるのに `--with-lang` を渡す → `msgfmt` が無い →
  検品の grep が落ちるが**何と食い違ったか 1 行も読めない** → 実物を出す →
  `--disable-wasm-strip-locales` は**上流に無い** → 親スイッチへ、と回り続けた。
  最後に**上流を checkout して全数 grep したら、前提が 3 つとも誤り**だった:
  ① `ENABLE_WASM_STRIP_LOCALES` は **死に変数**(上流全体で一致 1 件 = `config_host.mk.in:265`。
  makefile も C++ も**誰も読まない**)② `--disable-wasm-strip` は **Emscripten では効かない**
  (`configure.ac:1301` が host case で `enable_wasm_strip=yes` を**無条件に上書き**する。
  ⚠ 仮に効いたら `configure.ac:3457-3505` が発火せず `enable_dynamic_loading` まで戻るので、
  **wasm はリンクしない**)③ `WITH_LANG` は**出る file が違う**だけ(`config_host_lang.mk`)。
  ⚠ **決定的な誤りは「空になった変数」を「スイッチが効いた証拠」と読んだこと** ── 実際は
  `--with-wasm-module`(#145)と、`config_host.mk.in` の**右辺が空リテラルの行**によるものだった。
  🔑 **上流を触る回転が 2 回を超えたら、推測をやめて clone する**(実際それで 1 回で片付いた)。
  ⚠ そして真因は上の bullet と**同型**だった ──
  `static/CustomTarget_emscripten_fs_image.mk` が `en-US` を**名指し**で焼き、
  `program/resource/**`(`.mo`)を **1 行も入れていない**。configure のレバーではなかった

### 9. 編集の作法 ── 自分と他人のツリーを壊さない

- 🔴 **整形ツールを既存 file に掛けない**。`npx prettier` を掛けて `main.ts` を全面リフォーマット
  (302 行差分)し、リポジトリで唯一 double quote の file にした。diff が読めなくなり、
  次に触る PR が全部 conflict する
- 🔴 **制御文字をソースに生バイトで埋めない**(``'\u007f'`` と書く)。「正規表現に書かない」と
  注意書きしている当の file で 3 度踏んだので、`npm test` の `tests/repo-hygiene.test.ts` が
  機械的に止める
- 🔴 **内部の名前を、既存の id の前置きで始めない**(2026-08-06)。postMessage の種別を
  `pkc-html-blocked-images` ではなく `pkc-html-render-blocked` と名付けたところ、箱の id が
  `pkc-html-render-<hash>` なので **goldens の正規化(id を連番へ潰す処理)が message 種別も
  一緒に書き換え**、diff が別物に見えて原因を 1 往復探した。⚠ id を機械的に潰す処理が在る
  リポジトリでは、**「id らしく見える名前」は id として扱われる**
- 🔴 **書き込み権のあるサブエージェントを、自分の作業ツリーで並行に走らせない**(2026-08-04)。
  レビュー用に投げたエージェントが**私の編集中の file を変異させ、戻さずに終わった**
  (`// MUT-A: revoke removed` が `rasterize` の `URL.revokeObjectURL` を消していた)。自分の
  変更と混ざるので diff でも気づきにくい。読み取りだけならよい。書くなら**待ってから**か、
  worktree を分ける

## 資産(`.claude`)── PKC3 にも置く(user GO 済み)

**規律の正本は本 `CLAUDE.md`**(とくに不可侵指示と「検証の規律」)。
**手順と道具**は `.claude/` に在る。

| 置き場 | 中身 | 隔離 |
|---|---|---|
| `.claude/agents/pkc3-reviewer.md` | 着地前の敵対的レビュアー | **read-only** |
| `.claude/agents/pkc3-surveyor.md` | 実地調査員(file:line で根拠) | **read-only** |
| `.claude/agents/pkc3-implementer.md` | 実装を 1 主題ずつ分担する | 🔴 **worktree** |
| `.claude/agents/pkc3-verifier.md` | 変異試験を回す | 🔴 **worktree** |

⚠ **`isolation: "worktree"` が起動できない箱がある**(2026-08-14 実測
`no WorktreeCreate hooks are configured`)。🔴 **そのとき書けるエージェントを隔離なしで
起動しない** ── 「patch だけ返して」と頼むのは退避策ではない(`Write`/`Edit` を持ったまま
走るので、守っているのは指示だけである)。正しい退避は **read-only の型に下書きさせ、
依頼者が当てる**こと。🔑 **規律を守るのは tools の一覧であって、プロンプトの文言ではない。**
手順は `.claude/skills/subagent-scale/SKILL.md` §1

| スキル(手順) | 中身 |
|---|---|
| `subagent-scale/` | **大量に投げて突き合わせる**(上の方針の実体)/ 隔離 / 検算 |
| `mutation-testing/` | 変異試験の手順 + **動くハーネスの雛形** |
| `perf-measurement/` | **測って示す**(対照群 / 差分ハーネス / 被覆と感度 / 主張の範囲) |
| `smoke-testing/` | 実ブラウザ検証(2 つのブラウザ / 観測点の置き方) |
| `cowork-verification/` | **実機の検証を外部へ頼む**(指示プロンプトの骨組み / 報告様式) |
| `source-editing/` | 編集の作法(生バイト / template literal / 整形ツール) |
| `notice-writing/` | **お知らせを書く**(登記表の場所 / 上限 / 書いてはいけないこと) |
| `pr-landing/` | PR を作って着地させる(1 主題 / 止めて裁定を仰ぐ条件) |
| `session-handoff/` | 引き継ぎ **PR** の作り方(最初の仕事の有無は必須) |
| `knowledge-reflection/` | **教訓を資産へ分割して残す**(`.claude更新`) |

コマンド(導線): `/fanout` `/review` `/mutate` `/measure` `/handoff` `/claude-update`

- 🔴 **`.claude/skills/` を指す記述は、指す先が実在するときだけ書く**。
  2026-08-04 の引き継ぎ doc が 3 か所で存在しない `.claude/` を指し、
  「罠の正本は `.claude` 側」と書いたせいで**罠がどこにも残らないところだった**(#69)
- 🔴 **user 由来の金言的プロンプト(不可侵指示・裁定記録・プロセス指示)は
  `.claude/` へ移さない。** 本 file に在ることに意味がある ── 毎回読まれるのはここである。
  スキルへ移してよいのは**手順**だけ
- ⚠ **書き込み権のあるサブエージェントを自分の作業ツリーで並行に走らせない**
  (2026-08-04 に編集中の file を変異させられた)。だから上の 2 つは read-only である
- 🚫 PKC2(`sm06224/PKC2`)の `.claude/` は **read-only 参照のみ**。丸写しにしない
  (user 指示「流用 + 総合的見直し」)

### 2026-08-04〜07 に足した原則(手順は上のスキルに在る)

- 🔴 **片側を直したら、対称の反対側を必ず疑う**(2026-08-07)。検品を「出力の文字列を
  見る」形に直したのに、**トークンだけ直して規則は需要側の数を見たまま**だった ──
  組み立てを消すと検品が合格し、**規則 0 本の CSS が出荷される**(実測)。
  🔑 **1 巡目の修正は、2 巡目の対象である**
  - ⚠ **同じ日に 3 回踏んだ。**(1) 情報ペインとファイラの「器を捨てない」を直したのに
    **本文の面が同型のまま残り**、回帰 test も**その 2 面しか import していなかった** ──
    保存直後の「編集」が無言の dead click になっていた (2) `:::` の空行正規化は
    在るのに**改頁(`+++`)側に無く**、素直に書くと改頁が消えて `auto` の字が出ていた
    (3) parity test は `app.css` 側だけコメントを剥いでいて、**書き出し側は剥いでいなかった**。
    🔑 **「A を直した」と書いた瞬間に「B はどうか」を grep する**。
    とくに **test の import 一覧**は、直し忘れた面が一目で分かる場所である
- 🔴 **裁定を受け取ったら、その場で起票するか資産に書く。** 会話に流すと消える ──
  GO 済みの件を起票せず、**裁定済みを user に聞き直す**事故になった(#91)
  - 🔴 **裁定を「待っている」ものも起票する**(2026-08-15。user 指摘で判明)。
    移植候補の doc(削除済み。中身は #180 と個別 issue へ移した)は **22 件**を
    「`[ ]` を `[x]` にして返してください」の形で並べた doc だが、**11 日間 1 件も
    動かず**、その間 open issue を数えても PKC 本体の残件は**2 件しか見えなかった**
    (16 件が Office)。⚠ **doc に置いた仕事は、待っている限りどこにも積まれない** ──
    「裁定待ち」は状態であって、**台帳から消えてよい理由ではない**。
    🔑 待ちが 1 回でも空振りしたら、**待つのをやめて doc 自身の推薦から着手する**
    (止まっているほうが害が大きい)。台帳は #180
  - 🔴 **古い調査から起票するときは、起票の直前に現状を確かめる**(同日、user 指摘
    「**対応済みを誤認したらどうする?その抜けをお前はフォローできるのか?**」)。
    11 日前の doc から 6 件を起票したら、**2 件が事実と食い違っていた** ──
    A-3「固定順のみ」は**手動の並べ替えが既に実装済み**(`binder.ts:762`)、
    A-7「データはあるのに触れない」は**親子関係は情報ペインに出ている**
    (`inspector.ts:126`)。⚠ 誤った起票は**自分では気づけない** ── 次に読む人が
    「無い」を前提に作り、既に在るものを二重に作る。
    🔑 **1 件 1 grep でよい**(実際 2 分で 2 件見つかった)。書くのは「doc にこう
    書いてある」ではなく **「いま実装がこうである」**
  - 🔴 **仕事を doc の形で置かない**(user 指示 2026-08-15。**不可侵**):
    「**ファイルをただ置いておくのはやめろっていったよな?**」──
    **やること**は issue に置く。doc に書いてよいのは**設計と根拠**(なぜそうするか)
    であって、**残件の一覧を doc に持たせない**。⚠ doc の一覧は
    ①誰も burn down しない ②古くなっても誰も気づかない ③**既に実装済みの項目が
    残り続ける**(今回 2 件がそうだった)。移植候補 doc は #180 + 個別 issue へ
    移して**削除した** ── 参照していた 2 か所も同時に張り替えた
    (⚠ 実体を消したら**壊れたポインタを残さない**、#69 の教訓の逆向き)
- 🔴 **user に見える仕様を、レビュー応答の commit の中で勝手に決めない。**
  会話で 1 行確認する(2026-08-07「紙のリンクを黒に落とす判断の取り下げ」で踏み、
  確認した結果 **A: このまま**が裁定された)
- 🔴 **編集ツールが制御文字を生バイトで書くことがある。** escape を書いたつもりでも
  入る(1 セッションで 3 回)── 書き換えたら**バイト走査する**
  (`source-editing/`)。⚠ 制御文字を避けられるなら私用領域(``)を使う
- ⚠ **flake に見えるものが製品の穴だったことがある**(2026-08-07、`external-images` /
  `boot-edit` の **2 件**)。**test を緩める前にアプリ側を疑う**。直したら
  **確定的に鳴る unit** を足す ── ただし「字面の位置」で pin すると、位置を保って
  挙動を壊す変異が生き延びる。
  🔑 `boot-edit` の `Element is not attached to the DOM` は**再現しなくても**
  正体があった ── 「1 回通ったから直った」ではなく、**要素が作り直される経路**を
  コードで名指しできるまで追う(retry で消せる症状は、原因の側に門を置く)
- ⚠ **実体を作ってから導線を書く。** 逆順は壊れたポインタになる(#69 で 1 度失敗)

> ⚠ **この節は 2026-08-07 まで「⏸ user の裁定待ち」と書いていたが、嘘だった。**
> GO は 2026-07-30(プロセス指示「品質はサブエージェント・スキルで守る」)/
> 2026-08-05(「記法網羅試験をサブエージェントでやった方がいい」)/
> 2026-08-06(「**いずれにしてもサブエージェントを駆使してしっかり実装を分担して
> 実施願います**」)で出ていた。読んだうえで**起票も作成もしなかった**ため、
> 裁定済みの件を user に聞き直す事故になった。残りは #91。
> 🔑 **裁定を受け取ったら、その場で起票するか資産に書く** ── 会話に流すと消える。
